# 产物：对话里的 `GENERATED · N` 与坞的「产物」格

> 分支 `data-platform` 的第一份 spec。回归路线图主线：让「这一轮生成了什么」成为**从事实算出来的、看得见的**东西（不变式 5），
> 为 S16 / S23 先把 `filesCreated` 这条事实记进账本。参考 wisp-science 的 `GENERATED · 5` 产物条与右栏 `Artifacts (5)` 清单。
>
> **作者两次收窄（2026-08-26）**：第一版把这一格做成了「版本 + 来源三页签 + 图上批注」，作者说*「差点意思……其实就增加一个产物就可以了，
> 可以实时查看生成的东西，以及在对话聊天内容里面也展示生成的东西就可以了」*。那些整体挪到 §9「下一轮」。

## 0. 作者定下的

| 问 | 定 |
|---|---|
| 本轮范围 | ① 对话里的产物条 + ② 坞的「产物」格（一张实时清单 + 预览）。③ 笔记本以后再说 |
| `GENERATED` 算哪些 | **只算这一轮新建的**。改动已有文件属「审阅」格 |
| ② 落位 | **坞加一个房客「产物」**；文件格保持看文件系统；预览组件两格共用 |
| 数据层 | **产物 = 从 Run 推导，不建产物表**：Run 加 `filesCreated`。不存文件内容（S18 作者早已否决） |
| 挪到下一轮 | 版本切换、来源（代码 / 执行日志 / 环境）、图上批注 |

## 1. 今天的实际状态（动手前核的）

- 每次工具调用都是一条 Run（`requestType = tool_call:<工具名>`），带 `filesWritten`（git 前后 `status --porcelain` 算的）、`mayIncludeUserEdits`。缺省 = 不知道，空数组 = 确认没改（R3 的口径）。
- `filesWritten` **不分新建与改动**。但 `--porcelain --untracked-files=all` 的 `??` 行、以及 `policy/artifacts.ts` 的产物登记（按设备号 + inode + 出生时间登记「此前不存在、现在存在」）都知道哪些是新建的——只是没记进账本。
- 探针（`runtime/provenance.ts`）只跑本地 git。远端会话、PTY agent（claude / codex 内置工具）的文件事实都是「不知道」。
- 坞（`ui/state/right-dock.ts`）现有房客：审阅 / 文件 / 概览 / 网页 / 团队。**终端不在这里**，它是底部另一个坞。文件格有图 / CSV / PDF / 文本三态预览，远端预览 = 一次下载。
- `@` 引用语法已支持本地与远端路径，路径 + 类型不喂内容。

## 2. 数据层：Run 加一列 `files_created`

schema 15 → 16，事务迁移。与 `filesWritten` 并列、**同一口径：缺省 = 不知道；空数组 = 确认没新建**。老 run 一律缺省。

算法在包装器（`runtime/native.ts` 的 `wrap`）里：

```
filesCreated = (porcelain 的 ?? 行) ∪ (产物登记里「此前不存在、执行后存在」的)
```

交给 `tool_files` 事件多带一个 `filesCreated` 字段，`run-recorder.ts` 照写。
- 探针起来了（git 工作区）：两半都有。
- 非 git 工作区：探针不起，**这次一律「不知道」**（与 `filesWritten` 既有口径一致；产物登记只在探针起来时并进去）。补它是下一轮的事（§9）。
- 远端会话、PTY agent：缺省。
- 覆盖已有文件不算新建（登记的既有规则）。

**产物是查询，不是表**：

```
产物(sessionId) = ∪ runs(session).filesCreated    按出生 run 的 startedAt 排序
```

跨会话不算（另一段会话生成的进不了这一格）。删掉后仍列，标「已不存在」——它曾被生成是事实。

## 3. 协议（纯新增，minor 版本）

一个只读操作（`mutating: false`，无 `sideEffects`）：

```
listArtifacts { sessionId }
  → { artifacts: [{ path, kind, bornRunId, bornAt, exists }], unknown: { runId, toolCallId? }[] }
```

- `unknown` = 这段会话里 `filesCreated` 缺省的那几次（run，细到工具调用时带 `toolCallId`）。**界面必须显示它**。写成清单不写成总数，是因为产物条要按轮数未知——「这一轮有没有不知道的」得对得上 run，光有总数不够。
- `kind` 由后缀映射（`ui/file-kind.ts` 那一套搬到共享层），未知后缀 = `other`，不猜。
- `exists` 服务端查实（`lstat`），远端会话走已有的 `pathInfo`。
- 会话更新事件 `artifactsChanged { sessionId }`：run 结束且 `filesCreated` 非空时推一条，客户端重拉。事件只说「变了」，数据走查询（与 `dropItem` 同一纪律）。
- **不新增转录项类型**：产物条是按 turn 派生的视图——agent turn 结束后从 `listArtifacts` 筛出 `bornRunId` 的父 run 属于这一 turn 的那些（`parentRunId` 链已有）。历史会话打开也能画出来。
- **mock**：`scripts/mock-inference-server.mjs` 同一改动里补操作与事件（准入规则 1）；e2e 夹具让 `run_code` 真写出一张 png。

## 4. ① 对话里的产物条

位置：agent 那一轮回复的底部，标签 `GENERATED · N`。三种状态，**「不知道」不许画成「没有」**：

| 状态 | 判据 | 画法 |
|---|---|---|
| 正常 | 本轮有 `filesCreated` 非空的 run | chip（类型徽标 + 文件名），按出生顺序；已删的划掉标「已不存在」；超过 8 个折叠成「还有 N 个」；悬停显示相对路径全名 |
| 未知 | 本轮所有 run 的 `filesCreated` 都缺省 | 一行「本轮产出未知」+ 原因（外部 CLI 的文件操作不可见 / 远端会话 / 探针没跑）。**agent 声称的文件一个不列** |
| 混合 | 部分知道 | 列出数得出的，再加「另有 N 次运行产出未知」 |
| 确认没新建 | 全是空数组 | **不画**。`GENERATED · 0` 是噪音 |

点 chip：打开坞的「产物」格并选中它（坞关着就开）→ 直接进预览。
⋮ 菜单（与产物格里那份同一个组件）：在文件格里定位 · 用系统应用打开 · 复制路径 · 插成 `@` 引用。

## 5. ② 坞的「产物」格

坞的一个新房客，标签 `产物 (N)`，N 实时跟着变。两个视图：

**清单**（默认）：
- 按目录分组，组头 `outputs/ · 5`，可折叠；组内按出生时间。
- 一行 = 文件名（截断带 title）+ 类型徽标 + ⤓ 下载 + ⋮ 菜单（§4 那份）。
- 顶上「另有 N 次运行产出未知」（N > 0 才显示）。
- 已删的标「已不存在」，不移除；⤓ 与预览禁用。
- 空态：「这段会话还没有生成文件」——与「未知」分得开。
- **实时**：收到 `artifactsChanged` 就重拉；新长出来的行不闪不跳，清单保持滚动位置。

**预览**：点一行 → 这一格里显示（**复用文件格的预览组件**，三态；远端 = 一次下载），头上一条「← 返回清单 · 文件名 · outputs/ · 本机」+ ⤓ ⋮。

面板状态（清单 / 预览、选中项、折叠）按**会话作用域**持久化（`src/ui/state/` 分家表，key 带 sessionId）。切会话不带过来。

## 6. 失败必须出声（规格 7.5）

| 情况 | 说什么 |
|---|---|
| run 的 `filesCreated` 缺省 | 「另有 N 次运行产出未知」+ 原因 |
| 产物已不存在 | 清单标「已不存在」；预览「找不到 outputs/x.png（曾由这段会话生成）」 |
| `listArtifacts` 失败 | 格里出错态 + 重试，不是空清单 |
| 远端预览下载失败 | 沿用文件格的出声 |

## 7. 分层

- 事实层：`store/runs.ts`（`files_created`）、`store/schema.ts` 迁移；`project/run-recorder.ts` 从 `tool_files` 写入。
- 探针：`project/git-facts.ts` 多算 `??`；`runtime/native.ts` 包装器合并登记结果发 `filesCreated`。
- 协议：`protocol/operations.ts` 一个操作、`protocol/events.ts` 一个事件、`protocol/entities.ts` 的 `Artifact`。
- 服务：`workbench/backend.ts` 实现查询。
- 界面：`ui/state/right-dock.ts` 加房客 `artifacts`；新 `ui/artifacts.tsx`（格）与 `ui/generated-strip.tsx`（条）；预览组件从 `ui/files.tsx` 抽成共用；状态进 `ui/state/`。
- mock：`scripts/mock-inference-server.mjs`。

## 8. 怎么验（准入规则 3）

- 单测：迁移（老 run 的 `filesCreated` 缺省）；`filesCreated` 算法（`??` ∪ 登记、覆盖已有不算、非 git 且工具声明了 = 登记结果、非 git 且没声明 = 缺省、探针失败 = 缺省）；产物查询（跨会话不算、删后仍列、按出生排序、`unknownRuns` 数对）；`listArtifacts` schema 双向校验；按 turn 派生的筛法。
- e2e（真构建产物 + mock）：`run_code` 真写一张 png → 产物条出现 → 坞标签 `产物 (1)` → 点 chip 进预览 → 返回清单。另一条：PTY 会话那轮画「本轮产出未知」且**不列 agent 声称的文件**。确认没新建的轮次不画产物条。
- 设计契约扫描：新文案不与已有按钮文案互为子串。
- 视觉基线：坞多一个房客会动几张基线——**先看 diff 再重存**。

## 9. 下一轮（作者收窄时挪出来的，讨论已做完、只等这一格用起来再决定要不要）

- **来源**：点一个产物看生成它的那次 run 的代码 / 执行日志 / 环境。需要账本加 `run_io` 表（S16 的 `*_preview` + `*_truncated`）与 `getRunIo` 操作；老 run 与 PTY 会话两种缺省分得开（`not_recorded` / `invisible_agent`）。
- **版本**：同路径被后续 run 又写 = v2；版本 = run 链，不存内容（尊重 S18 否决）。
- **图上批注**：钉一点 → `@outputs/x.png 在图上 (62%, 71%) 处：…` 落进输入框，不直发；不裁图。
- 非 git 工作区的 `filesCreated`（只靠产物登记）。
- 「消息」「审阅」页签；远端会话的 `filesCreated`（要在远端跑 `git status`）；③ 笔记本格与批量派单进度。

## 10. 顺序

数据层 → 协议 + mock → ① 产物条 → ② 产物格。每步可停，每步过一次 `dev:mock` 人肉看。
