# 产物：对话里的 `GENERATED · N` 与坞的「产物」格 · 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让「这一轮生成了哪些文件」成为账本里的事实（Run 加 `filesCreated`），在对话里按轮画成 `GENERATED · N` chip 条，在右坞新房客「产物」里画成实时清单 + 预览。

**Architecture:** 探针（`git status --porcelain` 的 `??`/`A` 行 + 工具声明路径的「此前不存在、现在有」）算出 `filesCreated`，随 `tool_files` 事件进账本（`runs.files_created`，同时记 `tool_call_id` 好让界面按轮对应）。产物 = 本会话所有 tool_call Run 的 `filesCreated` 并集（一条查询，不建表）。协议新增只读 `listArtifacts` 与会话事件 `artifactsChanged`。界面按转录里的 tool item id ↔ `toolCallId` 把产物挂到它所属的 agent 轮。

**Tech Stack:** TypeScript / zod 协议 / better-sqlite3 / nanostores / React / vitest / Playwright（真构建产物 + `scripts/mock-inference-server.mjs` 假模型）。

**Spec:** `docs/superpowers/specs/2026-08-26-产物-design.md`。**分支 `data-platform`。**

> **状态（2026-08-26）：14 个 Task 全部完成**，逐 Task 经规格审查 + 代码审查；提交 `9df6a6f`…`8a527d0`。细节与验证数字见 `docs/DEVELOPMENT_HISTORY.md` 同日条目。

---

## 动手前必读（工程师零上下文）

- 账本口径：**缺省 = 不知道，空数组 = 确认没有**。任何把 `undefined` 写成 `[]` 的改动都是编造（不变式 5）。
- `src/runtime/provenance.ts` 的探针只在 **git 工作区**起来；起不来就不发 `tool_files`，那条 Run 的文件字段全缺省。**本计划不改这条**——非 git 工作区一律「不知道」（spec §2 据此改口径，见 Task 12）。
- 事件顺序：`src/workbench/backend.ts:846` 先 `events.ingest(e)` 再 `runRecorder.ingest(e)`，两者同步；客户端收到 `artifactsChanged` 再经 IPC 来查时账本已写完。
- 转录里 tool item 的 `id` **就是 pi 的 `toolCallId`**（`src/workbench/events.ts:521`）。这是界面把产物对到轮次的唯一钥匙，所以账本要记 `tool_call_id`。
- 三条准入规则（`CLAUDE.md`）：新协议操作同一改动里补 e2e 覆盖；可判定规则配扫描；改主路径要 `npm run dev:mock` 亲眼看一次。
- 常用命令：`npm test`（vitest）、`npm run typecheck`、`npm run test:e2e:only -- e2e/artifacts.spec.ts`（先 `npm run build`）、`npm run test:e2e:visual`。
- 提交信息**不要**加任何 Co-Authored-By 行；**不要 push**。

## 文件地图

| 文件 | 责任 | 动作 |
|---|---|---|
| `src/project/git-facts.ts` | `createdSince()`：porcelain 里的新建文件 | 改 |
| `src/runtime/provenance.ts` | `ToolFileFacts.filesCreated`；声明路径「此前不在、现在有」 | 改 |
| `src/runtime/types.ts` | `tool_files` 事件加 `filesCreated?` | 改 |
| `src/runtime/native.ts` | 内置工具包装器把产物登记的新建并进 `filesCreated` | 改 |
| `src/store/schema.ts` | v16：`runs.files_created`、`runs.tool_call_id` | 改 |
| `src/store/runs.ts` | 读写两列；`artifactsOf(sessionId)` 查询 | 改 |
| `src/project/run-recorder.ts` | `tool_start` 记 `toolCallId`；`tool_files` 记 `filesCreated` | 改 |
| `src/files/file-kind.ts` | `文件类按名字` 从 ui 搬到共享层（后端要用） | 新建 |
| `src/ui/file-kind.ts` | 改成 re-export | 改 |
| `src/protocol/entities.ts` | `RunSummary.filesCreated/toolCallId`；`ArtifactSchema` | 改 |
| `src/protocol/operations.ts` | `listArtifacts` | 改 |
| `src/protocol/events.ts` | `artifactsChanged` | 改 |
| `src/protocol/version.ts` / `index.ts` | 7.23 → 7.24；导出 | 改 |
| `src/workbench/backend.ts` | `listArtifacts` 实现（含 `exists` 查实） | 改 |
| `src/workbench/events.ts` | `tool_files` 且有新建 → 推 `artifactsChanged` | 改 |
| `src/ui/state/catalog.ts` / `sync.ts` | `$artifacts`、`loadArtifacts` | 改 |
| `src/ui/state/right-dock.ts` | 房客 `artifacts` | 改 |
| `src/ui/views.tsx` | `房客名`；`ConversationView` 接 `artifacts` 并给每个 agent 轮挂产物条 | 改 |
| `src/ui/generated-strip.tsx` | `本轮产物()` 纯函数 + `GeneratedStrip` | 新建 |
| `src/ui/artifacts.tsx` | `ArtifactsPanel`：清单 / 预览 | 新建 |
| `src/ui/App.tsx` | 订阅事件、装配房客、传 props | 改 |
| `src/ui/i18n/en.ts` | 新文案英文 | 改 |
| `src/ui/styles.css` | `.generated-strip` `.artifact-*` | 改 |
| `tests/…`、`e2e/artifacts.spec.ts` | 见各 Task | 新建/改 |
| `docs/DEVELOPMENT_HISTORY.md`、spec §2 | 收尾 | 改 |

---

### Task 1: `createdSince` —— porcelain 里的「新建」

**Files:**
- Modify: `src/project/git-facts.ts`（`parsePorcelain` 附近，约 101 行）
- Test: `tests/project/git-facts.test.ts`

- [x] **Step 1: 写失败的测试**（追加到 `tests/project/git-facts.test.ts` 末尾；文件顶部已有 `git()`、`newRepo()`，import 行加上 `createdSince`）

```ts
import { NotAGitRepoError, changesAgainstHead, createdSince, diffSince, fileDiffAgainstHead, snapshot } from "../../src/project/git-facts.js"

describe("createdSince：基线之后新建的文件（产物条的事实来源）", () => {
  let repo: string
  beforeEach(() => {
    repo = newRepo()
  })

  it("新写的未跟踪文件算新建；改已有文件不算", async () => {
    const before = await snapshot(repo)
    writeFileSync(join(repo, "seed.txt"), "changed\n")
    writeFileSync(join(repo, "out.csv"), "a,b\n")
    expect(await createdSince(repo, before)).toEqual(["out.csv"])
  })

  it("基线时就已经在的未跟踪文件，不算这次新建", async () => {
    writeFileSync(join(repo, "old.csv"), "x\n")
    const before = await snapshot(repo)
    writeFileSync(join(repo, "old.csv"), "y\n")
    writeFileSync(join(repo, "new.csv"), "z\n")
    expect(await createdSince(repo, before)).toEqual(["new.csv"])
  })

  it("git add 过的新文件（索引里 A）也算新建", async () => {
    const before = await snapshot(repo)
    writeFileSync(join(repo, "added.txt"), "1\n")
    git(repo, "add", "added.txt")
    expect(await createdSince(repo, before)).toEqual(["added.txt"])
  })

  it("子目录里的文件带相对路径", async () => {
    const before = await snapshot(repo)
    mkdirSync(join(repo, "outputs"), { recursive: true })
    writeFileSync(join(repo, "outputs", "图.png"), "png")
    expect(await createdSince(repo, before)).toEqual(["outputs/图.png"])
  })
})
```

`mkdirSync` 要加进文件顶部的 `node:fs` import。

- [x] **Step 2: 跑，确认失败**

Run: `npx vitest run tests/project/git-facts.test.ts -t createdSince`
Expected: FAIL，`createdSince is not a function`（或 import 报错）。

- [x] **Step 3: 实现**（放在 `parsePorcelain` 之后）

```ts
/**
 * porcelain 里**此前不存在、现在有**的：未跟踪（`??`）与索引里新增（XY 里有 `A`）。
 * 重命名（`R`）不算新建——它是同一个文件换了名字。产物条只认「新生的」（spec §0）。
 */
function parsePorcelainCreated(stdout: string): string[] {
  const files = new Set<string>()
  for (const line of stdout.split("\n")) {
    if (line.length < 4) continue
    const xy = line.slice(0, 2)
    if (!(xy === "??" || xy[0] === "A" || xy[1] === "A")) continue
    files.add(unquotePath(line.slice(3)))
  }
  return [...files]
}

/**
 * 基线之后**新建**的文件（产物条的事实来源，2026-08-26）。
 *
 * 与 `diffSince` 分开：那条答「改了什么」，这条答「新生了什么」。
 * 基线时就已经脏（含未跟踪）的文件一律剔除——它在这次工具调用之前就存在。
 * **被 `.gitignore` 忽略的看不见**，与 `diffSince` 同一条已知边界，
 * 由 `provenance.ts` 用工具声明的路径兜底。
 */
export async function createdSince(workspace: string, baseline: GitBaseline): Promise<string[]> {
  const now = parsePorcelainCreated(
    await git(workspace, ["status", "--porcelain", "--untracked-files=all"]),
  )
  const before = new Set(baseline.dirtyFiles)
  return now.filter((f) => !before.has(f)).sort()
}
```

- [x] **Step 4: 跑，确认通过**

Run: `npx vitest run tests/project/git-facts.test.ts`
Expected: 全绿（原有用例也不许红）。

- [x] **Step 5: 提交**

```bash
git add src/project/git-facts.ts tests/project/git-facts.test.ts
git commit -m "feat(git-facts): createdSince——porcelain 里基线之后新建的文件"
```

---

### Task 2: 探针交出 `filesCreated`

**Files:**
- Modify: `src/runtime/provenance.ts`（`ToolFileFacts`、`观察()`）
- Test: `tests/provenance-probe.test.ts`

- [x] **Step 1: 看现有测试的搭台方式**

Run: `sed -n 1,40p tests/provenance-probe.test.ts`
记下它怎么建临时 git 仓库、怎么 `new ProvenanceProbe(dir)`。下面的用例照它的 helper 名写；若 helper 名不同，改用例不改语义。

- [x] **Step 2: 写失败的测试**（追加）

```ts
describe("filesCreated（产物条，2026-08-26）", () => {
  it("write 新建一个文件：filesCreated 只有它；改旧文件只进 filesWritten", async () => {
    const dir = newRepo()
    const probe = new ProvenanceProbe(dir)
    const h = (await probe.begin("write", { path: "outputs/a.csv" }))!
    mkdirSync(join(dir, "outputs"), { recursive: true })
    writeFileSync(join(dir, "outputs", "a.csv"), "1\n")
    writeFileSync(join(dir, "seed.txt"), "changed\n")
    const facts = (await h.finish())!
    expect(facts.filesCreated).toEqual(["outputs/a.csv"])
    expect(facts.filesWritten).toEqual(["outputs/a.csv", "seed.txt"])
  })

  it("被 .gitignore 忽略、但工具声明了要写且此前不在：仍算新建", async () => {
    const dir = newRepo()
    writeFileSync(join(dir, ".gitignore"), "figures/\n")
    const probe = new ProvenanceProbe(dir)
    const h = (await probe.begin("write", { path: "figures/f.png" }))!
    mkdirSync(join(dir, "figures"), { recursive: true })
    writeFileSync(join(dir, "figures", "f.png"), "png")
    const facts = (await h.finish())!
    expect(facts.filesCreated).toEqual(["figures/f.png"])
  })

  it("声明了却没真写出来：不算", async () => {
    const dir = newRepo()
    const probe = new ProvenanceProbe(dir)
    const h = (await probe.begin("write", { path: "never.txt" }))!
    const facts = (await h.finish())!
    expect(facts.filesCreated).toEqual([])
  })

  it("覆盖此前就存在的声明路径：不算新建", async () => {
    const dir = newRepo()
    const probe = new ProvenanceProbe(dir)
    const h = (await probe.begin("write", { path: "seed.txt" }))!
    writeFileSync(join(dir, "seed.txt"), "v2\n")
    const facts = (await h.finish())!
    expect(facts.filesCreated).toEqual([])
    expect(facts.filesWritten).toEqual(["seed.txt"])
  })
})
```

- [x] **Step 3: 跑，确认失败**

Run: `npx vitest run tests/provenance-probe.test.ts -t filesCreated`
Expected: FAIL（`filesCreated` 为 undefined）。

- [x] **Step 4: 实现**

`ToolFileFacts` 加字段：

```ts
export interface ToolFileFacts {
  filesWritten: string[]
  filesRead: string[]
  mayIncludeUserEdits: boolean
  /**
   * 这一次**新建**的文件（产物条的事实来源，2026-08-26，spec `2026-08-26-产物`）。
   * git 的 `??`/`A` ∪ 工具声明的「此前不在、现在有」。
   * 与 `filesWritten` 同一口径：整份 facts 缺席 = 不知道；空数组 = 确认没新建。
   */
  filesCreated: string[]
}
```

`观察()` 改成：拍基线时同时记下声明路径里**此前不存在**的；`finish()` 里并进去。把 import 改成 `import { createdSince, diffSince, snapshot, type GitBaseline } from "../project/git-facts.js"`（保留原有的其它 import）。

```ts
  private async 观察(声明: string[]): Promise<ProvenanceHandle | undefined> {
    let before: GitBaseline
    try {
      before = await snapshot(this.workspace)
    } catch {
      return undefined
    }
    const workspace = this.workspace
    const 在不在 = (rel: string) => {
      try {
        return statSync(resolve(workspace, rel)).isFile()
      } catch {
        return false
      }
    }
    // **执行前**记下声明路径里哪些还不存在——执行后再看就分不清「新建」与「覆盖」
    const 声明前不在 = 声明.filter((rel) => !在不在(rel))
    return {
      async finish(): Promise<ToolFileFacts | undefined> {
        try {
          const facts = await diffSince(workspace, before)
          const 真在 = 声明.filter(在不在)
          const created = await createdSince(workspace, before)
          return {
            filesWritten: [...new Set([...facts.files, ...真在])].sort(),
            filesRead: [],
            mayIncludeUserEdits: facts.mayIncludeUserEdits,
            filesCreated: [...new Set([...created, ...声明前不在.filter(在不在)])].sort(),
          }
        } catch {
          return undefined
        }
      },
    }
  }
```

（把原来 `finish` 里那两大段注释保留在对应位置，别删历史。）

- [x] **Step 5: 跑，确认通过；typecheck**

Run: `npx vitest run tests/provenance-probe.test.ts && npm run typecheck`
Expected: 全绿；typecheck 会在 `src/runtime/native.ts` / `types.ts` 报 `filesCreated` 缺字段——**那是 Task 3 的事**，先看到它、记下位置。

- [x] **Step 6: 提交**

```bash
git add src/runtime/provenance.ts tests/provenance-probe.test.ts
git commit -m "feat(provenance): 探针交出 filesCreated（git 新建 ∪ 声明路径此前不在）"
```

---

### Task 3: `tool_files` 事件带上 `filesCreated`；内置包装器并进产物登记

**Files:**
- Modify: `src/runtime/types.ts:254-261`
- Modify: `src/runtime/native.ts:628-650`（内置工具包装器）
- Test: `tests/runtime/`（新建 `tests/runtime/files-created-merge.test.ts`）

- [x] **Step 1: 事件形状**（`types.ts`）

```ts
  | {
      kind: "tool_files"
      sessionId: SessionId
      toolCallId: string
      filesWritten: string[]
      filesRead: string[]
      mayIncludeUserEdits: boolean
      /** 这一次新建的（产物条）。整条事件缺席 = 不知道；空数组 = 确认没新建 */
      filesCreated: string[]
    }
```

- [x] **Step 2: 抽一个纯函数并测它**——把「探针的 facts + 产物登记的新建」合并逻辑抽出来，放在 `src/runtime/provenance.ts` 末尾：

```ts
/**
 * 把产物登记（按 inode 身份记的「此前不存在、现在有」）并进探针的 facts（2026-08-26）。
 * 两个来源互补：git 看不见被忽略的文件，登记看不见没声明路径的（bash 里的 `cp`）。
 * `登记新建的` 是**绝对路径**，这里换算成相对工作区的。
 */
export function 并进登记新建(facts: ToolFileFacts, 登记新建的: readonly string[], cwd: string): ToolFileFacts {
  const rel = 登记新建的.map((p) => relative(cwd, p)).filter((p) => p && !p.startsWith(".."))
  return {
    ...facts,
    filesWritten: [...new Set([...facts.filesWritten, ...rel])].sort(),
    filesCreated: [...new Set([...facts.filesCreated, ...rel])].sort(),
  }
}
```

（`relative` 从 `node:path` import。）

测试 `tests/runtime/files-created-merge.test.ts`：

```ts
import { describe, expect, it } from "vitest"
import { 并进登记新建 } from "../../src/runtime/provenance.js"

const base = { filesWritten: ["a.txt"], filesRead: [], mayIncludeUserEdits: true, filesCreated: [] }

describe("并进登记新建", () => {
  it("登记到的绝对路径换成相对路径，同时进 written 与 created", () => {
    const r = 并进登记新建(base, ["/w/outputs/x.png"], "/w")
    expect(r.filesCreated).toEqual(["outputs/x.png"])
    expect(r.filesWritten).toEqual(["a.txt", "outputs/x.png"])
  })
  it("工作区外的登记不进来", () => {
    expect(并进登记新建(base, ["/elsewhere/y"], "/w").filesCreated).toEqual([])
  })
  it("去重", () => {
    const r = 并进登记新建({ ...base, filesCreated: ["outputs/x.png"] }, ["/w/outputs/x.png"], "/w")
    expect(r.filesCreated).toEqual(["outputs/x.png"])
  })
})
```

Run: `npx vitest run tests/runtime/files-created-merge.test.ts` → 先 FAIL 再实现再 PASS。

- [x] **Step 3: 接进 `native.ts` 的内置包装器**（`finally` 那段）

把

```ts
            if (handle) {
              const facts = await handle.finish()
              if (facts) emit({ kind: "tool_files", sessionId, toolCallId, ...facts })
            }
```

改成

```ts
            if (handle) {
              const facts = await handle.finish()
              if (facts) {
                // 产物登记按 inode 记下的「此前不在、现在有」并进来（spec 2026-08-26-产物 §2）
                const 登记到的 = 之前.filter(([p, 有]) => !有 && 产物登记.存在(p)).map(([p]) => p)
                emit({ kind: "tool_files", sessionId, toolCallId, ...并进登记新建(facts, 登记到的, cwd) })
              }
            }
```

`并进登记新建` 加进 `native.ts` 顶部从 `./provenance.js` 的 import。`套上溯源`（外部工具那条）不用动——它原样转发 facts，`filesCreated` 已经在里面。

- [x] **Step 4: typecheck + 相关测试**

Run: `npm run typecheck && npx vitest run tests/runtime tests/provenance-probe.test.ts tests/run-recorder.test.ts`
Expected: typecheck 干净；`run-recorder.test.ts` 里手写的 `tool_files` 事件会因缺 `filesCreated` 报类型错——给那些字面量补 `filesCreated: []`（只补类型，不改断言）。

- [x] **Step 5: 提交**

```bash
git add src/runtime/types.ts src/runtime/native.ts src/runtime/provenance.ts tests/runtime/files-created-merge.test.ts tests/run-recorder.test.ts
git commit -m "feat(runtime): tool_files 事件带 filesCreated；内置包装器并进产物登记的新建"
```

---

### Task 4: 账本 v16——`files_created` 与 `tool_call_id`；`artifactsOf` 查询

**Files:**
- Modify: `src/store/schema.ts:16`、迁移末尾（`files_written` 那段之后）
- Modify: `src/store/runs.ts`（`RunInsert`、`RunFinish`、`RunRow`、`toRun`、`parseFiles`、`insert`、`finish`、`patchFiles`）
- Modify: `src/protocol/entities.ts:98`（`RunSummary` 加两个可选字段）
- Test: `tests/store/runs.test.ts`

- [x] **Step 1: 写失败的测试**（追加）

```ts
describe("filesCreated 与 toolCallId（产物，2026-08-26）", () => {
  it("缺省=不知道，空数组=确认没新建，两者分得开", () => {
    const { runs } = seed(makeDb())
    runs.insert({ ...baseRun, runId: "r-unknown" })
    runs.insert({ ...baseRun, runId: "r-none", filesCreated: [] })
    runs.insert({ ...baseRun, runId: "r-some", filesCreated: ["outputs/a.csv"], toolCallId: "c1" })
    expect(runs.get("r-unknown")!.filesCreated).toBeUndefined()
    expect(runs.get("r-none")!.filesCreated).toEqual([])
    expect(runs.get("r-some")!.filesCreated).toEqual(["outputs/a.csv"])
    expect(runs.get("r-some")!.toolCallId).toBe("c1")
  })

  it("patchFiles 能补 filesCreated；不给就不动", () => {
    const { runs } = seed(makeDb())
    runs.insert({ ...baseRun, runId: "r1" })
    runs.patchFiles("r1", { filesWritten: ["a"], mayIncludeUserEdits: true, filesCreated: ["a"] })
    expect(runs.get("r1")!.filesCreated).toEqual(["a"])
    runs.patchFiles("r1", { filesWritten: ["a", "b"], mayIncludeUserEdits: true })
    expect(runs.get("r1")!.filesCreated).toEqual(["a"])
  })

  it("artifactsOf：按出生顺序并集、跨会话不算、失败的也算、记不知道的那几次", () => {
    const db = makeDb()
    const { runs, sessions } = seed(db)
    sessions.insert({ id: "s2", agentId: "ds-chat", workspace: "/w", sessionDir: "/w/.dawn/s2", state: "alive", createdAt: "2026-08-08T00:00:00Z", projectId: "p1" })
    const tool = (id: string, startedAt: string, extra: Partial<Parameters<typeof runs.insert>[0]>) =>
      runs.insert({ ...baseRun, runId: id, requestType: "tool_call:write", startedAt, ...extra })
    tool("t1", "2026-08-26T10:00:00Z", { toolCallId: "c1", filesCreated: ["outputs/b.png", "outputs/a.csv"] })
    tool("t2", "2026-08-26T10:01:00Z", { toolCallId: "c2" }) // 不知道
    tool("t3", "2026-08-26T10:02:00Z", { toolCallId: "c3", filesCreated: ["outputs/a.csv", "outputs/c.pdf"], hasError: true, status: "failed", finishedAt: "2026-08-26T10:02:01Z" })
    tool("t4", "2026-08-26T10:03:00Z", { sessionId: "s2", toolCallId: "c4", filesCreated: ["other.txt"] })
    runs.insert({ ...baseRun, runId: "turn", requestType: "agent_turn", startedAt: "2026-08-26T09:59:00Z", toolCallId: undefined })
    const r = runs.artifactsOf("s1")
    expect(r.artifacts.map((a) => a.path)).toEqual(["outputs/a.csv", "outputs/b.png", "outputs/c.pdf"])
    expect(r.artifacts[0]).toMatchObject({ bornRunId: "t1", bornToolCallId: "c1", bornAt: "2026-08-26T10:00:00Z" })
    expect(r.artifacts[2]).toMatchObject({ bornRunId: "t3", bornToolCallId: "c3" })
    expect(r.unknown).toEqual([{ runId: "t2", toolCallId: "c2" }])
  })
})
```

`baseRun` 若没有 `requestType`/`status`/`startedAt` 等字段，看文件里它的定义补齐（它就在文件顶部）。

- [x] **Step 2: 跑，确认失败**

Run: `npx vitest run tests/store/runs.test.ts -t 产物`
Expected: FAIL（类型/未定义方法）。

- [x] **Step 3: schema v16**

`SCHEMA_VERSION = 16`。在 `files_written` 那段迁移之后加：

```ts
  // v16（2026-08-26，产物）：这一次**新建**了哪些文件（JSON 数组，NULL = 不知道），
  // 以及这条 tool_call Run 对应 pi 的 toolCallId——界面靠它把产物挂到对话里的那一轮。
  // 老 run 两列都留 NULL：它们产生时没记这件事（不变式 5：不用今天的事实冒充当时的）。
  if (!hasColumn(db, "runs", "files_created")) {
    db.exec(`ALTER TABLE runs ADD COLUMN files_created TEXT`)
    db.exec(`ALTER TABLE runs ADD COLUMN tool_call_id TEXT`)
  }
```

同时在 v1 的 `CREATE TABLE runs` 里（`may_include_user_edits` 之后）加两列定义，让新库一次建全：

```sql
      files_created TEXT,
      tool_call_id  TEXT,
```

- [x] **Step 4: `runs.ts`**

`RunInsert` 加：
```ts
  filesCreated?: string[]
  /** pi 的 toolCallId（只有 tool_call 类 Run 有）。界面按它对到转录里的 tool item */
  toolCallId?: string
```
`RunFinish` 加 `filesCreated?: string[]`。`RunRow` 加 `files_created: string | null`、`tool_call_id: string | null`。

`insert` 的列表与 VALUES 各加 `files_created, tool_call_id` / `@filesCreated, @toolCallId`，`.run({...})` 里加：
```ts
        filesCreated: rec.filesCreated ? JSON.stringify(rec.filesCreated) : null,
        toolCallId: rec.toolCallId ?? null,
```
`finish` 的 SET 加一行 `files_created = COALESCE(@filesCreated, files_created),`，`.run` 参数加 `filesCreated: fin.filesCreated ? JSON.stringify(fin.filesCreated) : null`。

`patchFiles` 参数类型加 `filesCreated?: string[]`，SQL 加 `files_created = COALESCE(@filesCreated, files_created),`，参数 `filesCreated: files.filesCreated === undefined ? null : JSON.stringify(files.filesCreated)`。

`toRun` 在 `...parseFiles(r),` 之后加：
```ts
    ...(r.files_created === null || r.files_created === undefined ? {} : { filesCreated: JSON.parse(r.files_created) as string[] }),
    ...(r.tool_call_id ? { toolCallId: r.tool_call_id } : {}),
```

新方法（放在 `get` 之前）：

```ts
  /**
   * 本会话的产物（spec 2026-08-26-产物 §2）：所有 tool_call Run 的 `filesCreated` 并集，
   * 按**出生**（第一次出现的那条 Run 的 startedAt）排序。**不建表，一条查询。**
   * `unknown` 是 `files_created` 为 NULL 的那几次——「不知道」必须能被数出来并显示。
   */
  artifactsOf(sessionId: string): {
    artifacts: { path: string; bornRunId: string; bornToolCallId?: string; bornAt: string }[]
    unknown: { runId: string; toolCallId?: string }[]
  } {
    const rows = this.db
      .prepare(
        `SELECT id, tool_call_id, started_at, files_created FROM runs
          WHERE session_id = ? AND request_type LIKE 'tool_call%'
          ORDER BY started_at ASC, id ASC`,
      )
      .all(sessionId) as { id: string; tool_call_id: string | null; started_at: string; files_created: string | null }[]
    const seen = new Map<string, { path: string; bornRunId: string; bornToolCallId?: string; bornAt: string }>()
    const unknown: { runId: string; toolCallId?: string }[] = []
    for (const r of rows) {
      if (r.files_created === null) {
        unknown.push({ runId: r.id, ...(r.tool_call_id ? { toolCallId: r.tool_call_id } : {}) })
        continue
      }
      let created: string[]
      try {
        created = JSON.parse(r.files_created) as string[]
      } catch {
        continue
      }
      for (const path of created) {
        if (seen.has(path)) continue
        seen.set(path, { path, bornRunId: r.id, bornAt: r.started_at, ...(r.tool_call_id ? { bornToolCallId: r.tool_call_id } : {}) })
      }
    }
    return { artifacts: [...seen.values()], unknown }
  }
```

- [x] **Step 5: 协议实体**（`entities.ts` `RunSummarySchema`，`mayIncludeUserEdits` 之后）

```ts
    /**
     * 这一次**新建**了哪些文件（产物条，2026-08-26）。与 `filesWritten` 同一口径：
     * 缺省 = 不知道，空数组 = 确认没新建。
     */
    filesCreated: z.array(z.string().min(1)).optional(),
    /** pi 的 toolCallId（只有 tool_call 类 Run 有）——转录里 tool item 的 id 就是它 */
    toolCallId: z.string().min(1).optional(),
```

- [x] **Step 6: 跑，确认通过**

Run: `npx vitest run tests/store tests/protocol && npm run typecheck`
Expected: 全绿。`tests/store/no-secret-columns.test.ts` 若对列名有白名单，把两列加进去。

- [x] **Step 7: 提交**

```bash
git add src/store/schema.ts src/store/runs.ts src/protocol/entities.ts tests/store/runs.test.ts
git commit -m "feat(store): 账本 v16——runs.files_created / tool_call_id；artifactsOf 查询"
```

---

### Task 5: 记录器写入两列

**Files:**
- Modify: `src/project/run-recorder.ts:191-215`（`begin`）、`:310-327`（`tool_start`）、`:330-345`（`tool_files`）
- Test: `tests/run-recorder.test.ts`

- [x] **Step 1: 写失败的测试**（追加到「文件事实」describe 里）

```ts
  it("tool_start 记下 toolCallId；tool_files 记下 filesCreated", () => {
    rec.beginTurn(SESSION)
    rec.ingest({ kind: "tool_start", sessionId: SESSION, toolCallId: "c9", toolName: "write", input: {} })
    rec.ingest({
      kind: "tool_files", sessionId: SESSION, toolCallId: "c9",
      filesWritten: ["outputs/a.csv"], filesRead: [], mayIncludeUserEdits: true, filesCreated: ["outputs/a.csv"],
    })
    const tool = list().find((r) => r.requestType === "tool_call:write")!
    expect(tool.toolCallId).toBe("c9")
    expect(tool.filesCreated).toEqual(["outputs/a.csv"])
  })

  it("没收到 tool_files 的工具调用：filesCreated 缺省（不知道）", () => {
    rec.beginTurn(SESSION)
    rec.ingest({ kind: "tool_start", sessionId: SESSION, toolCallId: "c10", toolName: "bash", input: {} })
    rec.ingest({ kind: "tool_end", sessionId: SESSION, toolCallId: "c10", toolName: "bash", isError: false, text: "", truncated: false, bytes: 0 })
    const tool = list().find((r) => r.requestType === "tool_call:bash")!
    expect(tool.filesCreated).toBeUndefined()
    expect(tool.toolCallId).toBe("c10")
  })
```

- [x] **Step 2: 跑，确认失败**

Run: `npx vitest run tests/run-recorder.test.ts -t toolCallId`
Expected: FAIL。

- [x] **Step 3: 实现**

`begin` 加第五个参数并透传：

```ts
  private begin(
    sessionId: SessionId,
    requestType: string,
    origin: "user" | "agent" | "system",
    parentRunId?: string,
    toolCallId?: string,
  ): string | undefined {
    …
    this.runs.insert({
      …
      ...(parentRunId ? { parentRunId } : {}),
      ...(toolCallId ? { toolCallId } : {}),
      ...(env ? { environmentSnapshotId: env } : {}),
    })
```

`tool_start` 那行改成 `const runId = this.begin(event.sessionId, kind, "agent", s?.turnRunId, event.toolCallId)`。

`tool_files` 的 `patchFiles` 调用加 `filesCreated: event.filesCreated,`。

- [x] **Step 4: 跑，确认通过**

Run: `npx vitest run tests/run-recorder.test.ts && npm run typecheck`

- [x] **Step 5: 提交**

```bash
git add src/project/run-recorder.ts tests/run-recorder.test.ts
git commit -m "feat(recorder): tool_call Run 记 toolCallId 与 filesCreated"
```

---

### Task 6: 文件类判定搬到共享层

后端要按后缀给产物标 `kind`，而 `文件类按名字` 住在 `src/ui/`（后端不许 import ui）。

**Files:**
- Create: `src/files/file-kind.ts`
- Modify: `src/ui/file-kind.ts`（改成 re-export）
- Test: `tests/ui/file-kind.test.ts`（已有，跑它即可）

- [x] **Step 1: 搬**

Run: `git mv src/ui/file-kind.ts src/files/file-kind.ts`，然后新建 `src/ui/file-kind.ts`：

```ts
/** 判定住在共享层（`src/files/file-kind.ts`）——后端给产物标类型也用它。界面这里只是转口 */
export { 文件类按名字, type 文件类 } from "../files/file-kind.js"
```

若 `src/files/file-kind.ts` 里 import 了任何 `../ui/…` 或 React，把那部分留在 ui 侧（`类型图标` 已在 `files.tsx`，应该没有）。

- [x] **Step 2: 跑**

Run: `npx vitest run tests/ui/file-kind.test.ts && npm run typecheck`
Expected: 全绿。

- [x] **Step 3: 提交**

```bash
git add -A src/files/file-kind.ts src/ui/file-kind.ts
git commit -m "refactor(files): 文件类判定搬到共享层，界面转口"
```

---

### Task 7: 协议——`Artifact`、`listArtifacts`、`artifactsChanged`、7.24

**Files:**
- Modify: `src/protocol/entities.ts`（末尾加 `ArtifactSchema`）
- Modify: `src/protocol/operations.ts`（`getProvenance` 之后加 `listArtifacts`）
- Modify: `src/protocol/events.ts:471`（`dropItem` 之后加一条）
- Modify: `src/protocol/version.ts`（`"7.24"`）、`src/protocol/index.ts`（导出）
- Test: `tests/protocol/entities.test.ts`、`tests/protocol/operations.test.ts`、`tests/protocol/events.test.ts`

- [x] **Step 1: 写失败的测试**

`tests/protocol/operations.test.ts` 追加：

```ts
describe("listArtifacts（产物，7.24）", () => {
  it("请求只要 sessionId；响应带 artifacts 与 unknown", () => {
    const op = OPERATIONS.listArtifacts
    expect(op.mutating).toBe(false)
    expect(op.request.safeParse({ sessionId: "s1" }).success).toBe(true)
    expect(op.request.safeParse({}).success).toBe(false)
    const ok = op.response.safeParse({
      artifacts: [{ path: "outputs/a.csv", kind: "table", bornRunId: "r1", bornToolCallId: "c1", bornAt: "2026-08-26T10:00:00.000Z", exists: true }],
      unknown: [{ runId: "r2", toolCallId: "c2" }],
    })
    expect(ok.success).toBe(true)
    // exists 缺省 = 不知道（远端查不了），允许
    expect(op.response.safeParse({ artifacts: [{ path: "x", kind: "other", bornRunId: "r", bornAt: "2026-08-26T10:00:00.000Z" }], unknown: [] }).success).toBe(true)
  })
})
```

`tests/protocol/events.test.ts` 追加：

```ts
  it("artifactsChanged 只带信封——数据走 listArtifacts", () => {
    const u = { workbenchProtocolVersion: "7.24", sessionId: "s", revision: 3, type: "artifactsChanged" }
    expect(SessionUpdateSchema.safeParse(u).success).toBe(true)
    expect(SessionUpdateSchema.safeParse({ ...u, artifacts: [] }).success).toBe(false)
  })
```

（用该文件已有的 import 与 describe 结构。）

- [x] **Step 2: 跑，确认失败**

Run: `npx vitest run tests/protocol`

- [x] **Step 3: 实现**

`entities.ts` 末尾：

```ts
/**
 * 一个产物（spec 2026-08-26-产物 §3）：本会话某次 tool_call **新建**的文件。
 * 不是表，是从 Run 推导的视图；不存内容（S18 作者否决）。
 */
export const ArtifactSchema = z
  .object({
    /** 相对工作区 */
    path: z.string().min(1),
    /** 按后缀判（`src/files/file-kind.ts`），认不出 = other，不猜 */
    kind: z.enum(["markdown", "text", "table", "image", "code", "shell", "notebook", "archive", "pdf", "other"]),
    bornRunId: z.string().min(1),
    /** 转录里 tool item 的 id；界面靠它把产物挂到那一轮。老 run 没有 */
    bornToolCallId: z.string().min(1).optional(),
    bornAt: Iso,
    /** 现在还在不在。**缺省 = 查不了**（远端会话），不是「不在」 */
    exists: z.boolean().optional(),
  })
  .strict()
export type Artifact = z.infer<typeof ArtifactSchema>
```

`operations.ts`（`getProvenance` 之后）：

```ts
  /**
   * 本会话的产物清单（spec 2026-08-26-产物 §3）。**只读、无副作用**。
   * `unknown` 是 `filesCreated` 缺省的那几次工具调用——界面必须把它数出来说
   * 「另有 N 次运行产出未知」，不许把「不知道」画成「没有」。
   */
  listArtifacts: {
    request: z.object({ sessionId: z.string().min(1) }).strict(),
    response: z
      .object({
        artifacts: z.array(ArtifactSchema),
        unknown: z.array(z.object({ runId: z.string().min(1), toolCallId: z.string().min(1).optional() }).strict()),
      })
      .strict(),
    mutating: false,
  },
```

（`ArtifactSchema` 加进 operations.ts 顶部从 `./entities.js` 的 import。）

`events.ts`（`dropItem` 之后）：

```ts
  /**
   * 本会话的产物变了（2026-08-26）：某次工具调用新建了文件。
   * **只说「变了」，数据走 `listArtifacts`**——与 `dropItem` 同一纪律，事件里不塞清单。
   */
  z.object({ ...envelope, type: z.literal("artifactsChanged") }).strict(),
```

`version.ts`：`"7.24"`。`index.ts`：在 entities 的两个导出块里加 `ArtifactSchema` / `Artifact`。

- [x] **Step 4: 跑**

Run: `npx vitest run tests/protocol && npm run typecheck`
Expected: 全绿。若有测试断言协议版本字面值（grep `"7.23"` tests/ e2e/），一并改成 `7.24`。

- [x] **Step 5: 提交**

```bash
git add src/protocol tests/protocol
git commit -m "feat(protocol): 7.24——Artifact 实体、listArtifacts 操作、artifactsChanged 事件"
```

---

### Task 8: 后端实现 + 中枢推事件

**Files:**
- Modify: `src/workbench/backend.ts`（`getRun` 之后加 `listArtifacts`）
- Modify: `src/workbench/events.ts`（`ingest` 的 switch 里加 `tool_files` 分支）
- Test: `tests/workbench/backend.test.ts`、`tests/workbench/events.test.ts`

- [x] **Step 1: 写失败的测试**

`tests/workbench/events.test.ts` 追加（用文件里已有的 `hub()` 与 `collector()`）：

```ts
describe("产物变了（2026-08-26）", () => {
  it("tool_files 带新建 → 推一条 artifactsChanged；没新建不推", () => {
    const h = hub()
    h.track("a", "native")
    h.subscribe("a")
    const seen = collector(h)
    h.ingest("a", { kind: "tool_files", sessionId: "a", toolCallId: "c1", filesWritten: ["x"], filesRead: [], mayIncludeUserEdits: true, filesCreated: ["x"] })
    h.ingest("a", { kind: "tool_files", sessionId: "a", toolCallId: "c2", filesWritten: ["y"], filesRead: [], mayIncludeUserEdits: true, filesCreated: [] })
    expect(seen.filter((u) => u.type === "artifactsChanged")).toHaveLength(1)
  })
})
```

`tests/workbench/backend.test.ts` 追加（在 `describe("真实后端 · 经服务端端到端")` 里，用 `ctx`）：

```ts
  it("listArtifacts：从账本推导，exists 查实，unknown 数得出（2026-08-26 产物）", async () => {
    const ws = newRepo()
    const sid = await 开一段(ws)
    const pid = ctx.sessionStore.get(sid)!.projectId!
    writeFileSync(join(ws, "a.csv"), "1\n")
    ctx.runStore.insert({ runId: "t1", projectId: pid, sessionId: sid, origin: "agent", requestType: "tool_call:write", status: "completed", startedAt: "2026-08-26T10:00:00.000Z", finishedAt: "2026-08-26T10:00:01.000Z", hasError: false, toolCallId: "c1", filesCreated: ["a.csv", "gone.png"] })
    ctx.runStore.insert({ runId: "t2", projectId: pid, sessionId: sid, origin: "agent", requestType: "tool_call:bash", status: "completed", startedAt: "2026-08-26T10:01:00.000Z", finishedAt: "2026-08-26T10:01:01.000Z", hasError: false, toolCallId: "c2" })
    const r = (await ctx.backend.listArtifacts({ sessionId: sid })) as { artifacts: { path: string; kind: string; exists?: boolean }[]; unknown: { runId: string }[] }
    expect(r.artifacts).toEqual([
      expect.objectContaining({ path: "a.csv", kind: "table", exists: true }),
      expect.objectContaining({ path: "gone.png", kind: "image", exists: false }),
    ])
    expect(r.unknown).toEqual([{ runId: "t2", toolCallId: "c2" }])
  })
```

`ctx.sessionStore` 若 `make()` 没有返回，补进 `make()` 的返回对象（它在函数里叫 `sessionStore`）。

- [x] **Step 2: 跑，确认失败**

Run: `npx vitest run tests/workbench/events.test.ts tests/workbench/backend.test.ts -t 产物`

- [x] **Step 3: 中枢**（`src/workbench/events.ts` 的 `ingest` switch，`tool_end` case 之前）

```ts
      case "tool_files":
        // 有新建才推（spec 2026-08-26-产物 §3）：清单本身走 listArtifacts 查，这里只说「变了」
        if (event.filesCreated.length > 0) this.bump(sessionId, e, { type: "artifactsChanged" })
        return
```

若 switch 原本在 `default` 里对 `tool_files` 什么都不做，加这条 case 即可；若它落在别的分支里被处理过，把这一段插到那处。

- [x] **Step 4: 后端**（`getRun` 之后）

```ts
    listArtifacts: async ({ sessionId }) => {
      // `sessions` 是 SessionManager（backend.ts:232），`get()` 回 SessionRecord（含 workspace、remote?）。
      // 会话已退出它也答得出（记录还在），历史会话打开时清单照样要画
      const 会话 = sessions.get(sessionId)
      const { artifacts, unknown } = runs.artifactsOf(sessionId)
      const 远端 = Boolean(会话?.remote)
      return {
        artifacts: artifacts.map((a) => ({
          ...a,
          kind: 产物类型(a.path),
          // 远端查不了就不给 exists（缺省 = 不知道），本机 lstat 查实
          ...(远端 || !会话 ? {} : { exists: 存在(join(会话.workspace, a.path)) }),
        })),
        unknown,
      }
    },
```

`产物类型` / `存在` 两个小 helper 写在 backend.ts 顶部附近（`join` 若还没 import，从 `node:path` 加）：

```ts
import { lstatSync } from "node:fs"
import { 文件类按名字 } from "../files/file-kind.js"

const 存在 = (p: string): boolean => {
  try {
    lstatSync(p)
    return true
  } catch {
    return false
  }
}
const 产物类型 = (path: string): Exclude<ReturnType<typeof 文件类按名字>, "dir"> => {
  const k = 文件类按名字(path.split("/").pop() ?? path, "file")
  return k === "dir" ? "other" : k
}
```

测试里 `ctx.sessionStore` 若 `make()` 没返回，把 `sessionStore` 加进 `make()` 的返回对象；`rec.projectId` 用 `SessionStore.get(sid)` 的返回值取。

- [x] **Step 5: 跑**

Run: `npx vitest run tests/workbench && npm run typecheck`
Expected: 全绿。

- [x] **Step 6: 提交**

```bash
git add src/workbench/backend.ts src/workbench/events.ts tests/workbench
git commit -m "feat(workbench): listArtifacts 从账本推导并查实 exists；tool_files 有新建就推 artifactsChanged"
```

---

### Task 9: 界面状态与房客

**Files:**
- Modify: `src/ui/state/catalog.ts`（atom）、`src/ui/state/sync.ts`（loader）、`src/ui/state/right-dock.ts`（房客）
- Modify: `src/ui/views.tsx:1032`（`房客名`）、`:1038`（快捷键：`artifacts` 返回 `""`）
- Modify: `src/ui/i18n/en.ts`
- Test: `tests/ui/right-dock.test.ts`（若无则新建）

- [x] **Step 1: 状态**

`catalog.ts`：

```ts
import type { Artifact } from "../../protocol/index.js"
export interface ArtifactList {
  artifacts: readonly Artifact[]
  unknown: readonly { runId: string; toolCallId?: string }[]
}
/** 当前会话的产物（spec 2026-08-26-产物）。缺省 = 还没取到 / 没有会话 */
export const $artifacts = atom<ArtifactList | undefined>(undefined)
export const setArtifacts = (v: ArtifactList | undefined) => setValue($artifacts, v)
```

`sync.ts`（`loadRuns` 之后；`setArtifacts` 加进 import）：

```ts
export const loadArtifacts = (c: WorkbenchClient, sessionId: string | undefined): Promise<void> => {
  if (!sessionId) {
    setArtifacts(undefined)
    return Promise.resolve()
  }
  return c.get<ArtifactList>("listArtifacts", { sessionId }).then((v) => {
      // 身份守卫：回来晚了不许覆盖已切走的会话
      if (sessionId !== $activeSessionId.get()) return
      setArtifacts(v)
    })
    .catch(fail)
}
```

- [x] **Step 2: 房客**

`right-dock.ts`：

```ts
export type 坞房客 = "review" | "files" | "artifacts" | "overview" | "web" | "team"
export const 全部房客: readonly 坞房客[] = ["review", "files", "artifacts", "overview", "web", "team"]
```

`views.tsx` `房客名`：加 `who === "artifacts" ? t("产物") :`；`房客快捷键`：`if (who === "web" || who === "overview" || who === "artifacts") return ""`。

`en.ts` 加 `"产物": "Artifacts",`。

- [x] **Step 3: 跑**

Run: `npx vitest run tests/ui && npm run typecheck`
Expected: 绿。`tests/ui/design-contract.test.ts` 或 i18n 扫描若要求新 `t("…")` 都有英文，这一步就会告诉你缺哪条。

- [x] **Step 4: 提交**

```bash
git add src/ui/state src/ui/views.tsx src/ui/i18n/en.ts
git commit -m "feat(ui): 产物状态与 loader；坞加房客「产物」"
```

---

### Task 10: `本轮产物()` 纯函数 + `GeneratedStrip`

**Files:**
- Create: `src/ui/generated-strip.tsx`
- Test: `tests/ui/generated-strip.test.ts`
- Modify: `src/ui/i18n/en.ts`、`src/ui/styles.css`

- [x] **Step 1: 写失败的测试**

```ts
import { describe, expect, it } from "vitest"
import { 本轮产物 } from "../../src/ui/generated-strip.js"
import type { TranscriptItem } from "../../src/protocol/index.js"

const turn = (id: string, who: "user" | "agent"): TranscriptItem => ({ type: "turn", id, who, text: "", final: true })
const tool = (id: string): TranscriptItem => ({ type: "tool", id, name: "write", input: {}, status: "ok" })
const art = (path: string, c: string) => ({ path, kind: "table" as const, bornRunId: "r-" + c, bornToolCallId: c, bornAt: "2026-08-26T10:00:00.000Z", exists: true })

describe("本轮产物", () => {
  const items = [turn("u1", "user"), tool("c1"), tool("c2"), turn("a1", "agent"), turn("u2", "user"), tool("c3"), turn("a2", "agent")]

  it("只挑上一句用户发言之后、这一轮 agent 之前的工具调用", () => {
    const r = 本轮产物(items, "a2", { artifacts: [art("a.csv", "c1"), art("b.csv", "c3")], unknown: [] }, "native")
    expect(r).toEqual({ kind: "some", artifacts: [expect.objectContaining({ path: "b.csv" })], unknownCount: 0 })
  })

  it("整轮都不知道 → unknown；部分不知道 → some 且 unknownCount", () => {
    expect(本轮产物(items, "a1", { artifacts: [], unknown: [{ runId: "r", toolCallId: "c1" }, { runId: "r2", toolCallId: "c2" }] }, "native")).toEqual({ kind: "unknown", reason: "not_observed" })
    expect(本轮产物(items, "a1", { artifacts: [art("a.csv", "c1")], unknown: [{ runId: "r2", toolCallId: "c2" }] }, "native")).toEqual({ kind: "some", artifacts: [expect.objectContaining({ path: "a.csv" })], unknownCount: 1 })
  })

  it("确认没新建（有工具调用、都知道、都是空）→ none，不画", () => {
    expect(本轮产物(items, "a1", { artifacts: [], unknown: [] }, "native")).toEqual({ kind: "none" })
  })

  it("外部 CLI 会话：一律未知，原因 invisible_agent", () => {
    expect(本轮产物([turn("u", "user"), turn("a", "agent")], "a", { artifacts: [], unknown: [] }, "pty")).toEqual({ kind: "unknown", reason: "invisible_agent" })
  })

  it("这一轮没有任何工具调用 → none", () => {
    expect(本轮产物([turn("u", "user"), turn("a", "agent")], "a", { artifacts: [], unknown: [] }, "native")).toEqual({ kind: "none" })
  })
})
```

- [x] **Step 2: 跑，确认失败**

Run: `npx vitest run tests/ui/generated-strip.test.ts`

- [x] **Step 3: 实现** `src/ui/generated-strip.tsx`

```tsx
/**
 * 对话里的产物条 `GENERATED · N`（spec 2026-08-26-产物 §4）。
 *
 * **按轮派生，不是转录项**：这一轮 = 上一句用户发言之后、这条 agent 回复之前的所有工具调用；
 * 产物按 `bornToolCallId` 对到这些工具调用（转录里 tool item 的 id 就是 pi 的 toolCallId）。
 * 三态分得开：正常 / 未知 / 混合；确认没新建**不画**。
 */
import type { Artifact, TranscriptItem } from "../protocol/index.js"
import type { ArtifactList } from "./state/catalog.js"
import { Button } from "./primitives.js"
import { t, tf } from "./i18n/index.js"
import { 文件类按名字 } from "./file-kind.js"

export type 轮产物 =
  | { kind: "none" }
  | { kind: "unknown"; reason: "invisible_agent" | "not_observed" }
  | { kind: "some"; artifacts: Artifact[]; unknownCount: number }

export function 本轮产物(items: readonly TranscriptItem[], agentTurnId: string, list: ArtifactList, sessionKind: string): 轮产物 {
  // pty / cli 的工具不经我们的包装器，看不见；acp 借手时走我们的工具，按账本算
  if (sessionKind === "pty" || sessionKind === "cli") return { kind: "unknown", reason: "invisible_agent" }
  const end = items.findIndex((i) => i.id === agentTurnId)
  if (end < 0) return { kind: "none" }
  const 本轮工具 = new Set<string>()
  for (let i = end - 1; i >= 0; i--) {
    const it = items[i]!
    if (it.type === "turn" && it.who === "user") break
    if (it.type === "tool") 本轮工具.add(it.id)
  }
  if (本轮工具.size === 0) return { kind: "none" }
  const artifacts = list.artifacts.filter((a) => a.bornToolCallId && 本轮工具.has(a.bornToolCallId))
  const unknownCount = list.unknown.filter((u) => u.toolCallId && 本轮工具.has(u.toolCallId)).length
  if (artifacts.length === 0 && unknownCount === 0) return { kind: "none" }
  if (artifacts.length === 0 && unknownCount === 本轮工具.size) return { kind: "unknown", reason: "not_observed" }
  return { kind: "some", artifacts, unknownCount }
}

const 徽标 = (path: string) => {
  const k = 文件类按名字(path.split("/").pop() ?? path, "file")
  return k === "image" ? "IMAGE" : k === "table" ? "CSV" : k === "pdf" ? "PDF" : k === "markdown" ? "MD" : k === "code" || k === "shell" ? "CODE" : k === "notebook" ? "NB" : k.toUpperCase()
}

const 折叠阈值 = 8

export function GeneratedStrip({ 产物, onOpen }: { 产物: 轮产物; onOpen: (path: string) => void }) {
  if (产物.kind === "none") return null
  if (产物.kind === "unknown") {
    return (
      <p className="generated-strip unknown" role="note">
        <span className="label">{t("本轮产出未知")}</span>
        <span className="caveat">
          {产物.reason === "invisible_agent" ? t("外部 CLI 的文件操作不可见") : t("远端会话或非 git 工作区，探针没跑")}
        </span>
      </p>
    )
  }
  const 显示 = 产物.artifacts.slice(0, 折叠阈值)
  const 还有 = 产物.artifacts.length - 显示.length
  return (
    <div className="generated-strip" role="group" aria-label={t("本轮生成的文件")}>
      <span className="label">
        {tf("GENERATED · {0}", 产物.artifacts.length)}
        {产物.unknownCount > 0 ? <span className="caveat"> · {tf("另有 {0} 次运行产出未知", 产物.unknownCount)}</span> : null}
      </span>
      <ul className="generated-chips">
        {显示.map((a) => (
          <li key={a.path}>
            <Button variant="ghost" size="inline" className={`generated-chip${a.exists === false ? " gone" : ""}`} title={a.path} onClick={() => onOpen(a.path)}>
              <span className="kind-tag">{徽标(a.path)}</span>
              <span className="name">{a.path.split("/").pop()}</span>
              {a.exists === false ? <span className="caveat">{t("已不存在")}</span> : null}
            </Button>
          </li>
        ))}
        {还有 > 0 ? <li className="caveat">{tf("还有 {0} 个，在坞的「产物」里看全部", 还有)}</li> : null}
      </ul>
    </div>
  )
}
```

`en.ts` 加：`"本轮产出未知": "Outputs of this turn unknown"`, `"外部 CLI 的文件操作不可见": "File operations of external CLIs are invisible"`, `"远端会话或非 git 工作区，探针没跑": "Remote session or non-git workspace; the probe did not run"`, `"本轮生成的文件": "Files generated in this turn"`, `"GENERATED · {0}": "GENERATED · {0}"`, `"另有 {0} 次运行产出未知": "{0} more run(s) with unknown outputs"`, `"已不存在": "no longer exists"`, `"还有 {0} 个，在坞的「产物」里看全部": "{0} more — see all in the Artifacts pane"`。

`styles.css` 末尾：

```css
/* 产物条（对话里，spec 2026-08-26-产物 §4） */
.generated-strip { margin-top: var(--dawn-space-2); font-size: var(--dawn-fs-meta); }
.generated-strip .label { display: block; color: var(--dawn-text-3); letter-spacing: .04em; font-weight: 600; margin-bottom: var(--dawn-space-1); }
.generated-strip.unknown .caveat { margin-left: var(--dawn-space-2); }
.generated-chips { list-style: none; margin: 0; padding: 0; display: flex; flex-wrap: wrap; gap: var(--dawn-space-1); }
.generated-chip { display: inline-flex; align-items: center; gap: 6px; padding: 4px 8px; border: 1px solid var(--dawn-stroke-3); border-radius: var(--dawn-radius-2); background: var(--dawn-surface-2); }
.generated-chip.gone .name { text-decoration: line-through; opacity: .6; }
.kind-tag { font-size: 10px; font-weight: 700; padding: 1px 5px; border-radius: 4px; background: var(--dawn-ok-soft, #dff5e8); color: var(--dawn-ok, #1e7a45); }
```

（令牌名以 `src/ui/tokens.css` 里真实存在的为准；没有 `--dawn-ok-soft` 就用 `color-mix(in srgb, var(--dawn-ok) 15%, transparent)`，别硬编一个新色。）

- [x] **Step 4: 跑**

Run: `npx vitest run tests/ui && npm run typecheck`

- [x] **Step 5: 提交**

```bash
git add src/ui/generated-strip.tsx tests/ui/generated-strip.test.ts src/ui/i18n/en.ts src/ui/styles.css
git commit -m "feat(ui): 产物条——本轮产物() 三态判定 + GeneratedStrip"
```

---

### Task 11: `ArtifactsPanel`（坞「产物」格：清单 / 预览）

**Files:**
- Create: `src/ui/artifacts.tsx`
- Test: `tests/ui/artifacts-panel.test.tsx`
- Modify: `src/ui/i18n/en.ts`、`src/ui/styles.css`

- [x] **Step 1: 写失败的测试**（用 `tests/ui/environment-panel.test.tsx` 的渲染方式：`@testing-library/react` 的 `render`）

```tsx
import { describe, expect, it, vi } from "vitest"
import { render, screen, fireEvent } from "@testing-library/react"
import { ArtifactsPanel } from "../../src/ui/artifacts.js"

const art = (path: string, exists = true) => ({ path, kind: "table" as const, bornRunId: "r", bornToolCallId: "c", bornAt: "2026-08-26T10:00:00.000Z", exists })

describe("ArtifactsPanel", () => {
  it("按目录分组、数得出未知次数；已不存在的标出来", () => {
    render(<ArtifactsPanel data={{ artifacts: [art("outputs/a.csv"), art("outputs/b.csv", false), art("c.txt")], unknown: [{ runId: "r9" }] }} readFile={vi.fn()} onOpenInFiles={vi.fn()} onDownload={vi.fn()} onOpenExternally={vi.fn()} />)
    expect(screen.getByText("outputs/")).toBeTruthy()
    expect(screen.getByText(/另有 1 次运行产出未知/)).toBeTruthy()
    expect(screen.getByText("已不存在")).toBeTruthy()
  })

  it("空态与「还没取到」分得开", () => {
    const { rerender } = render(<ArtifactsPanel data={undefined} readFile={vi.fn()} onOpenInFiles={vi.fn()} onDownload={vi.fn()} onOpenExternally={vi.fn()} />)
    expect(screen.getByText(/正在取产物清单/)).toBeTruthy()
    rerender(<ArtifactsPanel data={{ artifacts: [], unknown: [] }} readFile={vi.fn()} onOpenInFiles={vi.fn()} onDownload={vi.fn()} onOpenExternally={vi.fn()} />)
    expect(screen.getByText(/还没有生成文件/)).toBeTruthy()
  })

  it("点一行进预览，「返回清单」回来", async () => {
    const readFile = vi.fn().mockResolvedValue({ kind: "text", path: "c.txt", mediaType: "text/plain", bytes: 2, text: "hi" })
    render(<ArtifactsPanel data={{ artifacts: [art("c.txt")], unknown: [] }} readFile={readFile} onOpenInFiles={vi.fn()} onDownload={vi.fn()} onOpenExternally={vi.fn()} />)
    fireEvent.click(screen.getByRole("button", { name: /c\.txt/ }))
    expect(readFile).toHaveBeenCalledWith("c.txt")
    expect(await screen.findByRole("button", { name: "返回清单" })).toBeTruthy()
  })
})
```

`readFile` 的返回形状照 `ResponseOf<"readFile">`（`src/ui/files.tsx:50`）——写测试前 `sed -n 2248,2300p src/protocol/operations.ts` 看一眼 text 那一支的字段名，改 mock 不改断言。

- [x] **Step 2: 跑，确认失败**

- [x] **Step 3: 实现** `src/ui/artifacts.tsx`

```tsx
/**
 * 坞的「产物」格（spec 2026-08-26-产物 §5）：本会话生成了什么，实时列出来，点一个能看。
 * 只有清单与预览两个视图；版本 / 来源 / 批注在下一轮。
 */
import { useEffect, useState } from "react"
import type { Artifact } from "../protocol/index.js"
import type { ArtifactList } from "./state/catalog.js"
import { Button, EmptyState, Loader } from "./primitives.js"
import { FilePreview, type FileContent } from "./files.js"
import { t, tf } from "./i18n/index.js"

export function ArtifactsPanel({
  data,
  readFile,
  onOpenInFiles,
  onDownload,
  onOpenExternally,
  onInsertReference,
}: {
  data: ArtifactList | undefined
  readFile: (path: string) => Promise<FileContent>
  onOpenInFiles: (path: string) => void
  onDownload: (path: string) => void
  onOpenExternally: (path: string) => void
  onInsertReference?: ((path: string) => void) | undefined
  /** 外面（产物条）点了某个产物：直接进它的预览 */
  focus?: string | undefined
}) {
  const [看的, 设看的] = useState<string | undefined>(undefined)
  const [内容, 设内容] = useState<FileContent | undefined>(undefined)
  const [读错, 设读错] = useState<string | undefined>(undefined)

  useEffect(() => {
    if (!看的) return
    let 活 = true
    设内容(undefined)
    设读错(undefined)
    readFile(看的).then((c) => { if (活) 设内容(c) }).catch((e: unknown) => { if (活) 设读错(e instanceof Error ? e.message : String(e)) })
    return () => { 活 = false }
  }, [看的, readFile])

  if (!data) return <Loader label={t("正在取产物清单")} />

  if (看的) {
    return (
      <div className="artifacts-pane">
        <header className="artifacts-head">
          <Button variant="ghost" size="sm" onClick={() => 设看的(undefined)}>{t("返回清单")}</Button>
          <span className="name" title={看的}>{看的}</span>
          <Button variant="ghost" size="sm" onClick={() => onOpenInFiles(看的)}>{t("在文件里定位")}</Button>
        </header>
        {读错 ? (
          <EmptyState title={t("读不到这个文件")} description={读错} action={<Button size="sm" onClick={() => 设看的((p) => p)}>{t("再试一次")}</Button>} />
        ) : (
          <FilePreview path={看的} content={内容} onOpenExternally={onOpenExternally} onDownload={onDownload} />
        )}
      </div>
    )
  }

  if (data.artifacts.length === 0 && data.unknown.length === 0) {
    return <EmptyState title={t("这段会话还没有生成文件")} description={t("agent 新建的文件会实时出现在这里。")} />
  }

  const 组 = new Map<string, Artifact[]>()
  for (const a of data.artifacts) {
    const i = a.path.lastIndexOf("/")
    const dir = i < 0 ? "" : a.path.slice(0, i + 1)
    组.set(dir, [...(组.get(dir) ?? []), a])
  }

  return (
    <div className="artifacts-pane">
      {data.unknown.length > 0 ? (
        <p className="caveat">{tf("另有 {0} 次运行产出未知", data.unknown.length)}</p>
      ) : null}
      {[...组.entries()].map(([dir, list]) => (
        <section key={dir || "."} className="artifacts-group">
          <h4 className="artifacts-group-title">
            <span>{dir || t("（工作区根）")}</span>
            <span className="count">{list.length}</span>
          </h4>
          <ul className="artifacts-list">
            {list.map((a) => (
              <li key={a.path} className={`artifact-row${a.exists === false ? " gone" : ""}`}>
                <Button variant="ghost" size="inline" className="artifact-open" title={a.path} disabled={a.exists === false} onClick={() => 设看的(a.path)}>
                  <span className="name">{a.path.slice(dir.length)}</span>
                  <span className="kind-tag">{a.kind.toUpperCase()}</span>
                  {a.exists === false ? <span className="caveat">{t("已不存在")}</span> : null}
                </Button>
                <span className="artifact-actions">
                  <Button variant="ghost" size="inline" aria-label={tf("下载 {0}", a.path)} disabled={a.exists === false} onClick={() => onDownload(a.path)}>⤓</Button>
                  <Button variant="ghost" size="inline" aria-label={tf("在文件里定位 {0}", a.path)} onClick={() => onOpenInFiles(a.path)}>⌖</Button>
                  {onInsertReference ? (
                    <Button variant="ghost" size="inline" aria-label={tf("把 {0} 插成引用", a.path)} onClick={() => onInsertReference(a.path)}>@</Button>
                  ) : null}
                </span>
              </li>
            ))}
          </ul>
        </section>
      ))}
    </div>
  )
}
```

`focus` prop 的效果：加一个 `useEffect(() => { if (focus) 设看的(focus) }, [focus])`。⋮ 菜单里「系统打开 / 复制路径」两项这一版不做成菜单——`FilePreview` 头上已有「用系统应用打开」；清单行给三颗直接按钮（⤓ / ⌖ / @），**每颗都有 aria-label**，不做悬停才出现的东西。

`en.ts` 加：`"正在取产物清单": "Loading artifacts"`, `"返回清单": "Back to list"`, `"在文件里定位": "Locate in Files"`, `"读不到这个文件": "Cannot read this file"`, `"再试一次": "Try again"`, `"这段会话还没有生成文件": "No files generated in this session yet"`, `"agent 新建的文件会实时出现在这里。": "Files the agent creates will show up here as they are made."`, `"（工作区根）": "(workspace root)"`, `"下载 {0}": "Download {0}"`, `"在文件里定位 {0}": "Locate {0} in Files"`, `"把 {0} 插成引用": "Insert {0} as a reference"`。**检查「返回清单」「再试一次」「在文件里定位」不与既有按钮文案互为子串**（`grep -n "再试" src/ui/i18n/en.ts`——若已有「重试」之类，改用既有那条）。

`styles.css`：

```css
/* 坞「产物」格（spec 2026-08-26-产物 §5） */
.artifacts-pane { display: flex; flex-direction: column; gap: var(--dawn-space-2); padding: var(--dawn-space-2); }
.artifacts-head { display: flex; align-items: center; gap: var(--dawn-space-2); }
.artifacts-head .name { flex: 1; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.artifacts-group-title { display: flex; justify-content: space-between; font-size: var(--dawn-fs-meta); color: var(--dawn-text-3); letter-spacing: .04em; margin: var(--dawn-space-1) 0; }
.artifacts-list { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 4px; }
.artifact-row { display: flex; align-items: center; justify-content: space-between; border: 1px solid var(--dawn-stroke-3); border-radius: var(--dawn-radius-2); padding: 4px 8px; }
.artifact-row.gone .name { text-decoration: line-through; opacity: .6; }
.artifact-open { display: inline-flex; align-items: center; gap: 6px; min-width: 0; }
.artifact-open .name { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.artifact-actions { display: inline-flex; gap: 2px; }
```

- [x] **Step 4: 跑**

Run: `npx vitest run tests/ui && npm run typecheck`

- [x] **Step 5: 提交**

```bash
git add src/ui/artifacts.tsx tests/ui/artifacts-panel.test.tsx src/ui/i18n/en.ts src/ui/styles.css
git commit -m "feat(ui): 坞「产物」格——按目录分组的清单 + 共用预览"
```

---

### Task 12: 装配进 `App.tsx` 与 `ConversationView`

**Files:**
- Modify: `src/ui/App.tsx`（订阅回调 ~502、会话切换、坞渲染 ~4260、传给 `ConversationView`）
- Modify: `src/ui/views.tsx`（`ConversationView` 加 `artifacts` / `onOpenArtifact` props；`TranscriptRow` 在 agent final 轮的 `.bubble` 之后渲染 `GeneratedStrip`）
- Modify: spec §2（非 git 口径）

- [x] **Step 1: 事件 → 重拉**（`App.tsx` 订阅回调里，`if (u.type === "team")` 旁边）

```ts
        // 产物变了（2026-08-26）：只说变了，清单重拉
        if (u.type === "artifactsChanged") void loadArtifacts(client, u.sessionId)
```

会话切换：找到按 `sessionId` 变化重取东西的 effect（`loadContextUsage` 那类），加 `void loadArtifacts(client, sessionId)`。`loadArtifacts` 从 `./state/sync.js` import；`$artifacts` 用 `useStore` 读成 `artifacts`。

- [x] **Step 2: 坞渲染**（`rightDockTenant === "team"` 那个分支旁）

```tsx
            ) : rightDockTenant === "artifacts" ? (
              <ArtifactsPanel
                key={sessionId}
                data={artifacts}
                focus={产物焦点}
                readFile={(path) => {
                  const 去哪读 = 文件所在 ? { connectionId: 文件所在.connectionId, path } : projectId ? { projectId, path } : undefined
                  if (!去哪读) return Promise.reject(new Error(t("还没有选中项目")))
                  return client.get<FileContent>("readFile", 去哪读)
                }}
                onOpenInFiles={openFile}
                onDownload={(path) => 下载(path)}
                onOpenExternally={(path) => void client.get("openExternally", { path }).catch(fail)}
              />
```

`下载(path)`：用文件格已有的下载入口（`造文件面板` 里传给 `FilesView` 的 `onDownload` 那个函数名，`grep -n "startDownload" src/ui/App.tsx`），直接复用同一个函数；`openExternally` 的请求形状看 `src/protocol/operations.ts:2334`，照它传参。`产物焦点` 是一个 `useState<string | undefined>`，由产物条点 chip 时设置：

```ts
  const [产物焦点, 设产物焦点] = useState<string | undefined>(undefined)
  const openArtifact = useCallback((path: string) => {
    设产物焦点(path)
    setRightDockTenant("artifacts")
    setRightDockOpen(true)
  }, [])
```

- [x] **Step 3: `ConversationView`**

props 加 `artifacts?: ArtifactList | undefined`、`onOpenArtifact?: (path: string) => void`；在 `items.map` 里给 `TranscriptRow` 多传：

```tsx
                {...(artifacts && onOpenArtifact && item.type === "turn" && item.who === "agent" && item.final
                  ? { generated: 本轮产物(items, item.id, artifacts, session.kind), onOpenArtifact }
                  : {})}
```

`TranscriptRow` props 加 `generated?: 轮产物`、`onOpenArtifact?: (path: string) => void`；在 agent 轮 `.bubble` 关闭之后（`turn-body` 里）：

```tsx
        {generated && onOpenArtifact ? <GeneratedStrip 产物={generated} onOpen={onOpenArtifact} /> : null}
```

App 里给 `ConversationView` 传 `artifacts={artifacts} onOpenArtifact={openArtifact}`。

- [x] **Step 4: spec 口径修正**（`docs/superpowers/specs/2026-08-26-产物-design.md` §2「非 git 工作区」那一条）改成：

> - 非 git 工作区：探针不起，**这次一律「不知道」**（与 `filesWritten` 既有口径一致；产物登记只在探针起来时并进去）。补它是下一轮的事（§9）。

并在 §9 加一条「非 git 工作区的 `filesCreated`（只靠产物登记）」。

- [x] **Step 5: typecheck + 全量单测**

Run: `npm run typecheck && npm test`
Expected: 全绿（2261 + 新增）。

- [x] **Step 6: 亲眼看一次**（准入规则 3）

Run: `npm run dev:mock`
在对话里说一句让假模型调 `write`（看 `scripts/mock-inference-server.mjs` 的 `toolCall` 触发规则；若只有 e2e 能注入，跳到 Task 13 的 e2e 用截图代替）。看：产物条出现 → 点 chip 坞切到「产物」并进预览 → 「返回清单」。

- [x] **Step 7: 提交**

```bash
git add src/ui/App.tsx src/ui/views.tsx docs/superpowers/specs/2026-08-26-产物-design.md
git commit -m "feat(ui): 产物条挂进对话轮次；坞「产物」格装配；artifactsChanged 触发重拉"
```

---

### Task 13: e2e（真构建产物 + 假模型）

**Files:**
- Create: `e2e/artifacts.spec.ts`

- [x] **Step 1: 写用例**

```ts
import { test, expect, 在项目里开会话, 进坞 } from "./fixtures.js"

test.describe("产物：对话里的 GENERATED 条与坞的「产物」格", () => {
  test.use({
    dawnOptions: {
      gitInit: true,
      toolCall: {
        toolName: "write",
        args: { path: "outputs/volcano_data.csv", content: "gene,log2FC,padj\nTP53,2.1,0.001\n" },
        say: "我先把数据写出来。",
      },
    },
  })

  test("write 新建一个 csv → 产物条 → 点 chip 进坞预览 → 返回清单", async ({ dawn }) => {
    const { page } = dawn
    await 在项目里开会话(page)
    const 框 = page.getByPlaceholder(/今天帮你做些什么/)
    await 框.fill("生成一份火山图数据")
    await 框.press("Enter")

    const 条 = page.getByRole("group", { name: "本轮生成的文件" })
    await expect(条).toBeVisible({ timeout: 60_000 })
    await expect(条.getByText("GENERATED · 1")).toBeVisible()
    await 条.getByRole("button", { name: /volcano_data\.csv/ }).click()

    // 坞切到「产物」，直接进了预览
    await expect(page.getByRole("tab", { name: "产物", exact: true })).toHaveAttribute("aria-selected", "true")
    await expect(page.locator(".preview")).toBeVisible()
    await page.getByRole("button", { name: "返回清单" }).click()
    await expect(page.getByText("outputs/")).toBeVisible()
    await expect(page.getByRole("button", { name: /^volcano_data\.csv/ })).toBeVisible()
  })
})

/** 与 e2e/cli-agent.spec.ts 同一份假 CLI（e2e/fixtures/claude） */
const FAKE = resolve(import.meta.dirname, "fixtures/claude")
const PROVIDERS = `agents:
  ds-chat:
    kind: native
    provider: deepseek
    model: deepseek-v4-flash
    capabilities: [chat, exec]
  claude:
    kind: cli
    command: "${FAKE}"
    args: []
    models: [opus, sonnet, haiku]
    capabilities: [chat, exec]
`

test.describe("外部 CLI 会话：本轮产出未知，不列 agent 声称的文件", () => {
  test.use({ dawnOptions: { providersYaml: PROVIDERS, gitInit: true } })

  test("cli 会话的 agent 轮画「本轮产出未知」", async ({ dawn }) => {
    const { page } = dawn
    await 用某个agent开一段(page, /claude/)
    await 等进了对话(page)
    await page.getByPlaceholder(/今天帮你做些什么/).fill("把结果写到 outputs/fake.csv")
    await page.getByRole("button", { name: "发送", exact: true }).click()
    await expect(page.getByText(/假 CLI 已应答/)).toBeVisible({ timeout: 30_000 })
    await expect(page.getByText("本轮产出未知")).toBeVisible()
    await expect(page.getByText("外部 CLI 的文件操作不可见")).toBeVisible()
    // agent 嘴里说的文件一个不列
    await expect(page.getByText("fake.csv")).toHaveCount(0)
  })
})
```

文件顶部 import 改成：`import { resolve } from "node:path"` 与 `import { test, expect, 在项目里开会话, 用某个agent开一段, 等进了对话 } from "./fixtures.js"`。

- [x] **Step 2: tab 的可访问名要稳定**

`RightDock` 的标签按钮加 `aria-label={房客名(who)}`，数字角标放在按钮里一个 `<span className="dock-tab-badge">` 中（产物格给 `artifacts?.artifacts.length`）。`aria-selected` 若原来没设，按 `tenant === who` 设上。

- [x] **Step 3: 跑**

Run: `npm run build && npm run test:e2e:only -- e2e/artifacts.spec.ts`
Expected: 2 passed。红了先看 `test-results/` 里的截图，再改。

- [x] **Step 4: 视觉基线**

Run: `npm run test:e2e:visual`
坞多一个标签会让开着坞的基线红——**先看每张 `-diff.png`**，确认差异只是多了一个「产物」标签，再 `npm run test:e2e:visual:update`，然后再跑两遍 `npm run test:e2e:visual` 确认稳定。

- [x] **Step 5: 设计契约与 i18n 扫描**

Run: `npx vitest run tests/ui/design-contract.test.ts tests/ui/i18n.test.ts`
Expected: 绿。红了按它报的那条改文案，不改测试。

- [x] **Step 6: 提交**

```bash
git add e2e/artifacts.spec.ts e2e/__screenshots__ src/ui/views.tsx
git commit -m "test(e2e): 产物条与产物格真链路；坞标签 aria 稳定；视觉基线重存（看过 diff）"
```

---

### Task 14: 收尾——开发历史、全量验证

**Files:**
- Modify: `docs/DEVELOPMENT_HISTORY.md`（顶部追加）

- [x] **Step 1: 全量**

Run: `npm run typecheck && npm test && npm run test:e2e`
Expected: 全绿。把三个数字（单测数、e2e 数、新增用例数）记下来写进历史。

- [x] **Step 2: 历史条目**

```markdown
### 2026-08-26 — 产物：对话里 `GENERATED · N` 产物条 + 坞「产物」格（data-platform 分支）

- **Type**: feat
- **Motivation**: 回归主线第一步。「这一轮生成了什么」此前只有 agent 嘴上说；账本记了改动却不分新建，界面上没有任何一处能实时看到产出。
- **What**: 探针加 `filesCreated`（git `??`/`A` ∪ 声明路径此前不在，再并进产物登记）；账本 v16 加 `runs.files_created` / `tool_call_id`；`RunStore.artifactsOf` 一条查询推导产物（不建表、不存内容）；协议 7.24 加 `Artifact`、`listArtifacts`、`artifactsChanged`；对话里按轮派生产物条三态（正常 / 未知 / 混合，确认没新建不画；外部 CLI 一律未知）；坞新房客「产物」= 按目录分组的实时清单 + 共用 `FilePreview`。`文件类按名字` 搬到 `src/files/`。
- **Impact**: 协议 minor 升级；schema 15 → 16（老 run 两列 NULL = 不知道）；坞多一个房客，视觉基线重存（diff 只是多一个标签）。非 git 工作区仍是「不知道」（spec §2 已改口径）。
- **Verification**: 单测 N 全绿（新增：createdSince 4、探针 4、合并 3、store 3、recorder 2、协议 2、中枢 1、后端 1、本轮产物 5、面板 3）；e2e 2 条真链路（write 新建 → 产物条 → 坞预览 → 返回；pty 会话画未知）；视觉基线 10/10 连验两轮；`dev:mock` 人肉看过。
```

- [x] **Step 3: 提交**

```bash
git add docs/DEVELOPMENT_HISTORY.md
git commit -m "docs(history): 产物——产物条与产物格落地"
```

---

## 自查（写完计划后对着 spec 过的）

- spec §2 数据层 → Task 1–5（非 git 口径在 Task 12 Step 4 改 spec 对齐实现）
- spec §3 协议 → Task 7、8；`unknownRuns: number` 在实现里变成 `unknown: {runId, toolCallId?}[]`——**因为产物条要按轮数未知，光有总数不够**；spec §3 的响应形状在 Task 12 Step 4 一并改成这个
- spec §4 产物条三态 → Task 10、12
- spec §5 清单 / 预览 / 实时 → Task 11、12、9
- spec §6 出声 → 未知（Task 10/11）、已不存在（Task 11）、取失败（`loadArtifacts` 走 `fail`，面板在 `data === undefined` 时显示「正在取」——**取失败后它会一直转**；在 Task 12 Step 1 里给 `loadArtifacts` 的 catch 加 `setArtifacts({ artifacts: [], unknown: [] })` 之外还要出声，用 `fail`，已在 sync 里）
- spec §8 验证 → Task 13、14
- 类型名一致性：`ArtifactList`（catalog）、`轮产物`（generated-strip）、`Artifact`（protocol）、`FileContent`（files.tsx）——全计划同名
