# 回到底部浮标 · 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 贴底跟随一旦撒手，转录区底部居中浮出一颗带字的药丸；点它回到底部并恢复跟随。

> **2026-09-09 形状改过一次**：第一版做的是「右下角一颗圆的 ↓」，作者看到实物后改成
> 设计里的方案一（底部居中、两个状态都带字）。下面各步的代码是第一版的原样留档，
> 最终形态以 `src/ui/back-to-bottom.tsx` 与规格文档为准。

**Architecture:** 一个自足的组件 `回到底部`，放在 `<StickToBottom>` 里、`<StickToBottom.Content>` 外——那一层不滚，所以浮标不会跟着内容滚走。三个状态（不画 / 圆的 / 带字）由库自己的 `isAtBottom` 加一个「内容长高过」的标志决定；后者由组件自己的 `ResizeObserver` 盯 `contentRef` 得出，不靠调用方传参。对话区与笔记本各放一颗，同一份代码。

**Tech Stack:** React 19 · `use-stick-to-bottom@1.1.6` · vitest + @testing-library/react · Playwright（真实 Electron 产物）

**规格：** `docs/superpowers/specs/2026-09-08-回到底部浮标-design.md`

---

## 文件清单

| 文件 | 干什么 |
|---|---|
| `src/ui/back-to-bottom.tsx`（新建） | 判据（纯函数）+ 组件。**这一条功能的全部逻辑只住在这里** |
| `tests/ui/back-to-bottom.test.ts`（新建） | 盯那条纯判据 |
| `src/ui/icons.tsx`（改） | 加 `下箭头图标` |
| `src/ui/styles.css`（改） | `.turns` / `.nb-cells` 加 `position: relative`；`.stick-pill` 的样子 |
| `src/ui/views.tsx`（改，约 4326–4388） | 对话区那颗 |
| `src/ui/notebook.tsx`（改，约 332–361） | 笔记本那颗 |
| `src/ui/i18n/en.ts`（改） | 两条英文词条 |
| `e2e/stick-to-bottom.spec.ts`（改） | 三条 e2e |
| `docs/DEVELOPMENT_HISTORY.md`（改） | 一条记录 |

**不新建 CSS 文件**：本项目样式集中在 `styles.css` 一份，跟着现有形态走。

---

### Task 1: 一支朝下的箭头

**Files:**
- Modify: `src/ui/icons.tsx`（紧跟在 `上箭头图标` 后面）

- [ ] **Step 1: 加图标**

`上箭头图标` 的路径原样上下翻转（`viewBox` 是 16×16，所以 `y → 16 - y`）：

```tsx
/** 回到底部。一支朝下的箭头 */
export function 下箭头图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M8.6 13.4a.9.9 0 0 1-1.2 0l-4.5-4.4a.9.9 0 0 1 1.2-1.3l2.8 2.7V2.9a.9.9 0 0 1 1.8 0v7.5l2.8-2.7a.9.9 0 1 1 1.2 1.3l-4.5 4.4Z" />
    </壳>
  )
}
```

- [ ] **Step 2: 确认能编译**

Run: `npm run typecheck`
Expected: 没有输出（tsc 静默通过）

---

### Task 2: 判据 —— 一个纯函数，先写测试

判据本身是确定的，但它发生在一场真滚动里。**抽成纯函数**的理由与 `state/view.ts`
里的 `carryDraft` 一模一样：*「这是一个竞态，用 e2e 去撞那个窗口只会得到一条不稳的用例，
而规则本身是确定的，可以直接验。」*

另外 jsdom **没有真的 `ResizeObserver`**（`tests/ui/setup.ts` 里塞的是一个空壳），
所以「内容长高了」这一步在单元测试里根本发生不了——它归 e2e。

**Files:**
- Create: `tests/ui/back-to-bottom.test.ts`
- Create: `src/ui/back-to-bottom.tsx`

- [ ] **Step 1: 写会失败的测试**

`tests/ui/back-to-bottom.test.ts`：

```ts
/**
 * 「回到底部」浮标的判据（2026-09-08，规格 `2026-09-08-回到底部浮标-design.md`）。
 *
 * 这里只验**该不该画、画成哪一种**。真的滚动、真的 `ResizeObserver`
 * 归 `e2e/stick-to-bottom.spec.ts`——jsdom 里那个 `ResizeObserver` 是个空壳
 * （`tests/ui/setup.ts` 写着理由），拿它验「内容长高了」等于什么都没验。
 */
import { describe, expect, it } from "vitest"
import { 该画哪一种, 下一个长过 } from "../../src/ui/back-to-bottom.js"

describe("该画哪一种", () => {
  it("贴着底就什么都不画——跟随好好的，不该有东西挡着", () => {
    expect(该画哪一种(true, false)).toBe("不画")
  })

  it("**贴着底时哪怕标志还留着，也不画**", () => {
    expect(该画哪一种(true, true)).toBe("不画")
  })

  it("没贴底、底下没长东西 → 圆的", () => {
    expect(该画哪一种(false, false)).toBe("圆的")
  })

  it("没贴底、底下长过 → 带字", () => {
    expect(该画哪一种(false, true)).toBe("带字")
  })
})

describe("下一个长过", () => {
  it("没贴底的时候内容长高了 → 立起来", () => {
    expect(下一个长过(false, { 贴底: false, 长了: true })).toBe(true)
  })

  it("**贴着底时长高不算**——你本来就在看着它长", () => {
    expect(下一个长过(false, { 贴底: true, 长了: true })).toBe(false)
  })

  it("回到底部就清零", () => {
    expect(下一个长过(true, { 贴底: true, 长了: false })).toBe(false)
  })

  it("没贴底、也没长 → 保持原样（不许自己熄灭）", () => {
    expect(下一个长过(true, { 贴底: false, 长了: false })).toBe(true)
  })
})
```

- [ ] **Step 2: 跑一遍，确认它红**

Run: `npx vitest run tests/ui/back-to-bottom.test.ts`
Expected: FAIL —— `Failed to resolve import "../../src/ui/back-to-bottom.js"`

- [ ] **Step 3: 只写这两个纯函数（组件下一个任务再加）**

`src/ui/back-to-bottom.tsx`：

```tsx
/**
 * 「回到底部」浮标（2026-09-08，规格 `2026-09-08-回到底部浮标-design.md`）。
 *
 * 作者报的是：*「分析的内容不更新，需要我手动往下挪动。」*
 * 同一天已经修掉一条真根因（丢掉的 `mouseup`，见 `mouse-stuck.ts`），
 * 但那句话里还剩一半没被回答——**跟随撒手之后没有出路**。
 *
 * 这个库撒手本身是对的：你主动上滚了，就不该被下一个 token 弹回底部。
 * 缺的是「撒手了」这件事**看得见**、并且**一键回得去**。
 * 「看不见的能力等于不存在」——2026-08-10 一天之内为这条栽过两次。
 */

/** 三个状态。**贴着底就什么都不画**——跟随好好的时候不该有东西挡着 */
export function 该画哪一种(贴底: boolean, 长过: boolean): "不画" | "圆的" | "带字" {
  if (贴底) return "不画"
  return 长过 ? "带字" : "圆的"
}

/**
 * 「底下长出新东西了」这个标志怎么变。
 *
 * **抽成纯函数**的理由与 `carryDraft` 同一条：它发生在一场竞态里，
 * 而规则本身是确定的，可以直接验。
 *
 * 三条：贴回底就清零；没贴底时内容长高就立起来；其余保持原样
 * （**不许自己熄灭**——熄灭了人就永远看不到「底下有东西」这件事）。
 */
export function 下一个长过(前: boolean, { 贴底, 长了 }: { 贴底: boolean; 长了: boolean }): boolean {
  if (贴底) return false
  return 前 || 长了
}
```

- [ ] **Step 4: 跑一遍，确认它绿**

Run: `npx vitest run tests/ui/back-to-bottom.test.ts`
Expected: `Test Files 1 passed` · `Tests 8 passed`

- [ ] **Step 5: 提交（只在作者说要提交时执行——本仓库的规矩是「问了才提交，永不 push」）**

```bash
git add tests/ui/back-to-bottom.test.ts src/ui/back-to-bottom.tsx src/ui/icons.tsx
git commit -m "feat(ui): the rule behind the jump-to-latest pill"
```

---

### Task 3: 组件本身

**Files:**
- Modify: `src/ui/back-to-bottom.tsx`（在两个纯函数下面追加）

- [ ] **Step 1: 写组件**

追加到 `src/ui/back-to-bottom.tsx`，并在文件顶部补上 import：

```tsx
import { useEffect, useRef, useState } from "react"
import { useStickToBottomContext } from "use-stick-to-bottom"
import { 下箭头图标 } from "./icons.js"
import { t } from "./i18n/index.js"
```

```tsx
/**
 * 放在 `<StickToBottom>` 里、`<StickToBottom.Content>` **外面**。
 *
 * 那一层不滚（真正在滚的是 `.Content` 自己造的 `height:100%` 那个 div，
 * 2026-09-08 在真产物上量过），所以绝对定位的浮标不会跟着内容滚走。
 * 外层需要 `position: relative`——`.turns` 与 `.nb-cells` 各加了一句。
 */
export function 回到底部() {
  const { isAtBottom, scrollToBottom, contentRef } = useStickToBottomContext()
  const [长过, 设长过] = useState(false)

  /**
   * 「底下长出新东西了」**组件自己看得出来**，不用调用方传记号进来。
   *
   * 两条理由：① 流式回复时条数不变、内容却在长——传条数会漏掉最常见的那一种，
   * 而那正是作者报的场景；② 对话区与笔记本因此是同一份代码、零配置，
   * 不会各自算一遍「什么算新内容」然后算得不一样。
   *
   * `贴底ref` 而不是依赖数组：观察器只装一次，回调里要读的是**此刻**贴没贴底。
   */
  const 贴底ref = useRef(isAtBottom)
  贴底ref.current = isAtBottom
  useEffect(() => {
    const el = contentRef.current
    if (!el) return
    let 上次高 = el.getBoundingClientRect().height
    const ro = new ResizeObserver((条目们) => {
      const 高 = 条目们[0]?.contentRect.height ?? 上次高
      const 长了 = 高 > 上次高 + 0.5 // 半个像素的抖动不算「长出东西」
      上次高 = 高
      设长过((前) => 下一个长过(前, { 贴底: 贴底ref.current, 长了 }))
    })
    ro.observe(el)
    return () => ro.disconnect()
  }, [contentRef])

  // 贴回底部就清零（滚回去、或者跟随自己追上了都算）
  useEffect(() => {
    if (isAtBottom) 设长过(false)
  }, [isAtBottom])

  const 哪一种 = 该画哪一种(isAtBottom, 长过)
  if (哪一种 === "不画") return null

  /**
   * **带字那一种不写 `aria-label`**：可见文字就是它的可访问名。
   * 两个都写会让「`aria-label` 与按钮文案之间没有子串关系」那条扫描当场撞车
   * （两者一模一样，而一模一样是子串关系里最强的那一种）。
   */
  return (
    <button
      type="button"
      className={`stick-pill${哪一种 === "带字" ? " has-new" : ""}`}
      title={哪一种 === "带字" ? t("有新内容") : t("回到底部")}
      {...(哪一种 === "圆的" ? { "aria-label": t("回到底部") } : {})}
      onClick={() => {
        设长过(false)
        void scrollToBottom()
      }}
    >
      <下箭头图标 className="row-icon" />
      {哪一种 === "带字" ? <span className="stick-pill-say">{t("有新内容")}</span> : null}
    </button>
  )
}
```

- [ ] **Step 2: 确认能编译**

Run: `npm run typecheck`
Expected: 没有输出

- [ ] **Step 3: 确认判据那几条还是绿的**

Run: `npx vitest run tests/ui/back-to-bottom.test.ts`
Expected: `Tests 8 passed`

---

### Task 4: 样子

**Files:**
- Modify: `src/ui/styles.css`

- [ ] **Step 1: 让两个外层成为定位参照**

把 `.turns` 那条规则（约 781 行）加一句 `position: relative;`：

```css
.turns {
  position: relative; /* 「回到底部」浮标的定位参照（2026-09-08）。这一层不滚 */
  flex: 1;
  overflow: auto;
  padding: var(--dawn-space-3) var(--dawn-page-inset-x);
  font-size: var(--dawn-chat-size);
  line-height: var(--dawn-chat-leading);
}
```

`.nb-cells`（约 6508 行）同理，整行替换成：

```css
/* position: relative 是「回到底部」浮标的定位参照（2026-09-08）。这一层不滚 */
.nb-cells { position: relative; flex: 1; min-height: 0; overflow: auto; }
```

- [ ] **Step 2: 浮标的样子**

追加到 `styles.css` 末尾：

```css
/**
 * 「回到底部」浮标（2026-09-08，规格 `2026-09-08-回到底部浮标-design.md`）。
 *
 * 平时是一颗 32px 的圆点，只有一支箭头；底下长出新内容时**撑开成胶囊、带字**——
 * 而那一刻正是它最该自我说明的一刻（作者报的就是这个场景）。
 * 圆的那一种靠 `aria-label` / `title` 兜底。
 *
 * 浮起来的面照 `.agent-menu` 那一套：阴影 + 发丝线，不是粗边框。
 */
.stick-pill {
  position: absolute;
  right: var(--dawn-space-4);
  bottom: var(--dawn-space-4);
  z-index: var(--z-modal-popover);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  height: 32px;
  min-width: 32px;
  padding: 0;
  border: var(--dawn-hairline) solid var(--dawn-stroke-float);
  border-radius: var(--dawn-radius-full);
  background: var(--dawn-surface-elevated);
  box-shadow: var(--dawn-shadow-float);
  color: var(--dawn-text-1);
  /* 字号走令牌，不写裸数字——`design-contract` 那条扫描 2026-09-08 当场抓过一次 */
  font-size: var(--dawn-fs-meta);
  cursor: pointer;
}
.stick-pill:hover { background: var(--dawn-surface-panel); }
/* 带字那一种：撑开，左边留给箭头、右边留给字 */
.stick-pill.has-new { padding: 0 12px 0 10px; }
.stick-pill-say { white-space: nowrap; }
```

- [ ] **Step 3: 确认样式表还能被构建**

Run: `npm run build`
Expected: `✓ built in …`，无报错

---

### Task 5: 接进对话区

**Files:**
- Modify: `src/ui/views.tsx`（`<StickToBottom className="turns">` 那一段，约 4326–4388）

- [ ] **Step 1: import**

在 views.tsx 顶部的 import 区（`import { StickToBottom } from "use-stick-to-bottom"` 那一行附近）加：

```tsx
import { 回到底部 } from "./back-to-bottom.js"
```

- [ ] **Step 2: 放一颗**

把 `</StickToBottom.Content>` 与 `</StickToBottom>` 之间加上浮标——
**在 `.Content` 外面**，那一层不滚：

```tsx
        </StickToBottom.Content>
        {/**
          * **跟随撒手之后的出路**（2026-09-08，作者：*「分析的内容不更新，
          * 需要我手动往下挪动。」*）。放在 `.Content` 外面：那一层不滚，
          * 浮标因此不会跟着内容滚走。见 `back-to-bottom.tsx` 的文件头。
          */}
        <回到底部 />
      </StickToBottom>
```

- [ ] **Step 3: 编译 + 构建**

Run: `npm run typecheck && npm run build`
Expected: typecheck 静默；build 打印 `✓ built in …`

---

### Task 6: 接进笔记本

**Files:**
- Modify: `src/ui/notebook.tsx`（约 332–361）

- [ ] **Step 1: import**

```tsx
import { 回到底部 } from "./back-to-bottom.js"
```

- [ ] **Step 2: 放一颗**

```tsx
        </StickToBottom.Content>
        {/* 与对话区同一颗、同一套文案（2026-09-08）：同一条规则不许只套一半 */}
        <回到底部 />
      </StickToBottom>
```

- [ ] **Step 3: 编译 + 构建**

Run: `npm run typecheck && npm run build`
Expected: typecheck 静默；build 成功

---

### Task 7: 英文词条

**Files:**
- Modify: `src/ui/i18n/en.ts`

- [ ] **Step 1: 加两条**

按字典里现有的排布加进去（键是中文原文）：

```ts
  "回到底部": "Jump to latest",
  "有新内容": "New messages",
```

- [ ] **Step 2: 确认没有词条缺失的吼声**

Run: `npx vitest run tests/ui/i18n.test.ts`
Expected: PASS（这条用例盯的是「界面里出现的每一句都在字典里」）

---

### Task 8: e2e —— 三条，先红后绿

**Files:**
- Modify: `e2e/stick-to-bottom.spec.ts`

- [ ] **Step 1: 写三条会失败的用例**

追加到 `e2e/stick-to-bottom.spec.ts` 末尾（`灌几轮` / `离底` / `滚的是` 已在文件顶部定义，直接用）：

```ts
/**
 * 跟随撒手之后的出路（2026-09-08，规格 `2026-09-08-回到底部浮标-design.md`）。
 *
 * 作者：*「分析的内容不更新，需要我手动往下挪动。」* 丢掉的 `mouseup` 那条已经修了，
 * 这三条盯的是剩下那一半：**撒手之后有没有一个看得见的出路**。
 */
test.describe("回到底部浮标", () => {
  test.use({ dawnOptions: { firstChunkDelayMs: 2_500 } })

  test("**贴着底时一个像素都不画**", async ({ dawn }) => {
    const { app, page } = dawn
    await 开一段临时会话(page, "开始")
    await app.evaluate(({ BrowserWindow }) => BrowserWindow.getAllWindows()[0]?.setSize(900, 560))
    await 灌几轮(page, ["第一句话", "第二句话", "第三句话"])
    expect(await 离底(page)).toBeLessThan(20)
    // 跟随好好的时候不该有任何东西挡着——这条是防它变成常驻遮挡物的闸
    await expect(page.locator(".stick-pill")).toHaveCount(0)
  })

  test("**往上翻 → 浮标出现 → 点它 → 回到底且跟随恢复**", async ({ dawn }) => {
    const { app, page } = dawn
    await 开一段临时会话(page, "开始")
    await app.evaluate(({ BrowserWindow }) => BrowserWindow.getAllWindows()[0]?.setSize(900, 560))
    await 灌几轮(page, ["第一句话", "第二句话", "第三句话"])

    // 自己往上翻——库的行为是「撒手」，这是对的；要验的是撒手之后有没有出路
    await page.evaluate((sel) => {
      ;(document.querySelector(sel) as HTMLElement).scrollTop = 0
    }, 滚的是)
    await expect(page.locator(".stick-pill")).toHaveCount(1)

    await page.locator(".stick-pill").click()
    await expect.poll(async () => 离底(page), { timeout: 10_000 }).toBeLessThan(20)
    // 回到底了，浮标就该消失
    await expect(page.locator(".stick-pill")).toHaveCount(0)

    // **跟随真的恢复了**：再灌一轮，不用手动碰它
    await 灌几轮(page, ["恢复之后这一句"])
    expect(await 离底(page)).toBeLessThan(20)
  })

  test("**翻上去之后新内容到达 → 浮标带上字**", async ({ dawn }) => {
    const { app, page } = dawn
    await 开一段临时会话(page, "开始")
    await app.evaluate(({ BrowserWindow }) => BrowserWindow.getAllWindows()[0]?.setSize(900, 560))
    await 灌几轮(page, ["第一句话", "第二句话", "第三句话"])

    await page.evaluate((sel) => {
      ;(document.querySelector(sel) as HTMLElement).scrollTop = 0
    }, 滚的是)
    // 刚翻上去：圆的，没有字
    await expect(page.locator(".stick-pill")).toHaveCount(1)
    await expect(page.locator(".stick-pill.has-new")).toHaveCount(0)

    // 底下真的长出东西
    await 灌几轮(page, ["翻上去之后来的这一句"])
    await expect(page.locator(".stick-pill.has-new")).toHaveCount(1)
    await expect(page.getByRole("button", { name: "有新内容", exact: true })).toBeVisible()
  })
})
```

- [ ] **Step 2: 跑一遍，确认前两条红**

Run: `npx playwright test e2e/stick-to-bottom.spec.ts -g "回到底部浮标" --reporter=line`
Expected: 「贴着底时一个像素都不画」绿（现在本来就没有这个元素），另外两条**红**
（`.stick-pill` 找不到）——**在把 Task 5/6 build 进产物之前跑这一步**，
它证明的是「这三条真的在测新东西」。

> 如果你已经先 build 了 Task 5/6，把这一步改成：`git stash` 掉 views/notebook 那两处接线，
> build，跑，确认红，再 `git stash pop`、重新 build。**红那一轮必须真的看见**——
> 本项目的规矩是「先量后改」，一条从没红过的用例证明不了任何事。

- [ ] **Step 3: 构建之后再跑，确认三条全绿**

Run: `npm run build && npx playwright test e2e/stick-to-bottom.spec.ts --reporter=line`
Expected: `5 passed`（原有 2 条 + 新增 3 条）

---

### Task 9: 两道扫描 + 十张基线

**Files:**
- 只跑，不改（除非扫描抓到东西）

- [ ] **Step 1: 设计契约扫描**

Run: `npx vitest run tests/ui/design-contract.test.ts`
Expected: PASS

盯的是这两条：**没有一个按钮文案是另一个的子串**、**`aria-label` 与按钮文案之间
也没有子串关系**。「回到底部」「有新内容」如果撞上了，**换一个说法**，
不要为了躲子串把文字去掉——「看不见的能力等于不存在」那条更硬。

- [ ] **Step 2: 视觉基线**

Run: `npm run test:e2e:visual:only`
Expected: `10 passed` —— 浮标贴底时不画，理论上一张都不该动。

**红了先看 diff 图**：`test-results/<用例>/<名字>-diff.png`。
真是浮标漏进去了（说明「贴底不画」那条没兜住），**回去改代码，不是重存基线**。

- [ ] **Step 3: 全量**

Run: `npm test && npm run typecheck && npm run test:e2e:only`
Expected: 单元全绿（`tests/kernel/outputs.integration.test.ts` 在满负载下偶发超时，
单跑即过，与本条无关）；typecheck 静默；e2e `483 passed`

---

### Task 10: 记一笔

**Files:**
- Modify: `docs/DEVELOPMENT_HISTORY.md`

- [ ] **Step 1: 在最顶上追加一条**

```markdown
### 2026-09-08 — 跟随撒手之后，出路要看得见：回到底部浮标

- **Type**: feat
- **Motivation**: 作者：*「分析的内容不更新，需要我手动往下挪动。」*
  同一天已修掉一条真根因（丢掉的 `mouseup`），但那句话里还剩一半没被回答——
  **整个界面里没有任何一个「回到底部」的入口**（`scrollToBottom` 一处都没用过）。
  库撒手本身是对的；缺的是「撒手了」看得见、且一键回得去。
  「看不见的能力等于不存在」——2026-08-10 一天之内为这条栽过两次。
- **What**: 新增 `src/ui/back-to-bottom.tsx`。三个状态（不画 / 圆的 / 带字），
  判据抽成纯函数（同 `carryDraft` 那条理由：竞态里发生、规则本身确定）。
  「底下长出新东西」由组件自己的 `ResizeObserver` 盯 `contentRef` 得出，**不让调用方传记号**——
  流式回复时条数不变、内容却在长，传条数会漏掉最常见的那一种。
  放在 `<StickToBottom>` 里、`.Content` 外（那一层不滚，2026-09-08 量过）。
  对话区与笔记本各一颗，同一份代码、同一套文案。
- **Impact**: 翻上去看历史之后不再只能手动拖回底部；正在跑的时候翻上去，
  浮标会带上「有新内容」。贴着底时**一个像素都不画**。
- **Verification**: `tests/ui/back-to-bottom.test.ts` 八条盯判据；
  `e2e/stick-to-bottom.spec.ts` 新增三条（贴底不画 / 点它回底且跟随恢复 /
  新内容到达时带字），后两条先红后绿；设计契约两条扫描通过；
  十张视觉基线未动（跑过确认，没有重存）；单元 + typecheck + 全量 e2e。
```

- [ ] **Step 2: 提交（只在作者说要提交时执行）**

```bash
git add src/ui/back-to-bottom.tsx src/ui/icons.tsx src/ui/styles.css src/ui/views.tsx \
        src/ui/notebook.tsx src/ui/i18n/en.ts tests/ui/back-to-bottom.test.ts \
        e2e/stick-to-bottom.spec.ts docs/DEVELOPMENT_HISTORY.md \
        docs/superpowers/specs/2026-09-08-回到底部浮标-design.md \
        docs/superpowers/plans/2026-09-08-回到底部浮标.md
git commit -m "feat(ui): a visible way back to the bottom when following lets go"
```

**不 push。** 本仓库的规矩：作者自己推。

---

### Task 11: 自己打开看一眼

单元和 e2e 都绿不等于「真的能用」——本项目为这条栽过（419 个测试全绿的那一版，
打开之后点什么都没反应）。

- [ ] **Step 1: 起来看**

Run: `npm run app`

- [ ] **Step 2: 手动走一遍**

1. 开一段对话，说几句让转录溢出；
2. 往上翻 → **右下角应该出现一颗圆的 `↓`**；
3. 悬停 → 提示「回到底部」；
4. 点它 → 滚到底、浮标消失；
5. 再发一句让它开始流式，**趁它在流的时候往上翻** → 浮标应该撑开成「↓ 有新内容」；
6. 打开坞里的「笔记本」，同样翻一次 → 那儿也该有一颗。

- [ ] **Step 3: 把结果如实写进上一条历史记录的 Verification 里**

「跑了，看见了」和「测试绿了」是两句话。
