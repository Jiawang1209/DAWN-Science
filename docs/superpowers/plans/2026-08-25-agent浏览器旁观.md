# agent 浏览器旁观 · 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 坞「网页」格分「自己浏览」|「agent 旁观」两个子页签;后者旁观 agent 的 headless 浏览器(截图一帧 + 去过哪儿 50 条)。

**Architecture:** 数据源是 `src/tools/browser/session.ts` 的模块级状态(与工具同进程);走现成 workbench 协议加两条只读操作 `browserObserve` / `browserFrame`;界面在 `web.tsx` 分面,agent 面是普通 DOM(Playwright 看得见)。

**Tech Stack:** zod 协议注册表、React + nanostores、vitest、Playwright e2e。

**Spec:** `docs/superpowers/specs/2026-08-25-agent浏览器旁观-design.md`

两条与 spec 的偏差(实现时把 spec 一并更正,见 Task 6):
1. 子页签文案定为 **「自己浏览」|「agent 旁观」**(spec 原写「浏览/agent」)——「浏览」是「用系统浏览器打开」的子串,过不了契约扫描。
2. e2e 跑真后端,验结构 + 空态 + WebContentsView 隐藏;**假历史/假帧不做**,真数据链路归 `browser-live.test.ts`。

---

### Task 1: session.ts 的旁观面(`旁观` / `截一帧`)

**Files:**
- Modify: `src/tools/browser/session.ts`
- Test: `tests/tools/browser.test.ts`

- [ ] **Step 1: 写失败的测试**(追加到 `tests/tools/browser.test.ts`,import 处加 `旁观, 截一帧`):

```ts
describe("旁观面(2026-08-25,坞「网页」格的「agent 旁观」页签)", () => {
  it("没开时如实说没开,历史照给(空数组也是答案)", async () => {
    const d = await 旁观()
    expect(d.open).toBe(false)
    expect(Array.isArray(d.history)).toBe(true)
    expect(d.activeTitle).toBe("")
  })
  it("没开时截帧响亮拒绝,不静默回空图,也不偷偷把浏览器拉起来", async () => {
    await expect(截一帧()).rejects.toThrow(/没开/)
    expect((await 旁观()).open).toBe(false) // 截帧失败之后浏览器仍然没被拉起来
  })
})
```

- [ ] **Step 2: 跑,确认红** — `npx vitest run tests/tools/browser.test.ts`,预期 `旁观 is not exported`。

- [ ] **Step 3: 实现**(追加到 `src/tools/browser/session.ts` 末尾):

```ts
/** 旁观面(2026-08-25,坞「网页」格「agent 旁观」页签):便宜的状态 + 历史,轮询用它 */
export async function 旁观(): Promise<{
  open: boolean
  channel: string
  activeUrl: string
  activeTitle: string
  tabs: number
  history: { url: string; title: string; at: string }[]
}> {
  const s = 状态()
  const t = 活跃 ? 标签们.get(活跃) : undefined
  const activeTitle = s.open && t && !t.page.isClosed() ? await t.page.title().catch(() => "") : ""
  return { open: s.open, channel: s.channel, activeUrl: s.activeUrl, tabs: s.tabs, activeTitle, history: [...历史] }
}

/**
 * 活跃页截一帧(PNG 的 base64)。**不走 `要页面()`**——那条会 lazy 启动浏览器,
 * 旁观面只看不推:没开就抛,话写给人看。
 */
export async function 截一帧(): Promise<string> {
  const t = 活跃 ? 标签们.get(活跃) : undefined
  if (!浏览器?.isConnected() || !t || t.page.isClosed()) {
    throw new Error("浏览器没开——agent 用过之后这里才有画面。")
  }
  return (await t.page.screenshot()).toString("base64")
}
```

- [ ] **Step 4: 跑,确认绿** — `npx vitest run tests/tools/browser.test.ts`
- [ ] **Step 5: Commit** — `git commit -m "feat(浏览器插件): session 旁观面——观察不启动"`

### Task 2: 协议两条操作 + 后端路由

**Files:**
- Modify: `src/protocol/operations.ts`(`setPluginFlag` 之后)
- Modify: `src/workbench/backend.ts`(`setPluginFlag` 之后;import 加 `旁观, 截一帧` from `../tools/browser/session.js`)
- Test: `tests/workbench/backend.test.ts`

- [ ] **Step 1: 失败测试**(追加进 backend.test.ts,用现成的 `backend` 实例):

```ts
it("browserObserve 没开如实说;browserFrame 响亮拒(2026-08-25 旁观面)", async () => {
  const d = (await backend.browserObserve({})) as { open: boolean; history: unknown[] }
  expect(d.open).toBe(false)
  await expect(backend.browserFrame({})).rejects.toThrow(/没开/)
})
```

- [ ] **Step 2: 跑,确认红**(属性不存在)。
- [ ] **Step 3: 协议注册**:

```ts
/* ── agent 浏览器旁观(2026-08-25,坞「网页」格收编) ── */

/** agent 那台 headless 浏览器的旁观面。**历史是观察不是转述**——记在我们自己的 page.on("load") 上 */
browserObserve: {
  request: z.object({}).strict(),
  response: z
    .object({
      open: z.boolean(),
      channel: z.string(),
      activeUrl: z.string(),
      activeTitle: z.string(),
      tabs: z.number().int().min(0),
      history: z.array(z.object({ url: z.string(), title: z.string(), at: z.string() }).strict()),
    })
    .strict(),
  mutating: false,
},

/** 活跃页截一帧(PNG base64)。浏览器没开就报错,**不静默回空图** */
browserFrame: {
  request: z.object({}).strict(),
  response: z.object({ png: z.string().min(1) }).strict(),
  mutating: false,
},
```

- [ ] **Step 4: 后端实现**:

```ts
/* ── agent 浏览器旁观(2026-08-25,规格 2026-08-25-agent浏览器旁观-design.md) ── */

browserObserve: async () => await 旁观(),

browserFrame: async () => {
  try {
    return { png: await 截一帧() }
  } catch (e) {
    throw fault("invalid_request", e instanceof Error ? e.message : String(e))
  }
},
```

- [ ] **Step 5: `npm run typecheck`** —— `WorkbenchBackend` 是全量映射类型,哪个 Fake 后端红了就照它对 `listPlugins` 的写法补两个最小实现。顺带看 `src/protocol/remote-capable.ts`:照 `listPlugins` 的归类处理这两条(旁观的是本机进程,远端会话上它答的仍是本机那台)。
- [ ] **Step 6: 跑测试确认绿**,`npx vitest run tests/workbench/`。
- [ ] **Step 7: Commit** — `feat(协议): browserObserve/browserFrame——旁观面两条只读操作`

### Task 3: 界面——web.tsx 分面 + AgentBrowserPane

**Files:**
- Create: `src/ui/agent-browser.tsx`
- Modify: `src/ui/web.tsx`、`src/ui/App.tsx:4244` 附近、`src/ui/styles.css`、`src/ui/i18n/en.ts`

- [ ] **Step 1: 新组件 `src/ui/agent-browser.tsx`**:

```tsx
/**
 * 「agent 旁观」面(2026-08-25):旁观 agent 那台 headless 浏览器。
 * **它是普通 DOM**——与「自己浏览」那半(WebContentsView,Playwright 看不见)不同,
 * 这一面 e2e 直接断言得到。轮询在 WebPanel 里(页签上的活跃点要用),这里只管画。
 */
import { useCallback, useEffect, useRef, useState } from "react"
import { Button } from "./primitives.js"
import { t, tf } from "./i18n/index.js"

export interface Agent旁观数据 {
  open: boolean
  channel: string
  activeUrl: string
  activeTitle: string
  tabs: number
  history: { url: string; title: string; at: string }[]
}

export function AgentBrowserPane({ 数, 错, frame, onRevisit }: {
  数: Agent旁观数据 | undefined
  /** 轮询失败的话(失败必须出声) */
  错: string
  frame(): Promise<string>
  onRevisit(url: string): void
}) {
  const [帧, 设帧] = useState<{ png: string; url: string; at: string } | undefined>()
  const [帧错, 设帧错] = useState("")
  const 帧过 = useRef("")
  const 刷帧 = useCallback(() => {
    void frame()
      .then((png) => {
        设帧({ png, url: "", at: new Date().toLocaleTimeString() })
        设帧错("")
      })
      .catch((e) => 设帧错(e instanceof Error ? e.message : String(e)))
  }, [frame])
  // 活跃 URL 变了自动重截一帧;手动按钮永远在
  useEffect(() => {
    const url = 数?.open ? 数.activeUrl : ""
    if (!url || url === 帧过.current) return
    帧过.current = url
    刷帧()
  }, [数?.open, 数?.activeUrl, 刷帧])

  if (!数) return <p className="hint">{错 || t("正在问 agent 的浏览器…")}</p>
  if (!数.open && 数.history.length === 0) {
    return <p className="empty">{t("agent 还没用过浏览器。它一动,这里就能看到它去过哪儿、正在看什么。")}</p>
  }
  return (
    <div className="agent-watch">
      {错 ? <p className="caveat">{错}</p> : null}
      <div className="agent-frame">
        {帧 ? (
          <img src={`data:image/png;base64,${帧.png}`} alt={t("agent 当前页的截图")} />
        ) : (
          <p className="hint">
            {数.open ? t("画面还没截过——按「刷新画面」。") : t("浏览器已经关了,画面没了;去过哪儿还在下面。")}
          </p>
        )}
        {帧错 ? <p className="caveat">{帧错}</p> : null}
        <div className="agent-frame-bar">
          <span className="agent-frame-url" title={数.activeUrl}>{数.activeUrl}</span>
          <Button variant="secondary" size="sm" disabled={!数.open} onClick={刷帧}>
            {t("刷新画面")}
          </Button>
          {帧 ? <span className="hint">{tf("截于 {0}", 帧.at)}</span> : null}
        </div>
        {数.activeTitle ? <p className="agent-frame-title">{数.activeTitle}</p> : null}
      </div>
      <div className="agent-visits">
        <h3 className="panel-title">{t("去过哪儿")}</h3>
        {数.history.length === 0 ? (
          <p className="hint">{t("还没去过任何地方。")}</p>
        ) : (
          <ul>
            {数.history.map((h, i) => (
              <li key={`${h.at}-${i}`}>
                {/* 重新访问是**你自己的会话**——不同登录态同一 URL 内容可以不同,措辞不许骗人 */}
                <button
                  className="agent-visit"
                  title={t("重新访问(你自己的会话,不是 agent 看到的那份)")}
                  onClick={() => onRevisit(h.url)}
                >
                  <span className="agent-visit-at">{h.at.slice(11, 19)}</span>
                  <span className="agent-visit-title">{h.title || h.url}</span>
                  <span className="agent-visit-url">{h.url}</span>
                </button>
              </li>
            ))}
          </ul>
        )}
      </div>
    </div>
  )
}
```

(`tf` 的确切导出名先看 `src/ui/i18n/index.ts`,不对就换成实际的插值函数。)

- [ ] **Step 2: `web.tsx` 分面**。`WebPanel` 加必选 prop `agent: { observe(): Promise<Agent旁观数据>; frame(): Promise<string> }`;组件内:

```tsx
const [面, 设面] = useState<"browse" | "agent">("browse")
const [旁观数, 设旁观数] = useState<Agent旁观数据 | undefined>()
const [旁观错, 设旁观错] = useState("")
// 2s 轻轮询(两个面都要:页签上的活跃点在浏览面也得亮)
useEffect(() => {
  let 活 = true
  const 问 = () =>
    void agent.observe().then((d) => { if (活) { 设旁观数(d); 设旁观错("") } })
      .catch((e) => { if (活) 设旁观错(e instanceof Error ? e.message : String(e)) })
  问()
  const 计 = setInterval(问, 2000)
  return () => { 活 = false; clearInterval(计) }
}, [agent])
/** 切面:离开浏览面要**立刻**把 WebContentsView 藏掉,不等 250ms 的命中测试 tick */
const 切面 = (到: "browse" | "agent") => {
  设面(到)
  上次.current = ""
  if (到 !== "browse") 发({ kind: "visible", on: false })
}
```

`待开` 那个 effect 里 `去(待开)` 之前加 `设面("browse")`(消息链接是给人看的,强制落浏览面)。

JSX:顶部加子页签行,原有 bar/error/slot 包进一个 `display` 控制的容器(**不卸载**——`状态` 与命中测试的 interval 都活着,`display:none` 时占位块矩形为 0,interval 自会把 `visible:false` 推过去兜底):

```tsx
<div className="webview">
  <div className="web-subtabs" role="tablist" aria-label={t("这一格看谁的")}>
    <Button variant="ghost" size="sm" role="tab" aria-selected={面 === "browse"} onClick={() => 切面("browse")}>
      {t("自己浏览")}
    </Button>
    <Button variant="ghost" size="sm" role="tab" aria-selected={面 === "agent"} onClick={() => 切面("agent")}>
      {t("agent 旁观")}
      {旁观数?.open ? <span className="agent-live-dot" aria-hidden /> : null}
    </Button>
  </div>
  <div className="web-browse" style={面 === "browse" ? undefined : { display: "none" }}>
    {/* …原有的 webview-bar / webview-error / webview-slot 原样搬进来… */}
  </div>
  {面 === "agent" ? (
    <AgentBrowserPane 数={旁观数} 错={旁观错} frame={agent.frame}
      onRevisit={(url) => { 切面("browse"); 去(url) }} />
  ) : null}
</div>
```

- [ ] **Step 3: `App.tsx` 装配**(4244 行那处):

```tsx
<WebPanel workspace={currentWorkspace} {...(projectId ? { projectId } : {})}
  agent={{
    observe: () => client.get("browserObserve", {}),
    frame: async () => (await client.get<{ png: string }>("browserFrame", {})).png,
  }} />
```

- [ ] **Step 4: `styles.css`**(色一律用 tokens.css 里现成的变量,活跃点用主题色变量——「活着跟主题色」那条):

```css
/* ── 网页格分面(2026-08-25,agent 浏览器旁观) ── */
.web-subtabs { display: flex; gap: 4px; padding: 4px 8px; border-bottom: 1px solid var(--border); }
.web-subtabs [aria-selected="true"] { background: var(--surface-2); }
.agent-live-dot { display: inline-block; width: 6px; height: 6px; border-radius: 50%; margin-left: 6px; background: var(--accent); }
.web-browse { display: flex; flex-direction: column; flex: 1; min-height: 0; }
.agent-watch { display: flex; flex-direction: column; gap: 8px; padding: 8px; overflow-y: auto; min-height: 0; }
.agent-frame img { max-width: 100%; border: 1px solid var(--border); border-radius: 4px; }
.agent-frame-bar { display: flex; align-items: center; gap: 8px; min-width: 0; }
.agent-frame-url { flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font-size: 12px; color: var(--muted); }
.agent-visits ul { list-style: none; margin: 0; padding: 0; }
.agent-visit { display: grid; grid-template-columns: auto 1fr; column-gap: 8px; width: 100%; text-align: left; background: none; border: none; padding: 4px 0; cursor: pointer; }
.agent-visit:hover { background: var(--surface-2); }
.agent-visit-at { color: var(--muted); font-variant-numeric: tabular-nums; }
.agent-visit-url { grid-column: 2; color: var(--muted); font-size: 12px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
```

变量名(`--border`/`--surface-2`/`--muted`/`--accent`)以 `tokens.css` 实际为准,写之前查一眼。

- [ ] **Step 5: `en.ts` 新增文案**(键=中文原文):自己浏览→"Browse here"、agent 旁观→"Agent view"、刷新画面→"Refresh frame"、这一格看谁的→"Whose view"、以及本 Task 出现的其余每一句 `t()`/`tf()`。英文侧同样不许互为子串("Browse here" 与 "Open in system browser" 互不包含,可)。
- [ ] **Step 6: 验证** — `npm run typecheck && npm test`(设计契约的子串扫描、i18n 扫描都在这里面;红了就换文案再跑)。
- [ ] **Step 7: Commit** — `feat(坞): 网页格分「自己浏览」「agent 旁观」两面`

### Task 4: e2e

**Files:**
- Create: `e2e/agent-browser.spec.ts`
- Modify: `e2e/web-preview.spec.ts`

- [ ] **Step 1: 新用例**(结构 + 空态;真后端里浏览器没开,空态就是真话):

```ts
/**
 * agent 浏览器旁观(2026-08-25):网页格分两面。
 * agent 那一面是**普通 DOM**——这一格里唯一 Playwright 看得见的东西,判据直接断言。
 */
import { test, expect, 在项目里开会话, 进坞 } from "./fixtures.js"

test("网页格两个子页签;agent 旁观空态如实说;切回浏览面地址栏还在", async ({ dawn }) => {
  const { page } = dawn
  await 在项目里开会话(page)
  await 进坞(page, "网页")
  await expect(page.getByRole("tab", { name: "自己浏览" })).toBeVisible()
  await page.getByRole("tab", { name: "agent 旁观" }).click()
  await expect(page.getByText(/agent 还没用过浏览器/)).toBeVisible()
  await expect(page.getByLabel("网址")).toBeHidden()
  await page.getByRole("tab", { name: "自己浏览" }).click()
  await expect(page.getByLabel("网址")).toBeVisible()
})
```

- [ ] **Step 2: `web-preview.spec.ts` 加一条**(用它现成的本机服务与 `视图(app)` 探针;fixture 里 app 的取法照该文件现有用例):

```ts
test("切到「agent 旁观」时,人的 WebContentsView 要藏起来;切回来还在原页", async ({ dawn }) => {
  const { page, app } = dawn
  await 开一段临时会话(page)
  await 进网页(page)
  await page.getByLabel("网址").fill(`http://${地址}/`)
  await page.getByLabel("网址").press("Enter")
  await expect.poll(async () => (await 视图(app))?.url ?? "").toContain(地址)
  await page.getByRole("tab", { name: "agent 旁观" }).click()
  await expect.poll(async () => (await 视图(app))?.visible).toBe(false)
  await page.getByRole("tab", { name: "自己浏览" }).click()
  await expect.poll(async () => (await 视图(app))?.visible).toBe(true)
  await expect.poll(async () => (await 视图(app))?.url ?? "").toContain(地址)
})
```

- [ ] **Step 3: 跑** — `npm run test:e2e -- agent-browser web-preview`(先 build)。另外单跑 `npm run test:e2e:visual` 确认十张基线没被动到(网页格不在五屏里,应当全绿;红了看 diff,不许条件反射 update)。
- [ ] **Step 4: Commit** — `test(e2e): agent 旁观两面切换与 WebContentsView 隐藏`

### Task 5: 真浏览器整链路(browser-live)

**Files:**
- Modify: `tests/tools/browser-live.test.ts`

- [ ] **Step 1: 追加**(该文件已有「探测不到浏览器带理由跳过”的开关与已 open 的页,照它的结构挂在 open 之后):

```ts
it("旁观:open 之后 observe 里有那条历史;frame 是合法 PNG", async () => {
  const d = await 旁观()
  expect(d.open).toBe(true)
  expect(d.history.length).toBeGreaterThan(0)
  expect(d.history[0]?.url).toBeTruthy()
  const png = Buffer.from(await 截一帧(), "base64")
  expect(png.subarray(0, 8).toString("hex")).toBe("89504e470d0a1a0a")
})
```

- [ ] **Step 2: 跑** — `npx vitest run tests/tools/browser-live.test.ts`(本机有 Chrome,应当真跑不跳)。
- [ ] **Step 3: Commit** — `test(浏览器插件): live 链路补旁观面`

### Task 6: 文档与 spec 更正

**Files:**
- Modify: `docs/superpowers/specs/2026-08-25-agent浏览器旁观-design.md`(文案更正为「自己浏览/agent 旁观」+ 子串理由;判据 e2e 行改为「结构 + 空态 + WebContentsView 隐藏,真数据链路在 browser-live」)
- Modify: `CLAUDE.md` 文档入口表加一行;`docs/DEVELOPMENT_HISTORY.md` 顶部加条目(模板照旧)
- [ ] **Step 1: 改三个文档**
- [ ] **Step 2: 最后全量验证** — `npm run typecheck && npm test && npm run test:e2e`
- [ ] **Step 3: Commit** — `feat(坞): agent 浏览器旁观收进网页格(docs+history)`

---

## Self-review 已做

- spec 每节都有对应任务(形状→T3,数据→T1/T2,状态归位→T3 Step 2,判据→T1/T2/T4/T5,更正→T6)。
- 无占位符;`旁观`/`截一帧`/`Agent旁观数据` 各任务间签名一致。
- mock 说明:两条新操作是 workbench 协议操作,由真后端服务(与 `listPlugins` 同例);`mock-inference-server` 管的是模型协议,不需要分支——e2e 里空态即真话。
