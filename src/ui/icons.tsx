/**
 * 图标（2026-08-12）。
 *
 * 作者：*「我觉得我们的图标也没有 workbuddy 好看，这个我觉得也要学习，
 * 我觉得他们的图标质感非常的棒。」*
 *
 * ## 量出来的三条，质感就出在这里
 *
 * CDP 连上运行中的 WorkBuddy 读它的 60 个 `<svg>`，主力形态是：
 *
 * ```
 * <svg viewBox="0 0 16 16" fill="currentColor" width=16 height=16 aria-hidden="true">
 * 色：rgba(0,0,0,.7)      高亮那颗：rgba(0,0,0,.9)
 * ```
 *
 * 1. **实心，`fill: currentColor`，`stroke: none`。**
 *    我原本猜它是线性描边——**量出来正好相反**。实心的好处不是「好看」，
 *    是它跟着文字色走：那四档透明度（1 / .7 / .5 / .3）一套下来，
 *    图标与它旁边的字**永远在同一档上**，暗色也不用另配一份。
 *    描边图标做不到这件事：`stroke-width` 是绝对的，颜色一淡它就先散掉。
 * 2. **16×16 的画布上画 16px 的图标**，不是把 24 的缩下来——
 *    边缘正好压在像素上，不发虚。
 * 3. **`aria-hidden="true"`**：图标从不承担含义，含义在它旁边的字里。
 *
 * ## 我们不取它的 path
 *
 * 与读 Hermes / Rho 同一条纪律：**读事实，不读表达**。
 * 量到的是「16px / 实心 / currentColor」这三条**做法**，
 * 那些 `d="M6.6651 0.0001L8.747…"` 是它的表达，一个字节都不抄。
 * 下面每一个 path 都是照着这三条自己画的。
 *
 * ## 为什么不是 emoji
 *
 * 此前侧栏用的是 `💬` `📁` `＋`。emoji 是**彩色字形**——
 * 它吃不到 `currentColor`，于是永远脱离那四档层次；
 * 而且它跟着操作系统的字体走，**同一份代码在两台机器上长得不一样**。
 */

/** 所有图标共用的外壳。**尺寸与 `aria-hidden` 只在这里定义一次** */
function 壳({ children, className }: { children: React.ReactNode; className?: string }) {
  return (
    <svg
      className={className ? `icon ${className}` : "icon"}
      viewBox="0 0 16 16"
      width="16"
      height="16"
      fill="currentColor"
      aria-hidden="true"
      focusable="false"
    >
      {children}
    </svg>
  )
}

type 图标属性 = { className?: string }

/** 对话。一颗实心气泡，左下角一个小尖 */
export function 对话图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M8 2.5c3.3 0 5.8 2 5.8 4.7 0 2.6-2.5 4.7-5.8 4.7-.5 0-1-.1-1.5-.2l-2.9 1.3a.4.4 0 0 1-.5-.5l.7-1.9C2.7 9.8 2.2 8.7 2.2 7.2 2.2 4.5 4.7 2.5 8 2.5Z" />
    </壳>
  )
}

/** 文件夹。**这里是工作路径的意思**，不是「文件」 */
export function 文件夹图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M2 4.6c0-.8.6-1.4 1.4-1.4h2.7c.4 0 .8.2 1.1.5l.8.9h4.6c.8 0 1.4.6 1.4 1.4v5.4c0 .8-.6 1.4-1.4 1.4H3.4c-.8 0-1.4-.6-1.4-1.4V4.6Z" />
    </壳>
  )
}

/** 加号。**用它替掉全角「＋」**——那个字符的粗细与位置跟着字体走 */
export function 加号图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M7.2 3.2a.8.8 0 0 1 1.6 0v4h4a.8.8 0 0 1 0 1.6h-4v4a.8.8 0 0 1-1.6 0v-4h-4a.8.8 0 0 1 0-1.6h4v-4Z" />
    </壳>
  )
}

/** 更多（⋯）。三颗实心圆点 */
export function 更多图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <circle cx="3.6" cy="8" r="1.25" />
      <circle cx="8" cy="8" r="1.25" />
      <circle cx="12.4" cy="8" r="1.25" />
    </壳>
  )
}

/** 复制。两张叠着的纸 */
export function 复制图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M5.6 1.8h5.2c1 0 1.8.8 1.8 1.8v5.2c0 1-.8 1.8-1.8 1.8H5.6c-1 0-1.8-.8-1.8-1.8V3.6c0-1 .8-1.8 1.8-1.8Z" />
      <path d="M2.4 5.2v6c0 1.4 1.2 2.6 2.6 2.6h5a.9.9 0 0 0 0-1.8H5c-.4 0-.8-.4-.8-.8v-6a.9.9 0 0 0-1.8 0Z" />
    </壳>
  )
}

/** 铅笔。**「改这句话」的意思**，不是「写一句新的」 */
export function 铅笔图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M11.1 1.9a1.6 1.6 0 0 1 2.3 0l.7.7a1.6 1.6 0 0 1 0 2.3l-.9.9-3-3 .9-.9Z" />
      <path d="M9.3 3.7l3 3-6 6a1 1 0 0 1-.5.3l-3 .7a.5.5 0 0 1-.6-.6l.7-3a1 1 0 0 1 .3-.5l6.1-5.9Z" />
    </壳>
  )
}

/** 置顶。一枚图钉 */
export function 置顶图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M9.9 1.4a1 1 0 0 1 1.4 0l3.3 3.3a1 1 0 0 1-.6 1.7l-1.6.2-3 3 .3 2.2a1 1 0 0 1-1.7.9L5.5 10.5l-3 3a.7.7 0 0 1-1-1l3-3-2.2-2.5a1 1 0 0 1 .9-1.7l2.2.3 3-3 .2-1.6a1 1 0 0 1 .3-.6Z" />
    </壳>
  )
}

/** 删除。一个垃圾桶 */
export function 删除图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M6.2 1.6h3.6c.6 0 1.1.5 1.1 1.1v.6h2.4a.8.8 0 0 1 0 1.6h-.4l-.6 8a1.8 1.8 0 0 1-1.8 1.7H5.5a1.8 1.8 0 0 1-1.8-1.7l-.6-8h-.4a.8.8 0 0 1 0-1.6h2.4v-.6c0-.6.5-1.1 1.1-1.1Zm.5 1.7h2.6v-.4H6.7v.4Z" />
    </壳>
  )
}

/**
 * 展开／收起。**一个三角，靠 CSS 旋转表达两态**——
 * 两个不同的字形会让展开这件事看起来像换了个东西。
 */
export function 三角图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M6 4.1a.6.6 0 0 1 1-.5l4 3.9a.6.6 0 0 1 0 .9l-4 3.9a.6.6 0 0 1-1-.5V4.1Z" />
    </壳>
  )
}

/**
 * 下拉（▾）。**pill 与下拉按钮尾巴上那个记号**。
 *
 * 它此前是字符 `▾`——那个字形在不同系统上大小差得很多，
 * 而它就贴在文字后面，一大一小格外显眼。
 */
export function 下拉图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M3.6 6.1a.7.7 0 0 1 1.2-.5L8 8.8l3.2-3.2a.7.7 0 0 1 1 1l-3.7 3.7a.7.7 0 0 1-1 0L3.8 6.6a.7.7 0 0 1-.2-.5Z" />
    </壳>
  )
}

/** 发送。一支朝上的箭头 */
export function 上箭头图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M7.4 2.6a.9.9 0 0 1 1.2 0l4.5 4.4a.9.9 0 0 1-1.2 1.3L8.9 5.6v7.5a.9.9 0 0 1-1.8 0V5.6L4.1 8.3a.9.9 0 1 1-1.2-1.3l4.5-4.4Z" />
    </壳>
  )
}

/** 终端。一个提示符 */
export function 终端图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M2.6 2.2h10.8c.9 0 1.6.7 1.6 1.6v8.4c0 .9-.7 1.6-1.6 1.6H2.6c-.9 0-1.6-.7-1.6-1.6V3.8c0-.9.7-1.6 1.6-1.6Zm1.6 3.2a.8.8 0 0 0-.5 1.4L5 8l-1.3 1.2a.8.8 0 1 0 1.1 1.2l1.9-1.8a.8.8 0 0 0 0-1.2L4.8 5.6a.8.8 0 0 0-.6-.2Zm4.3 4.4a.8.8 0 0 0 0 1.6h3a.8.8 0 0 0 0-1.6h-3Z" />
    </壳>
  )
}

/** 设置。一个齿轮的简化：圆 + 六个齿 */
export function 设置图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M8 1.2c.5 0 .9.3 1 .8l.2 1c.4.1.8.3 1.1.5l.9-.5a1 1 0 0 1 1.3.3l.7 1.2a1 1 0 0 1-.3 1.3l-.8.6a4.6 4.6 0 0 1 0 1.2l.8.6a1 1 0 0 1 .3 1.3l-.7 1.2a1 1 0 0 1-1.3.3l-.9-.5c-.3.2-.7.4-1.1.5l-.2 1a1 1 0 0 1-1 .8h-1.4a1 1 0 0 1-1-.8l-.2-1a4.6 4.6 0 0 1-1.1-.5l-.9.5a1 1 0 0 1-1.3-.3l-.7-1.2a1 1 0 0 1 .3-1.3l.8-.6a4.6 4.6 0 0 1 0-1.2l-.8-.6a1 1 0 0 1-.3-1.3l.7-1.2a1 1 0 0 1 1.3-.3l.9.5c.3-.2.7-.4 1.1-.5l.2-1a1 1 0 0 1 1-.8H8Zm-.3 4.4a2.4 2.4 0 1 0 0 4.8 2.4 2.4 0 0 0 0-4.8Z" />
    </壳>
  )
}

/** 文件。一页纸，右上角折角 */
export function 文件图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M4 1.4h4.3l4.3 4.3v7.3c0 .9-.7 1.6-1.6 1.6H4c-.9 0-1.6-.7-1.6-1.6V3c0-.9.7-1.6 1.6-1.6Zm5 1.4v2.5c0 .3.2.5.5.5h2.5L9 2.8Z" />
    </壳>
  )
}

/** 概览。三根长短不一的柱子 */
export function 概览图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M2.6 8.4c0-.5.4-.9.9-.9s.9.4.9.9v4.2a.9.9 0 0 1-1.8 0V8.4ZM7.1 4.2c0-.5.4-.9.9-.9s.9.4.9.9v8.4a.9.9 0 0 1-1.8 0V4.2ZM11.6 6.6c0-.5.4-.9.9-.9s.9.4.9.9v6a.9.9 0 0 1-1.8 0v-6Z" />
    </壳>
  )
}

/**
 * 出错。一个实心圆里一个感叹号。
 *
 * **不用 `⊘` 那个字符**：它是彩色字形之外的另一种坑——字宽与基线跟着字体走，
 * 在一行文字里高低不齐；而且它吃不到 `currentColor` 的层次。
 */
export function 出错图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M8 1.4a6.6 6.6 0 1 1 0 13.2A6.6 6.6 0 0 1 8 1.4Zm0 3a.9.9 0 0 0-.9 1v3.2a.9.9 0 0 0 1.8 0V5.4a.9.9 0 0 0-.9-1Zm0 6a1.05 1.05 0 1 0 0 2.1 1.05 1.05 0 0 0 0-2.1Z" />
    </壳>
  )
}

/** 技能。一颗四角星——**「一手绝活」的意思**，不是一个工具 */
export function 技能图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M8 1.2c.3 0 .6.2.7.5l1.1 3 3 1.1a.75.75 0 0 1 0 1.4l-3 1.1-1.1 3a.75.75 0 0 1-1.4 0l-1.1-3-3-1.1a.75.75 0 0 1 0-1.4l3-1.1 1.1-3c.1-.3.4-.5.7-.5Z" />
      <path d="M12.9 10.6c.2 0 .4.1.5.3l.4 1 1 .4a.55.55 0 0 1 0 1l-1 .4-.4 1a.55.55 0 0 1-1 0l-.4-1-1-.4a.55.55 0 0 1 0-1l1-.4.4-1c.1-.2.3-.3.5-.3Z" />
    </壳>
  )
}

/** 插件。一块拼图 */
export function 插件图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M6.4 1.2a2.1 2.1 0 0 1 2.1 2.1v.4h2.1c.8 0 1.4.6 1.4 1.4v2.1h.4a2.1 2.1 0 1 1 0 4.2h-.4v2.1c0 .8-.6 1.4-1.4 1.4H8.9a.7.7 0 0 1-.7-.7v-1.1a1.1 1.1 0 1 0-2.2 0v1.1a.7.7 0 0 1-.7.7H3.2c-.8 0-1.4-.6-1.4-1.4V9.9a.7.7 0 0 1 .7-.7h1.1a1.1 1.1 0 1 0 0-2.2H2.5a.7.7 0 0 1-.7-.7V5.1c0-.8.6-1.4 1.4-1.4h2.1v-.4A2.1 2.1 0 0 1 6.4 1.2Z" />
    </壳>
  )
}

/** 勾。**「就是这一个」**——列表里当前那一条 */
export function 勾图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M13.3 4.2a.9.9 0 0 1 0 1.3l-6 6a.9.9 0 0 1-1.3 0L2.7 8.2a.9.9 0 1 1 1.3-1.3l2.6 2.7 5.4-5.4a.9.9 0 0 1 1.3 0Z" />
    </壳>
  )
}

/** 外观。半明半暗的一个圆——**主题这件事本身就是明暗** */
export function 外观图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M8 1.2a6.8 6.8 0 1 1 0 13.6A6.8 6.8 0 0 1 8 1.2Zm0 1.6v10.4a5.2 5.2 0 0 0 0-10.4Z" />
    </壳>
  )
}

/** 模型。一个立方体 */
export function 模型图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M7.3 1.4a1.4 1.4 0 0 1 1.4 0l4.6 2.7c.2.1.2.4 0 .5L8.3 7.5a.6.6 0 0 1-.6 0L2.7 4.6c-.2-.1-.2-.4 0-.5l4.6-2.7ZM2 6.1c0-.2.2-.3.4-.2l4.7 2.7c.2.1.3.3.3.5v5.3c0 .2-.2.4-.4.3L2.7 12A1.4 1.4 0 0 1 2 10.8V6.1Zm12 0v4.7c0 .5-.3 1-.7 1.2l-4.3 2.5c-.2.1-.4 0-.4-.3V9.1c0-.2.1-.4.3-.5l4.7-2.7c.2-.1.4 0 .4.2Z" />
    </壳>
  )
}

/** 服务器。远端连接那一区 */
export function 服务器图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M2.8 2.2h10.4c1 0 1.8.8 1.8 1.8v1.6c0 1-.8 1.8-1.8 1.8H2.8C1.8 7.4 1 6.6 1 5.6V4c0-1 .8-1.8 1.8-1.8Zm.9 1.8a.9.9 0 1 0 0 1.8.9.9 0 0 0 0-1.8ZM2.8 8.6h10.4c1 0 1.8.8 1.8 1.8V12c0 1-.8 1.8-1.8 1.8H2.8C1.8 13.8 1 13 1 12v-1.6c0-1 .8-1.8 1.8-1.8Zm.9 1.8a.9.9 0 1 0 0 1.8.9.9 0 0 0 0-1.8Z" />
    </壳>
  )
}

/**
 * 侧栏。一块面板，左边那一列是实的。
 *
 * **一个图标管两态**（收起 / 展开），含义由它旁边的 `aria-label` 说——
 * 两个不同的字形会让「折叠」这件事看起来像换了个功能，
 * 这和远端那颗三角靠旋转表达两态是同一条。
 */
export function 侧栏图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path
        fillRule="evenodd"
        d="M3 2.6h10a1.8 1.8 0 0 1 1.8 1.8v7.2A1.8 1.8 0 0 1 13 13.4H3a1.8 1.8 0 0 1-1.8-1.8V4.4A1.8 1.8 0 0 1 3 2.6Zm3.6 1.4v8H13a.8.8 0 0 0 .8-.8V4.8a.8.8 0 0 0-.8-.8H6.6Z"
      />
    </壳>
  )
}

/** 搜索。一个放大镜——柄朝右下，与它旁边那些实心图标同一副重量 */
export function 搜索图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <path d="M7.1 1.4a5.7 5.7 0 0 1 4.5 9.2l3 3a.95.95 0 0 1-1.34 1.34l-3-3A5.7 5.7 0 1 1 7.1 1.4Zm0 1.9a3.8 3.8 0 1 0 0 7.6 3.8 3.8 0 0 0 0-7.6Z" />
    </壳>
  )
}

/** 停止。一个圆角实心方块——**与「播放/发送」那个三角是一对反义词** */
export function 停止图标({ className }: 图标属性 = {}) {
  return (
    <壳 {...(className ? { className } : {})}>
      <rect x="4" y="4" width="8" height="8" rx="1.6" />
    </壳>
  )
}

/**
 * 内核语言的小徽标（②，2026-08-14 作者要的）。
 *
 * 作者：*「如果是 R 语言的话，那么界面就标记一个 R 的 logo，
 * 如果是 python 的话，那就 python 的 logo。」*
 *
 * **用的是字母 + 各自生态眼熟的配色，不是官方 logo。** 两个理由：
 * 官方标识是商标图形（Python 那两条蛇在 16px 下也糊成一团），
 * 而**字母在这个尺寸上反而更快认**——这一格只有一行高。
 * 颜色照搬各自社区常用的那两支，一眼就分得开。
 */
export function R图标({ className }: 图标属性 = {}) {
  return (
    <svg className={className} viewBox="0 0 16 16" aria-hidden="true">
      <rect width="16" height="16" rx="3" fill="var(--dawn-lang-r)" />
      <text
        x="8"
        y="12"
        textAnchor="middle"
        fontSize="11"
        fontWeight="700"
        fill="var(--dawn-lang-r-text)"
        fontFamily="var(--dawn-font-sans)"
      >
        R
      </text>
    </svg>
  )
}

export function Python图标({ className }: 图标属性 = {}) {
  return (
    <svg className={className} viewBox="0 0 16 16" aria-hidden="true">
      {/* 上蓝下黄——Python 那两支颜色，比画蛇更认得出来 */}
      <rect width="16" height="16" rx="3" fill="var(--dawn-lang-py)" />
      <path d="M0 8h16v5a3 3 0 0 1-3 3H3a3 3 0 0 1-3-3z" fill="var(--dawn-lang-py-accent)" />
      <text
        x="8"
        y="11.5"
        textAnchor="middle"
        fontSize="9"
        fontWeight="700"
        fill="var(--dawn-lang-py-text)"
        fontFamily="var(--dawn-font-sans)"
      >
        Py
      </text>
    </svg>
  )
}
