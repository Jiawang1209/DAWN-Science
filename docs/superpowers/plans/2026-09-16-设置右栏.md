# 设置右栏 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 设置先以一条右栏出现（对话还在），按 ⤢ 才展开成现在这版整页；顺带给 MCP 与插件补上行尾计数。

**Architecture:** 设置不新造一套宽度/缝/记忆——它**住进右侧坞那一列**（`--dawn-dock-w`），与坞互斥。
一份 `sections` 数组喂两个形状：整页 `SettingsShell`（不改一个像素）与新的窄栏 `SettingsColumn`（名单 → 那一项的下钻）。
右边只有一条规则：**设置活着时那一列归设置；设置一关，被顶掉的房客回来。**

**Tech Stack:** React + nanostores（`src/ui/state/`）、Playwright e2e（跑真实构建产物）、vitest 单元。

规格：`docs/superpowers/specs/2026-09-16-设置右栏-design.md`。分支 `settings-panel`。

---

## 文件结构

| 文件 | 负责什么 |
|---|---|
| **新建** `src/ui/state/settings-column.ts` | 设置栏的状态与那四个动作（开/关/展开/收起），以及「被顶掉的是谁」。**只有它知道设置与坞的互斥规则** |
| **新建** `src/ui/settings-column.tsx` | `SettingsColumn` 组件：名单 ⇄ 那一项的下钻。**只管画，不管状态** |
| **新建** `e2e/settings-column.spec.ts` | 版面那五条判据 |
| **新建** `e2e/settings-counts.spec.ts` | 行尾计数那一条 |
| **新建** `tests/ui/settings-column.test.ts` | 互斥与回程的纯逻辑 |
| 改 `src/ui/state/view.ts` | `$settingsSection` 加持久化；`openSettingsSection` 改名为 `打开设置整页`（只剩连不上那一处用） |
| 改 `src/ui/state/index.ts` | 转出新模块 |
| 改 `src/ui/App.tsx` | ① `sections` 提成变量供两处共用 ② 入口改成开列 ③ 整页加 ⤡ ④ 两个计数 |
| 改 `src/ui/Settings.tsx` | `SettingsShell` 多收一个可选的 `onCollapse`，画那颗 ⤡ |
| 改 `src/ui/styles.css` | `.settings-column` 一族 |
| 改 `src/ui/i18n/en.ts` | 新文案的英文 |
| 改 `e2e/fixtures.ts` | `进设置` 改成「开列 → 展开 → 点分类」，26 支既有用例一个字不动 |

---

## Task 1：MCP 与插件的行尾计数

作者的第一件事，与版面无关，先单独落地。

**Files:**
- Create: `e2e/settings-counts.spec.ts`
- Modify: `src/ui/App.tsx`（`技能数` 那一组 state 附近，约 3137 行；`id: "mcp"` / `id: "plugins"` 两处 section）

- [ ] **Step 1: 写失败的 e2e**

```ts
// e2e/settings-counts.spec.ts
/**
 * 「扩展」那一组的行尾计数（2026-09-16，作者要的）。
 *
 * 作者：*「设置里面的 MCP 服务器，我其实希望你能在后面增加上数字，
 * 类似我们的 skills 后面增加配置好的数字。」*
 *
 * **口径 = 开着的台数**（与 `设技能数(开着的.length)` 一致）。三种读法里
 * 「连上的几台」被否掉是因为它会骗人：`state` 在手动点过「测试」之前一律是
 * `unknown`，那个数多数时候会是 0。
 */
import { test, expect, 进设置 } from "./fixtures.js"
import { join } from "node:path"

const 脚本 = join(process.cwd(), "scripts", "mcp-test-server.mjs")

/** 两台，都不关 —— 所以期望的数字是 2 */
const 配置 = `mcp:
  testbox:
    command: ${JSON.stringify(process.execPath)}
    args: [${JSON.stringify(脚本)}]
  needskey:
    command: ${JSON.stringify(process.execPath)}
    args: [${JSON.stringify(脚本)}]
    env: [DAWN_MCP_TEST_SECRET]
agents:
  ds-chat:
    kind: native
    provider: deepseek
    model: deepseek-v4-flash
    capabilities: [chat]
`

test.use({ dawnOptions: { providersYaml: 配置 } })

test("**MCP 服务器那一行，行尾的数是开着的台数**", async ({ dawn }) => {
  const { page } = dawn
  await 进设置(page, "外观")
  const 行 = page.locator(".settings-nav-item").filter({ hasText: "MCP 服务器" })
  await expect(行.locator(".side-count")).toHaveText("2")
})

test("**插件那一行也有一个同口径的数**", async ({ dawn }) => {
  const { page } = dawn
  await 进设置(page, "外观")
  const 行 = page.locator(".settings-nav-item").filter({ hasText: "插件" })
  // 内置插件至少一个（Office 那张卡），默认开着。**只断言「有个数且不是空」**——
  // 钉死具体几个会在加插件那天无辜变红，而这条要守的是「这一行有数」
  await expect(行.locator(".side-count")).toHaveText(/^\d+$/)
})
```

- [ ] **Step 2: 跑，确认两条都红**

```bash
npm run build && npx playwright test e2e/settings-counts.spec.ts --reporter=line
```

期望：两条都失败，报 `.side-count` 找不到（`element(s) not found`）。

- [ ] **Step 3: 在 `App.tsx` 里加两个数**

在 `const [技能数, 设技能数] = useState<number | undefined>(undefined)`（约 3137 行）下面加：

```tsx
  /**
   * 「扩展」那一组的行尾计数（2026-09-16，作者要的）。
   *
   * **口径与 `技能数` 一致：数「开着的」**，不是「配了几个」。同一个位置上的
   * 两个数用两种口径，正是这个项目吃过亏的那类「没有判据」。
   *
   * MCP **按项目问**（项目级 `.dawn/mcp.yaml` 会追加几台），所以它跟着
   * `projectId` 走——换项目时这个数会变，那是实情不是抖动。
   */
  const [MCP开着数, 设MCP开着数] = useState<number | undefined>(undefined)
  const [插件开着数, 设插件开着数] = useState<number | undefined>(undefined)
```

在那段已有的 `useEffect`（`设技能数(开着的.length)` 所在，约 3160–3175 行）**同一个 effect 内**追加两条载入；`还在` 那个陈旧守卫照抄已有写法：

```tsx
    client
      .get<MCP装载>("listMcpServers", projectId ? { projectId } : {})
      .then((r) => 还在 && 设MCP开着数(r.servers.filter((s) => !s.off).length))
      .catch((e: unknown) => console.error("[设置] MCP 台数读不到，那一行就不显示数字：", e))
    client
      .get<{ plugins: import("./skills.js").插件一个[] }>("listPlugins", {})
      .then((r) => 还在 && 设插件开着数(r.plugins.filter((p) => p.on).length))
      .catch((e: unknown) => console.error("[设置] 插件数读不到，那一行就不显示数字：", e))
```

**并把这个 effect 的依赖数组加上 `projectId`**（MCP 按项目问，不加就永远是第一个项目的数）。

然后在两处 section 上挂：

```tsx
                  id: "mcp",
                  group: t("扩展"),
                  title: t("MCP 服务器"),
                  count: MCP开着数,
```

```tsx
                  id: "plugins",
                  group: t("扩展"),
                  title: t("插件"),
                  count: 插件开着数,
```

`SettingsSection.count` 与 `.side-count` 都是现成的（`Settings.tsx` 约 652 行），不用改。

- [ ] **Step 4: 跑，确认两条都绿**

```bash
npm run build && npx playwright test e2e/settings-counts.spec.ts --reporter=line
```

期望：`2 passed`。

- [ ] **Step 5: 单元与类型**

```bash
npm run typecheck && npm test 2>&1 | grep -E "Test Files|Tests "
```

期望：typecheck 无输出错误；单元全绿。

- [ ] **Step 6: 提交**

```bash
git add e2e/settings-counts.spec.ts src/ui/App.tsx
git commit -m "feat(settings): count enabled MCP servers and plugins in the nav"
```

---

## Task 2：`$settingsSection` 记住

窄栏与整页共用「当前选中」，而它要跨重启活着（规格：`dawn.global.settings-section`）。

**Files:**
- Modify: `src/ui/state/view.ts:37-41`
- Test: `tests/ui/settings-section-persist.test.ts`（新建）

- [ ] **Step 1: 写失败的单元测试**

```ts
// tests/ui/settings-section-persist.test.ts
/**
 * 设置分类要跨重启活着（规格 `2026-09-16-设置右栏`）。
 *
 * **key 里的 `global` 是作用域声明**（`view.ts` 那条纪律：持久化状态必须在
 * key 里声明作用域）。它不属于某段会话、也不属于某个项目。
 */
import { beforeEach, describe, expect, it, vi } from "vitest"
import { $settingsSection, SETTINGS_SECTION_KEY, loadSettingsSection, 选设置分类 } from "../../src/ui/state/view.js"

beforeEach(() => {
  localStorage.clear()
  $settingsSection.set(undefined)
})

describe("设置分类的持久化", () => {
  it("选过之后存得下来", () => {
    选设置分类("models")
    expect(localStorage.getItem(SETTINGS_SECTION_KEY)).toBe("models")
  })

  it("读得回来", () => {
    localStorage.setItem(SETTINGS_SECTION_KEY, "mcp")
    loadSettingsSection()
    expect($settingsSection.get()).toBe("mcp")
  })

  it("**没存过就是没表达过偏好**——缺失不等于某个具体值", () => {
    loadSettingsSection()
    expect($settingsSection.get()).toBeUndefined()
  })

  it("**存储写不进去也不该拦住这一次选择**", () => {
    const 坏 = vi.spyOn(Storage.prototype, "setItem").mockImplementation(() => {
      throw new Error("quota")
    })
    const 吵 = vi.spyOn(console, "error").mockImplementation(() => {})
    选设置分类("kernels")
    expect($settingsSection.get()).toBe("kernels")
    expect(吵).toHaveBeenCalled() // 失败必须出声（规格 7.5）
    坏.mockRestore()
    吵.mockRestore()
  })
})
```

- [ ] **Step 2: 跑，确认红**

```bash
npx vitest run tests/ui/settings-section-persist.test.ts
```

期望：失败，`SETTINGS_SECTION_KEY` / `loadSettingsSection` / `选设置分类` 都不存在。

- [ ] **Step 3: 在 `view.ts` 里实现**

把 `view.ts` 里这一段（约 37–41 行）：

```ts
export const $settingsSection = atom<string | undefined>(undefined)
export const openSettingsSection = (id?: string) => {
  $settingsSection.set(id)
  $view.set("settings")
}
```

换成：

```ts
export const $settingsSection = atom<string | undefined>(undefined)

/** key 里的 `global` 是**作用域声明**：它不属于某段会话、也不属于某个项目 */
export const SETTINGS_SECTION_KEY = "dawn.global.settings-section"

/**
 * 选一类。**先生效，再尝试记住**——存储写不进去不该连这一次选择都不给选
 * （与 `setRightDockWidth` 同一条）。
 */
export function 选设置分类(id: string | undefined): void {
  $settingsSection.set(id)
  try {
    if (id === undefined) localStorage.removeItem(SETTINGS_SECTION_KEY)
    else localStorage.setItem(SETTINGS_SECTION_KEY, id)
  } catch (e) {
    console.error("[设置] 分类保存失败，本次切换仍然生效，但重启后不会被记住：", e)
  }
}

/**
 * 启动时读回。**没存过就是 `undefined`**——缺失不等于某个具体值，
 * 这里的缺失就是「没表达过偏好」，由设置屏自己回落到第一项。
 */
export function loadSettingsSection(): void {
  try {
    const v = localStorage.getItem(SETTINGS_SECTION_KEY)
    if (v !== null) $settingsSection.set(v)
  } catch (e) {
    console.error("[设置] 读不到已保存的分类，回落到默认：", e)
  }
}

/**
 * **打开整页设置。**
 *
 * 今天的调用点只有一处：命令面板里那五条 `settings.*`（`commands.ts`）。
 * **（2026-09-16 审查纠正的：`ConnectionSurface` 走的是 `actions.openSettings`，
 * 不经过这里——原先这段注释把它写成了唯一调用方，两个方向都是假的。）**
 *
 * Task 6 之后会再多一处、且只多一处：连不上时 `ConnectionSurface` 那颗
 * 「打开设置」——那时主区本来就没有对话，挤出一条栏来挤谁？
 * 别处一律走 `开设置栏`（`state/settings-column.ts`）。
 */
export const 打开设置整页 = (id?: string) => {
  选设置分类(id)
  $view.set("settings")
}
```

- [ ] **Step 4: 跑，确认绿**

```bash
npx vitest run tests/ui/settings-section-persist.test.ts
```

期望：`4 passed`。

- [ ] **Step 5: 补上调用点，让类型过**

`src/ui/state/index.ts`：把 `openSettingsSection` 那一行换成 `打开设置整页, 选设置分类, loadSettingsSection, SETTINGS_SECTION_KEY`。
`src/ui/App.tsx`：`import` 与 `actions.openSettingsSection` 暂时改成 `打开设置整页`（Task 6 会再改成开列），`onSelect={(id) => $settingsSection.set(id)}`（约 4044 行）改成 `onSelect={选设置分类}`。
在已有 `loadRightDock()` 的调用旁边加 `loadSettingsSection()`。

```bash
npm run typecheck
```

期望：无错误。

- [ ] **Step 6: 提交**

```bash
git add src/ui/state/view.ts src/ui/state/index.ts src/ui/App.tsx tests/ui/settings-section-persist.test.ts
git commit -m "feat(settings): remember which settings section was last open"
```

---

## Task 3：状态模块 `settings-column.ts`

**互斥与回程的全部规则只住在这一个文件里。** 纯逻辑，能单独验。

**Files:**
- Create: `src/ui/state/settings-column.ts`
- Create: `tests/ui/settings-column.test.ts`
- Modify: `src/ui/state/index.ts`

- [ ] **Step 1: 写失败的单元测试**

```ts
// tests/ui/settings-column.test.ts
/**
 * 右边那一列的一条规则（规格 `2026-09-16-设置右栏`）：
 *
 * > **设置活着的时候，右边那一列归设置；设置一关，上一位房客回来。**
 *
 * 作者原话：*「如果右边有面板的话，那么恢复之后是有面板的；
 * 如果右边没有面板的话，那么恢复之后就没面板。」*
 */
import { beforeEach, describe, expect, it } from "vitest"
import { $view } from "../../src/ui/state/view.js"
import { $rightDockOpen, $rightDockTenant } from "../../src/ui/state/right-dock.js"
import {
  $settingsColumnOpen,
  关掉设置,
  开设置栏,
  展开设置,
  收起设置,
} from "../../src/ui/state/settings-column.js"
import { $settingsSection } from "../../src/ui/state/view.js"

beforeEach(() => {
  localStorage.clear()
  $view.set("conversation")
  $rightDockOpen.set(false)
  $rightDockTenant.set("files")
  $settingsColumnOpen.set(false)
  $settingsSection.set(undefined)
})

describe("设置栏与坞互斥", () => {
  it("坞开着 → 开设置 → 坞让位", () => {
    $rightDockOpen.set(true)
    $rightDockTenant.set("notebook")
    开设置栏()
    expect($settingsColumnOpen.get()).toBe(true)
    expect($rightDockOpen.get()).toBe(false)
  })

  it("**关掉设置，被顶掉的那一位自己回来**", () => {
    $rightDockOpen.set(true)
    $rightDockTenant.set("notebook")
    开设置栏()
    关掉设置()
    expect($settingsColumnOpen.get()).toBe(false)
    expect($rightDockOpen.get()).toBe(true)
    expect($rightDockTenant.get()).toBe("notebook")
  })

  it("**来时右边空的，走时右边就空的**——不是「关掉设置就给你开个面板」", () => {
    开设置栏()
    关掉设置()
    expect($rightDockOpen.get()).toBe(false)
  })

  it("开设置栏可以直接钻进某一项（命令面板那条路）", () => {
    开设置栏("mcp")
    expect($settingsSection.get()).toBe("mcp")
    expect($settingsColumnOpen.get()).toBe(true)
  })
})

describe("展开与收起", () => {
  it("展开 → 整页，而那一列**空着**（作者选的）", () => {
    $rightDockOpen.set(true)
    $rightDockTenant.set("files")
    开设置栏("models")
    展开设置()
    expect($view.get()).toBe("settings")
    expect($settingsColumnOpen.get()).toBe(false)
    expect($rightDockOpen.get()).toBe(false) // 不是把面板放回来
    expect($settingsSection.get()).toBe("models") // 选中项不变
  })

  it("收起 → 回到那一列，选中项仍然不变", () => {
    开设置栏("models")
    展开设置()
    收起设置()
    expect($view.get()).toBe("conversation")
    expect($settingsColumnOpen.get()).toBe(true)
    expect($settingsSection.get()).toBe("models")
  })

  it("**从整页直接关掉，被顶掉的那一位照样回来**", () => {
    $rightDockOpen.set(true)
    $rightDockTenant.set("artifacts")
    开设置栏()
    展开设置()
    关掉设置()
    expect($view.get()).toBe("conversation")
    expect($rightDockOpen.get()).toBe(true)
    expect($rightDockTenant.get()).toBe("artifacts")
  })
})
```

- [ ] **Step 2: 跑，确认红**

```bash
npx vitest run tests/ui/settings-column.test.ts
```

期望：失败，`src/ui/state/settings-column.js` 不存在。

- [ ] **Step 3: 写 `src/ui/state/settings-column.ts`**

```ts
/**
 * 设置那一栏（2026-09-16，规格 `2026-09-16-设置右栏-design.md`）。**渲染进程自有。**
 *
 * 作者：*「我想要的是初始是类似于点击面板后的效果，然后也有一个按钮，可以展开。」*
 *
 * ## 它为什么不自己造一条栏
 *
 * 设置**住进右侧坞那一列**（`--dawn-dock-w`，280/380/720 那条缝照用）。
 * 互斥之后右边同时只有一位房客，两套列宽只会打架；而且宽度是
 * 「**这个人喜欢右边多宽**」，不是「设置喜欢多宽」——与侧栏宽度、主题同一类。
 *
 * ## 一条规则
 *
 * > **设置活着的时候，右边那一列归设置；设置一关，上一位房客回来。**
 *
 * 作者原话：*「如果右边有面板的话，那么恢复之后是有面板的；如果右边没有面板的话，
 * 那么恢复之后就没面板。」* —— 所以记的是「**上一位是谁**」，不是无脑开面板。
 *
 * 展开到整页时那一列**空着**（作者选的）。这与 `right-dock.ts` 里记着的
 * 「坞不随左半的屏切换而收起」不一致，是这一轮明确改掉的：设置此刻是主角，
 * 右边再挂一栏是分心。**别当成 bug 修回去。**
 *
 * ## 三份状态，零份持久化
 *
 * | | 持久化 | 为什么 |
 * |---|---|---|
 * | 开着没有 | ❌ | 它是**这个窗口此刻的样子**，不是偏好（与 `$rightDockOpen` 同一条） |
 * | 被顶掉的是谁 | ❌ | 只为这一次回程活着，重启之后无意义 |
 *
 * 「当前是哪一项」持久化，但它住在 `view.ts`（`$settingsSection`）——
 * 整页也要读它，**两个形状共用同一个事实**。
 */
import { atom, computed } from "nanostores"
import { setValue } from "./identity.js"
import { $view, setView, 选设置分类 } from "./view.js"
import { $rightDockOpen, $rightDockTenant, setRightDockOpen, setRightDockTenant, type 坞房客 } from "./right-dock.js"

export const $settingsColumnOpen = atom(false)

/**
 * 被设置顶掉的那一位。**`undefined` 意思是「来的时候右边本来就空着」**，
 * 不是「不知道」——所以关掉设置时它照样是一条明确的指令：右边继续空着。
 */
export const $被顶掉的房客 = atom<坞房客 | undefined>(undefined)

/**
 * 设置此刻在不在场（窄栏或整页，两种都算）。
 *
 * **必须是 `computed`，不能是普通函数**（2026-09-16 审查定的）：它有两个读者——
 * 事件处理器走 `.get()`，侧栏那颗按钮的高亮走 `useStore`。写成函数，
 * 高亮那一半就永远不会刷新，而那是「看得见的状态跟事实对不上」。
 */
export const $设置在场 = computed([$settingsColumnOpen, $view], (开着, 屏) => 开着 || 屏 === "settings")

/**
 * 开设置栏。给了 `section` 就**直接钻进那一项**（命令面板、侧栏那几条快捷入口）。
 *
 * **只在真的顶掉了谁的时候才记**：坞本来就关着时记成 `undefined`，
 * 于是关掉设置之后右边继续空着——这正是作者要的那一半。
 */
export function 开设置栏(section?: string): void {
  if (section !== undefined) 选设置分类(section)
  if ($rightDockOpen.get()) {
    // **真的顶掉了一位**：设置在不在场都不重要，记下的就该是这一位
    setValue($被顶掉的房客, $rightDockTenant.get())
    setRightDockOpen(false)
  } else if (!$设置在场.get()) {
    // 设置从无到有，而右边本来就空着 → 明确记下「走的时候也空着」
    setValue($被顶掉的房客, undefined)
  }
  // 设置此刻的形状由这个函数说了算：整页上再调它，整页要让位，
  // 否则窄栏与整页会同屏（审查实测过的 C2）
  if ($view.get() === "settings") setView("conversation")
  setValue($settingsColumnOpen, true)
}

/**
 * 坞要上位：设置让开。**反方向的互斥也只能住在这个文件里**——
 * `right-dock.ts` 反过来 import 会成环，散回 `App.tsx` 就等于没有唯一的规则。
 *
 * 清掉回程记忆：人这一次是**自己挑了一个房客**，不是「设置关掉、恢复上一位」。
 */
export function 坞上位(): void {
  setValue($settingsColumnOpen, false)
  setValue($被顶掉的房客, undefined)
}

/**
 * 关掉设置（窄栏那颗 ✕，或侧栏那颗再点一次）。**窄栏和整页都由它收场**——
 * 写成两条的话，从整页关掉时房客就回不来了。
 */
export function 关掉设置(): void {
  setValue($settingsColumnOpen, false)
  if ($view.get() === "settings") $view.set("conversation")
  const 上一位 = $被顶掉的房客.get()
  setValue($被顶掉的房客, undefined)
  if (上一位 === undefined) return
  setRightDockTenant(上一位)
  setRightDockOpen(true)
}

/**
 * ⤢ 展开成整页。**那一列空着**（作者选的），而「被顶掉的是谁」要留着——
 * 人还可能收起来、或者直接关掉，那两条路都得还得回去。
 */
export function 展开设置(): void {
  setValue($settingsColumnOpen, false)
  $view.set("settings")
}

/** ⤡ 收起，回到那一列。选中项不动——两个形状共用 `$settingsSection` */
export function 收起设置(): void {
  $view.set("conversation")
  setValue($settingsColumnOpen, true)
}
```

- [ ] **Step 4: 跑，确认绿**

```bash
npx vitest run tests/ui/settings-column.test.ts
```

期望：15 条全绿（上面列的 7 条是最小集；审查两轮又加了 8 条：命令面板跳分类不许抹掉回程记忆、整页上开窄栏整页要让位、真的顶掉了就必须记账、`坞上位` 两条、`$设置在场` 的响应式与两种形状、`关掉设置` 读的是记下的房客而不是坞此刻挂着谁）。

- [ ] **Step 5: 从 `state/index.ts` 转出**

在 `src/ui/state/index.ts` 里按现有写法加一段转出：

```ts
export {
  $settingsColumnOpen,
  $被顶掉的房客,
  $设置在场,
  开设置栏,
  关掉设置,
  展开设置,
  收起设置,
  坞上位,
} from "./settings-column.js"
```

```bash
npm run typecheck
```

期望：无错误。

- [ ] **Step 6: 提交**

```bash
git add src/ui/state/settings-column.ts src/ui/state/index.ts tests/ui/settings-column.test.ts
git commit -m "feat(settings): add the settings-column state with dock hand-back"
```

---

## Task 4：把 `sections` 提成一个变量

**纯搬移，一行逻辑都不改。** 单独一个提交，好让后面真出问题时能二分。

**Files:**
- Modify: `src/ui/App.tsx:3683-4050` 一带（`<SettingsShell sections={[…]}>` 那一大块）

- [ ] **Step 1: 提出来**

把 `view === "settings"` 分支里 `<SettingsShell sections={[ … ]} …/>` 的**那个数组字面量**原样搬到组件体内、`return` 之前，变成：

```tsx
  /**
   * 设置的十四块。**一份两用**（2026-09-16）：整页 `SettingsShell` 与窄栏
   * `SettingsColumn` 读同一份、同一个「当前选中」。
   *
   * 复制成两份的那一刻，「窄栏里少了一项」就会变成一个没人发现的缺陷——
   * 而这正是「两处长得一样的东西，等于没有判据」那条的反面：
   * **同一件事只有一个来源**。
   */
  const 设置分区: SettingsSection[] = [
    /* …原样搬过来，一个字不改… */
  ]
```

然后 `view === "settings"` 那处写成 `<SettingsShell sections={设置分区} selected={设置分类} onSelect={选设置分类} />`。

`SettingsSection` 从 `./Settings.js` 引入（已有导出）。

> **不要顺手改任何一块的内容。** 这一步唯一的判据是「e2e 一条都不少」。

- [ ] **Step 2: 类型与单元**

```bash
npm run typecheck && npm test 2>&1 | grep -E "Test Files|Tests "
```

期望：typecheck 无错误，单元全绿。

- [ ] **Step 3: 跑所有碰设置的 e2e**

```bash
npm run build && npx playwright test e2e/mcp.spec.ts e2e/skills.spec.ts e2e/memory.spec.ts e2e/subagent-roster.spec.ts e2e/settings-counts.spec.ts --reporter=line
```

期望：全绿。**红了就是搬漏了一块，不要往下走。**

- [ ] **Step 4: 提交**

```bash
git add src/ui/App.tsx
git commit -m "refactor(settings): lift the sections array so two layouts can share it"
```

---

## Task 5：`SettingsColumn` 组件与样式

**Files:**
- Create: `src/ui/settings-column.tsx`
- Modify: `src/ui/styles.css`（`.right-dock` 那一族后面，约 5150 行）
- Modify: `src/ui/i18n/en.ts`

- [ ] **Step 1: 写组件**

```tsx
// src/ui/settings-column.tsx
/**
 * 设置那一栏（2026-09-16，规格 `2026-09-16-设置右栏-design.md`）。
 *
 * **它是整页那两栏「在时间上错开」，不是另一套页面。** 整页是
 * `分类列 | 内容` 并排；这里是同样的分类、同样的分组、同样的顺序、同样的计数，
 * 只是一次只画一个。所以 `sections` 是同一份（`App.tsx` 的 `设置分区`）。
 *
 * ## 「我在哪一块」由谁回答
 *
 * 整页里那一列分类一直亮着，这个问题不用想。窄栏里由**标题 + 返回箭头**扛。
 * 300px 宽的栏里这是标准取舍：一次只答一个问题。
 *
 * ## 这个文件只管画
 *
 * 开合、互斥、回程全在 `state/settings-column.ts`。**这里一个 store 都不写**——
 * 状态散进组件之后，「关掉设置为什么没把面板还回来」就没有唯一的地方可查。
 */
import { Fragment } from "react"
import { Button } from "./primitives.js"
import type { SettingsSection } from "./Settings.js"
import { t } from "./i18n/index.js"

export function SettingsColumn({
  sections,
  selected,
  onSelect,
  onBack,
  onExpand,
  onClose,
}: {
  sections: SettingsSection[]
  /**
   * `undefined` = 停在名单上。**缺失就是「还没挑」**，不是某个具体分类。
   *
   * **认不出来的 id 也退回名单**（2026-09-16 审查提的）：`loadSettingsSection`
   * 不校验存下来的字符串——校验就得在 state 里再存一份分类 id 名单，
   * 而「同一份名单两个地方各存一遍」正是本项目当成「没有判据」的那种重复。
   * 所以判据放在这儿：`find` 找不到就当没挑过。下面那行 `?? undefined` 就是它。
   */
  selected: string | undefined
  onSelect: (id: string) => void
  onBack: () => void
  onExpand: () => void
  onClose: () => void
}) {
  const 当前 = selected === undefined ? undefined : (sections.find((s) => s.id === selected) ?? undefined)
  return (
    <aside className="settings-column" aria-label={t("设置")}>
      <header className="dock-head">
        {当前 ? (
          <Button variant="ghost" size="icon" className="settings-column-back" onClick={onBack} aria-label={t("返回设置名单")}>
            ‹
          </Button>
        ) : null}
        <h2 className="dock-title">{当前 ? 当前.title : t("设置")}</h2>
        <div className="settings-column-tools">
          {/* **可见文案 = 可及名字**（`dock.tsx` 那条纪律）：不另写 `aria-label`，
              免得眼睛读到的与读屏念到的不是一句话 */}
          <Button variant="ghost" size="sm" className="settings-column-expand" onClick={onExpand}>
            {t("展开")}
          </Button>
          <Button variant="ghost" size="icon" className="dock-close" onClick={onClose} aria-label={t("关闭设置")}>
            ✕
          </Button>
        </div>
      </header>
      <div className="dock-body">
        {当前 ? (
          当前.body
        ) : (
          /**
           * 名单。**沿用 `settings-nav-item` 现成的长相与 `side-count`**——
           * 同一种东西不该有两种样子，而且整页那边改了这边自动跟上。
           */
          <nav className="settings-column-list" aria-label={t("设置分类")}>
            {sections.map((s, i) => (
              <Fragment key={s.id}>
                {s.group && sections[i - 1]?.group !== s.group ? (
                  <p className="settings-nav-group">{s.group}</p>
                ) : null}
                <Button
                  variant="ghost"
                  size="sm"
                  className="row settings-nav-item"
                  onClick={() => onSelect(s.id)}
                >
                  {s.icon}
                  <span className="name">{s.title}</span>
                  {s.count !== undefined ? (
                    <span className="side-count" aria-hidden="true">
                      {s.count}
                    </span>
                  ) : null}
                  <span className="settings-column-arrow" aria-hidden="true">
                    ›
                  </span>
                </Button>
              </Fragment>
            ))}
          </nav>
        )}
      </div>
    </aside>
  )
}
```

- [ ] **Step 2: 样式**

在 `styles.css` 里 `.right-dock .dock-body { … }` 之后加：

```css
/* ── 设置那一栏（2026-09-16，规格 `2026-09-16-设置右栏`） ── */
/**
 * **它与 `.right-dock` 共用同一列、同一条缝**，所以外形也照抄那一套：
 * 一条发丝线、面板底色、占满网格给它的那一列。
 * 抄的是长相不是规则——开合与互斥住在 `state/settings-column.ts`。
 */
.settings-column {
  display: flex;
  flex-direction: column;
  width: 100%;
  min-width: 0;
  min-height: 0;
  border-left: var(--dawn-hairline) solid var(--dawn-stroke-2);
  background: var(--dawn-surface-panel);
  position: relative;
}
.settings-column .dock-head { gap: var(--dawn-space-1); }
.settings-column .dock-title { flex: 1; min-width: 0; overflow: hidden; text-overflow: ellipsis; }
.settings-column-tools { display: flex; align-items: center; gap: var(--dawn-space-1); flex: 0 0 auto; }
.settings-column-back { color: var(--dawn-text-3); flex: 0 0 auto; }
.settings-column-back:hover { color: var(--dawn-text-1); }
.settings-column-list { display: flex; flex-direction: column; gap: 2px; }
/* 行尾那个 `›`：**它是「点进去还有东西」的记号**，不是装饰——
   没有它，带计数的行和不带的行看起来像两种控件 */
.settings-column-arrow { margin-left: var(--dawn-space-1); color: var(--dawn-text-4); flex: 0 0 auto; }
/* 窄栏里那些内容页本来是照整页宽度做的：解开它们的宽度上限与外距，
   让它们老老实实铺满这一列（真装不下的那几块由 Task 7 量出来再单独修） */
.settings-column .dock-body > * { max-width: none; }
```

- [ ] **Step 3: 英文文案**

`src/ui/i18n/en.ts` 里加四条（按文件现有的排序位置插入）：

```ts
  "展开": "Expand",
  "关闭设置": "Close settings",
  "返回设置名单": "Back to settings list",
  "收起": "Collapse",
```

（`"设置"` / `"设置分类"` 已有，不重复加。）

- [ ] **Step 4: 类型 + i18n 扫描**

```bash
npm run typecheck && npx vitest run tests/ui/i18n.test.ts
```

期望：typecheck 无错误；i18n 扫描全绿（新文案有英文、且没有孤儿）。

- [ ] **Step 5: 提交**

```bash
git add src/ui/settings-column.tsx src/ui/styles.css src/ui/i18n/en.ts
git commit -m "feat(settings): add the narrow settings column component"
```

---

## Task 6：接进 App，并把 26 支既有 e2e 护住

**这一步会改掉「点设置」的行为，是全计划风险最高的一步。**
`e2e/fixtures.ts` 的 `进设置` 被 **26 支** spec 用着，它现在靠 `.settings-nav`（整页那一列）判断在不在设置里。
不同步改，就是这个项目栽过的那种「十几条 e2e 同时红」。

**Files:**
- Modify: `src/ui/App.tsx`
- Modify: `src/ui/Settings.tsx`（`SettingsShell` 加一颗 ⤡）
- Modify: `e2e/fixtures.ts:229-233`
- Test: `e2e/settings-column.spec.ts`（新建）

- [ ] **Step 1: 写失败的 e2e**

```ts
// e2e/settings-column.spec.ts
/**
 * 设置先当一条栏（2026-09-16，规格 `2026-09-16-设置右栏-design.md`）。
 *
 * 作者：*「我们点击设置的时候，其实设置页面整体就弹出来了，其实我想要的是
 * 初始是类似于点击面板后的效果，然后也有一个按钮，可以展开。」*
 *
 * 五条判据各自对着一件会悄悄坏掉的事。
 */
import { test, expect, 开一段临时会话 } from "./fixtures.js"

/** 侧栏那颗「设置」。**精确匹配**——「设置」是别处按钮文案的子串（那条踩过） */
const 设置键 = (page: import("@playwright/test").Page) =>
  page.getByRole("button", { name: "设置", exact: true })

test("**点设置 → 右边出现一条栏，而对话还在**", async ({ dawn }) => {
  const { page } = dawn
  await 开一段临时会话(page, "先说一句")
  await 设置键(page).click()
  await expect(page.locator(".settings-column")).toBeVisible()
  // 对话没有被顶掉——这是整件事的全部意义
  await expect(page.locator(".turns")).toBeVisible()
  // 还没挑，停在名单上
  await expect(page.locator(".settings-column-list")).toBeVisible()
})

test("**名单 → 点一项 → 换成那一项；‹ 回名单**", async ({ dawn }) => {
  const { page } = dawn
  await 开一段临时会话(page, "先说一句")
  await 设置键(page).click()
  await page.locator(".settings-column-list").getByRole("button", { name: "外观", exact: true }).click()
  await expect(page.locator(".settings-column-list")).toHaveCount(0)
  await expect(page.locator(".settings-column .dock-title")).toHaveText("外观")
  await page.locator(".settings-column-back").click()
  await expect(page.locator(".settings-column-list")).toBeVisible()
})

test("**⤢ 展开 → 整页，选中项不变；⤡ 收起 → 回栏，仍是同一项**", async ({ dawn }) => {
  const { page } = dawn
  await 开一段临时会话(page, "先说一句")
  await 设置键(page).click()
  await page.locator(".settings-column-list").getByRole("button", { name: "内核", exact: true }).click()

  await page.getByRole("button", { name: "展开", exact: true }).click()
  await expect(page.locator(".settings-shell")).toBeVisible()
  await expect(page.locator(".settings-column")).toHaveCount(0)
  await expect(page.locator(".settings-nav-item.current")).toHaveText(/内核/)

  await page.getByRole("button", { name: "收起", exact: true }).click()
  await expect(page.locator(".settings-column")).toBeVisible()
  await expect(page.locator(".settings-column .dock-title")).toHaveText("内核")
})

test("**坞开着 → 开设置 → 坞让位 → 关掉设置 → 坞自己回来**", async ({ dawn }) => {
  const { page } = dawn
  await 开一段临时会话(page, "先说一句")
  await page.getByRole("button", { name: "面板", exact: true }).click()
  await expect(page.locator(".right-dock")).toBeVisible()

  await 设置键(page).click()
  await expect(page.locator(".settings-column")).toBeVisible()
  await expect(page.locator(".right-dock")).toHaveCount(0)

  await page.locator(".settings-column .dock-close").click()
  await expect(page.locator(".settings-column")).toHaveCount(0)
  await expect(page.locator(".right-dock")).toBeVisible()
})

test("**设置栏开着时点「面板」，设置让开、面板真的出来**", async ({ dawn }) => {
  const { page } = dawn
  await 开一段临时会话(page, "先说一句")
  await 设置键(page).click()
  await expect(page.locator(".settings-column")).toBeVisible()
  // 反方向的互斥（2026-09-16 审查抓的）：缺了它这一下就是「点了没反应」——
  // 状态里坞开了，而屏幕上还是设置栏
  await page.getByRole("button", { name: "面板", exact: true }).click()
  await expect(page.locator(".right-dock")).toBeVisible()
  await expect(page.locator(".settings-column")).toHaveCount(0)
})

test("**来时右边空的，走时右边就空的**", async ({ dawn }) => {
  const { page } = dawn
  await 开一段临时会话(page, "先说一句")
  await expect(page.locator(".right-dock")).toHaveCount(0)
  await 设置键(page).click()
  await page.locator(".settings-column .dock-close").click()
  // 不是「关掉设置就给你开个面板」
  await expect(page.locator(".right-dock")).toHaveCount(0)
  await expect(page.locator(".settings-column")).toHaveCount(0)
})
```

- [ ] **Step 2: 跑，确认六条全红**

```bash
npm run build && npx playwright test e2e/settings-column.spec.ts --reporter=line
```

期望：`6 failed`，都是 `.settings-column` 找不到。

- [ ] **Step 3: `SettingsShell` 加那颗 ⤡**

`src/ui/Settings.tsx`：`SettingsShell` 的 props 加一个可选项，并在 `.settings-body-title` 那一行右侧画按钮。

```tsx
export function SettingsShell({
  sections,
  selected,
  onSelect,
  onCollapse,
}: {
  sections: SettingsSection[]
  selected?: string | undefined
  onSelect?: ((id: string) => void) | undefined
  /**
   * 收回那一栏（2026-09-16）。**可选**——整页在「连不上」那条路上也会出现，
   * 那时没有栏可以收回去，就不画这颗。
   * 「看不见的能力等于不存在」的反面同样成立：**没有的能力不许画一颗按钮**。
   */
  onCollapse?: (() => void) | undefined
}) {
```

把 `<h1 className="settings-body-title">{当前.title}</h1>` 换成：

```tsx
        <div className="settings-body-head">
          <h1 className="settings-body-title">{当前.title}</h1>
          {onCollapse ? (
            <Button variant="ghost" size="sm" className="settings-collapse" onClick={onCollapse}>
              {t("收起")}
            </Button>
          ) : null}
        </div>
```

`styles.css` 里加：

```css
/* 整页设置标题那一行：标题在左，「收起」在右（2026-09-16） */
.settings-body-head { display: flex; align-items: baseline; justify-content: space-between; gap: var(--dawn-space-3); }
```

- [ ] **Step 4: 接进 `App.tsx`**

① 引入：`开设置栏, 关掉设置, 展开设置, 收起设置, 坞上位, $设置在场, $settingsColumnOpen`，
并 `const 设置栏开着 = useStore($settingsColumnOpen)`、`const 设置在场 = useStore($设置在场)`。

**`设置在场` 必须走 `useStore($设置在场)`，不许在这里写 `设置栏开着 || view === "settings"`**
（2026-09-16 审查定的）：那是把互斥规则复制到第二个地方，而
`state/settings-column.ts` 存在的全部理由就是它只住在一处。

② 侧栏那颗（约 3622 行）：

```tsx
          /**
           * **2026-09-16 起它开的是右边那一栏，不再换整屏。**
           * 「再点一次就回去」那条保留——一个亮着的入口点下去毫无反应，
           * 人会以为它坏了（2026-08-11 作者提的）。
           */
          onOpenSettings={() => ($设置在场.get() ? 关掉设置() : 开设置栏())}
          settingsActive={设置在场}
```

③ `actions`（约 3100 行）：

```tsx
      openSettings: () => 开设置栏(),
      openSettingsSection: (id) => 开设置栏(id),
```

④ 网格列宽（约 3385 行）——**两种房客有一个在就得给宽度**：

```tsx
            "--dawn-dock-w": rightDockOpen || 设置栏开着 ? `${rightDockWidth}px` : "0px",
```

⑤ 那一列渲染处（约 4521 行）：把 `{rightDockOpen ? (<RightDock …>) : null}` 改成

```tsx
        {设置栏开着 ? (
          <>
            <SideSash
              width={rightDockWidth}
              min={RIGHT_DOCK_MIN}
              max={RIGHT_DOCK_MAX}
              onResize={(px, phase) => setRightDockWidth(px, 侧栏此刻多宽, phase === "commit")}
              side="right"
              attach="edge"
              label={t("调整面板宽度")}
            />
            <SettingsColumn
              sections={设置分区}
              selected={设置分类}
              onSelect={选设置分类}
              onBack={() => 选设置分类(undefined)}
              onExpand={展开设置}
              onClose={关掉设置}
            />
          </>
        ) : rightDockOpen ? (
          <RightDock …原样… >
        ) : null}
```

**import 的现状**（查过了，别漏）：`SideSash` 在 App.tsx 第 41 行**已经引入**，
`RIGHT_DOCK_MAX` 在第 196 行**已经引入**，但 **`RIGHT_DOCK_MIN` 没有**——
上面那段 `<SideSash>` 用到了它，要补进那个 import 块。

⑥ **八处开坞的地方，一律先让设置让开**（2026-09-16 审查抓的，缺了它就是「点了没反应」）。

`App.tsx` 里开坞有八处：`点开房客("files")`（约 408）、`点开房客("review")`（约 411）、
以及 `setRightDockTenant(x); setRightDockOpen(true)` 四处（约 1121 / 2571 / 3110 / 4198），
外加侧栏与快捷键共用的 `点开房客`。**没有一处会关掉设置栏**，而第 ⑤ 步的渲染是
「设置栏开着就画设置栏」——于是设置栏开着时点「面板」，状态里坞开了、**屏幕上什么都不变**。
本项目 2026-08-10 与 08-11 栽的就是这个症状：代码是好的，作者说「没有这个功能」。

每一处在开坞之前先调一句 `坞上位()`（Task 3 建的，反方向的互斥也只住在那一个文件里）。
**逐个数过去，八处一个都不能漏**——漏掉的那一处的症状正是「点了没反应」。

⑦ 整页那处（约 3683 行）传上 `onCollapse={收起设置}`。
**`ConnectionSurface` 那处不传**——那条路上没有栏可以收回去（`onOpenSettings` 仍走 `打开设置整页`）。

- [ ] **Step 5: 同步改夹具（**这一步不做就会 26 支同时红**）**

`e2e/fixtures.ts:229-233` 换成：

```ts
/**
 * 进设置的某一类。
 *
 * **2026-09-16 起「设置」开的是右边那一栏**，而这 26 支用例验的都是整页里那些面板，
 * 所以这里开完栏立刻 `展开`，让它们一个字都不用改。
 * 要验窄栏本身的用例走 `e2e/settings-column.spec.ts`，不走这条。
 */
export async function 进设置(page: Page, 分类: string): Promise<void> {
  if ((await page.locator(".settings-nav").count()) === 0) {
    if ((await page.locator(".settings-column").count()) === 0) {
      await page.getByRole("button", { name: "设置", exact: true }).click()
    }
    await page.getByRole("button", { name: "展开", exact: true }).click()
  }
  await page.locator(".settings-nav").getByRole("button", { name: 分类, exact: true }).click()
}
```

- [ ] **Step 6: 跑新用例 + 全部碰设置的旧用例**

```bash
npm run build && npx playwright test e2e/settings-column.spec.ts --reporter=line
```

期望：`6 passed`。然后：

```bash
npx playwright test $(grep -rl "进设置" e2e/*.spec.ts | tr '\n' ' ') --reporter=line
```

期望：全绿。**任何一条红都先怀疑夹具那一步，不要去改用例。**

- [ ] **Step 7: 提交**

```bash
git add src/ui/App.tsx src/ui/Settings.tsx src/ui/styles.css e2e/fixtures.ts e2e/settings-column.spec.ts
git commit -m "feat(settings): open settings as a right column, expandable to the full page"
```

---

## Task 7：量一遍窄栏里的十四块，只修真坏的

规格里那句「先量，再改」的自动化形式。**这一条会替我们指出哪几块装不下，不必现在猜。**

**Files:**
- Modify: `e2e/settings-column.spec.ts`（追加一条）
- Modify: `src/ui/styles.css`（只改量出来真坏的那几块）

- [ ] **Step 1: 写那条量尺**

```ts
/**
 * **窄栏里不许横向溢出**（规格「先量，再改」）。
 *
 * 这不是一条设计偏好，是一条**量得出来的结论**：横向溢出的症状是
 * 右半截内容跑到看不见的地方，而屏幕上没有任何东西说这件事发生了——
 * 与 2026-08-19 坞越界那次一模一样。
 *
 * 380 是右栏的默认宽度（`RIGHT_DOCK_DEFAULT`）。人可以拖到 720，
 * 但**第一眼看到的是 380**，所以判据钉在这儿。
 */
test("**十四块在默认宽度下都不横向溢出**", async ({ dawn }) => {
  const { app, page } = dawn
  await 开一段临时会话(page, "先说一句")
  await app.evaluate(({ BrowserWindow }) => BrowserWindow.getAllWindows()[0]?.setSize(1400, 900))
  await 设置键(page).click()
  await expect(page.locator(".settings-column-list")).toBeVisible()

  const 名字们 = await page.locator(".settings-column-list .settings-nav-item .name").allTextContents()
  const 挤的: string[] = []
  for (const 名 of 名字们) {
    await page.locator(".settings-column-list").getByRole("button", { name: 名, exact: true }).click()
    await page.waitForTimeout(250)
    const 溢 = await page.evaluate(() => {
      const el = document.querySelector(".settings-column .dock-body") as HTMLElement
      return el.scrollWidth - el.clientWidth
    })
    if (溢 > 1) 挤的.push(`${名}：横着多出 ${溢}px`)
    await page.locator(".settings-column-back").click()
  }
  expect(挤的, "这几块在 380px 下装不下——改 CSS，别拿「人可以拖宽」当答案").toEqual([])
})
```

- [ ] **Step 2: 跑，看它报出哪几块**

```bash
npm run build && npx playwright test e2e/settings-column.spec.ts --grep "不横向溢出" --reporter=line
```

期望：**要么直接绿**（那就说明规格里那句「大概率没几块要改」是对的，跳到 Step 4），
要么报出一份具体名单，例如 `["用量：横着多出 142px"]`。

- [ ] **Step 3: 只修报出来的那几块**

对名单里的每一块，在 `styles.css` 里**只在 `.settings-column` 作用域内**收窄，
整页的样子一个像素不动。

**选择器要从真实 DOM 里查出来，不许照抄下面这个例子里的类名**——它是格式示范，
`.usage-table` 这个类现在并不存在。查法：在 Step 2 报出名单之后，开着 `npm run app`
点进那一块，用 DevTools 找到真正横着撑出去的那个元素（`scrollWidth > clientWidth` 的那一个）。

```css
/* 格式示范：<那一块> 在 380px 的栏里横着多出 <N>px（2026-09-16 量的）：<怎么改的> */
.settings-column <真实选择器> { display: flex; flex-direction: column; gap: var(--dawn-space-2); }
.settings-column <真实选择器> > * { width: 100%; }
```

> **不要用 `overflow-x: auto` 把它藏起来。** 那是把「装不下」改写成「你自己去找」，
> 与规格 7.5「失败必须出声」正面冲突。

改完重跑 Step 2 的命令，直到绿。

- [ ] **Step 4: 整套 e2e**

```bash
npm run test:e2e 2>&1 | tail -20
```

期望：只剩 `remote-kernel.spec.ts` 那条图的用例红（本机 matplotlib 环境问题，与本轮无关；把改动 stash 掉照样红）。
**任何别的红都是这一轮弄的，先查再走。**

- [ ] **Step 5: 提交**

```bash
git add e2e/settings-column.spec.ts src/ui/styles.css
git commit -m "test(settings): assert no horizontal overflow in the narrow column"
```

---

## Task 8：视觉基线

**Files:**
- Modify: `e2e/visual.spec.ts`

- [ ] **Step 1: 加一屏**

在 `e2e/visual.spec.ts` 的 `SCREENS` 数组末尾（「概览」那一项之后）追加：

```ts
  {
    /**
     * 设置那一栏（2026-09-16）。截的是**名单那一屏**：它是点开设置第一眼
     * 看到的样子，而且「扩展」那一组的行尾计数只在这一屏上看得见。
     */
    name: "设置栏",
    go: async (page) => {
      await expect(page.getByPlaceholder(/今天帮你做些什么/)).toBeVisible()
      await page.getByRole("button", { name: "设置", exact: true }).click()
      await expect(page.locator(".settings-column-list")).toBeVisible()
      /**
       * **等那个数真的到了再截。** 计数要等一次后端往返，只等名单画出来
       * 会截到「数字还没来」的那一帧——而那一帧和「这行本来就没数」长得一样，
       * 于是基线里钉住的是一个错的事实。
       */
      await expect(
        page.locator(".settings-nav-item").filter({ hasText: "Skills" }).locator(".side-count"),
      ).toBeVisible()
    },
  },
```

已有的 `mask` 列表照旧生效（时钟、模型目录条数这类会自己变的都在里面）。
**这一屏如果引入了新的会自己变的东西，同轮加进 `mask`**——不遮的话它每过一分钟红一次，
而那正是把人训练成条件反射按 update 的最快方式。

- [ ] **Step 2: 存基线**

```bash
npm run test:e2e:visual:update
```

**只能用这条**（它带 `=all`）。手敲不带 `=all` 的那句只会重写比对失败的那几张。

- [ ] **Step 3: 存完必须再验两遍**

```bash
npm run test:e2e:visual && npm run test:e2e:visual:only
```

期望：两遍都绿。某一张重存完仍稳定红时**别再 update**——
把验证那一轮 `test-results/<用例>/<名字>-actual.png` 拷成基线，再验两遍
（2026-08-22 命令面板那张就是这样救回来的）。

- [ ] **Step 4: 提交**

```bash
git add e2e/visual.spec.ts e2e/__screenshots__
git commit -m "test(visual): add a baseline for the settings column"
```

---

## Task 9：收尾

- [ ] **Step 1: 全套**

```bash
npm run typecheck && npm test 2>&1 | grep -E "Test Files|Tests " && npm run test:e2e 2>&1 | tail -5
```

- [ ] **Step 2: 自己打开看一次**

```bash
npm run app
```

**「测试绿了」不等于「能用了」**——419 个测试全绿的那一版，打开之后点什么都没反应。
要自己走一遍的四条：点设置 → 栏出来且对话还在；下钻/返回；展开/收起；开着面板时点设置再关掉，面板回来。

- [ ] **Step 3: 写开发历史**

按 `docs/DEVELOPMENT_HISTORY.md` 顶部的模板追加一条（`Type: feat`），
把「作者选的那四件事」与「MCP 计数为什么数开着的」写进 `What`。

- [ ] **Step 4: 提交**

```bash
git add docs/DEVELOPMENT_HISTORY.md
git commit -m "docs(history): settings as a right column, plus MCP and plugin counts"
```

**不要 push。** 作者自己推。
